---
title: Présentation de StoServe
description: L’exemple de code StoServe montre comment utiliser les services de stockage structurés fournis dans l’implémentation de fichiers composés. L’utilisation des interfaces IStorage et IStream standard est décrite.
ms.assetid: 41ccd333-15c8-46b2-91c6-3e1929f7198c
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 9c0c8325fbc20d4917785d0b83ca70bffa996824
ms.sourcegitcommit: 592c9bbd22ba69802dc353bcb5eb30699f9e9403
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 08/20/2020
ms.locfileid: "104315930"
---
# <a name="stoserve-overview"></a><span data-ttu-id="fcc3e-104">Présentation de StoServe</span><span class="sxs-lookup"><span data-stu-id="fcc3e-104">StoServe Overview</span></span>

## <a name="purpose"></a><span data-ttu-id="fcc3e-105">Objectif</span><span class="sxs-lookup"><span data-stu-id="fcc3e-105">Purpose</span></span>

<span data-ttu-id="fcc3e-106">L’objectif principal de cet exemple de code est l’utilisation de services de stockage structurés fournis dans l’implémentation des fichiers composés.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-106">The primary focus of this code example is the use of Structured Storage services as provided in the compound files implementation.</span></span> <span data-ttu-id="fcc3e-107">L’utilisation des interfaces [**IStorage**](/windows/desktop/api/Objidl/nn-objidl-istorage) et [**IStream**](/windows/desktop/api/Objidl/nn-objidl-istream) standard est décrite.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-107">The use of the standard [**IStorage**](/windows/desktop/api/Objidl/nn-objidl-istorage) and [**IStream**](/windows/desktop/api/Objidl/nn-objidl-istream) interfaces is described.</span></span> <span data-ttu-id="fcc3e-108">**StoServe** fonctionne avec l’exemple de code [StoClien](structured-storage-client-sample--stoclien-.md) pour illustrer l’utilisation conjointe du stockage de fichiers composés par le client et le serveur.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-108">**StoServe** works with the [StoClien](structured-storage-client-sample--stoclien-.md) code example to illustrate the joint use of compound file storage by client and server.</span></span>

## <a name="functionality"></a><span data-ttu-id="fcc3e-109">Fonctionnalités</span><span class="sxs-lookup"><span data-stu-id="fcc3e-109">Functionality</span></span>

<span data-ttu-id="fcc3e-110">L’exemple **StoServe** présente l’objet com de l’en-papiers, qui représente virtuellement une feuille vierge de papier.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-110">The **StoServe** sample introduces the COPaper COM object, which virtually represents a blank sheet of drawing paper.</span></span>

<span data-ttu-id="fcc3e-111">Les objets de copapier exposent un ensemble de caractéristiques pour le dessin libre sur l’aire de papier en utilisant l’entrée manuscrite de la couleur et de la largeur spécifiées.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-111">COPaper objects expose a set of features for free-form drawing on the paper surface using "ink" of specified color and width.</span></span> <span data-ttu-id="fcc3e-112">La fonctionnalité est semblable aux exemples de didacticiels « Scribble » dans de nombreuses versions de Microsoft Visual C++.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-112">The functionality is outwardly similar to the "scribble" tutorial samples in many versions of Microsoft Visual C++.</span></span> <span data-ttu-id="fcc3e-113">La différence dans les exemples **StoServe** / **StoClien** est basée sur l’architecture, principalement sur la technologie com.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-113">The difference in the **StoServe**/**StoClien** samples is an architecture-based primarily on COM technology.</span></span> <span data-ttu-id="fcc3e-114">Les fonctionnalités de papier de dessin électronique des objets de codocumentations sont disponibles pour les clients via une interface [**IPaper**](ipaper-methods.md) personnalisée.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-114">The electronic drawing paper features of COPaper objects are available to clients through a custom [**IPaper**](ipaper-methods.md) interface.</span></span> <span data-ttu-id="fcc3e-115">Le « codocument » implémente l’interface **IPaper** .</span><span class="sxs-lookup"><span data-stu-id="fcc3e-115">COPaper implements the **IPaper** interface.</span></span> <span data-ttu-id="fcc3e-116">Une distinction architecturale claire est conservée entre le client et le serveur.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-116">A clear architectural distinction is kept between client and server.</span></span> <span data-ttu-id="fcc3e-117">Aucune interface graphique utilisateur n’est fournie par le colivre.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-117">No graphical user interface (GUI) is provided by COPaper.</span></span> <span data-ttu-id="fcc3e-118">La conception de l’objet codocument s’appuie sur le client pour tous les comportements de l’interface graphique.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-118">The design of the COPaper object relies on the client for all GUI behavior.</span></span> <span data-ttu-id="fcc3e-119">Le coarticle encapsule uniquement la capture et le stockage des données d’encre dessinées sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-119">COPaper encapsulates only the server-based capture and storage of the drawn ink data.</span></span>

<span data-ttu-id="fcc3e-120">Les données d’encre qui sont dessinées sur la surface du copapier peuvent être stockées dans et chargées à partir de fichiers composés.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-120">The ink data that is drawn on the COPaper surface can be stored in and loaded from compound files.</span></span> <span data-ttu-id="fcc3e-121">Les méthodes [**IPaper**](ipaper-methods.md), [**Save**](ipaper--save.md) et [**Load**](ipaper--load.md) acceptent un pointeur d’interface [**IStorage**](/windows/desktop/api/Objidl/nn-objidl-istorage) .</span><span class="sxs-lookup"><span data-stu-id="fcc3e-121">The [**IPaper**](ipaper-methods.md), [**Save**](ipaper--save.md) and [**Load**](ipaper--load.md) methods accept an [**IStorage**](/windows/desktop/api/Objidl/nn-objidl-istorage) interface pointer.</span></span> <span data-ttu-id="fcc3e-122">Le coarticle utilise cette interface **IStorage** fournie par le client pour stocker les données de dessin.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-122">COPaper uses this client-provided **IStorage** interface to store the drawing data.</span></span>

<span data-ttu-id="fcc3e-123">Le coarticle est hébergé dans un serveur in-process et devient public en tant que composant COM personnalisé.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-123">COPaper is housed in an in-process server and is made publicly available as a custom COM component.</span></span> <span data-ttu-id="fcc3e-124">Comme pour les autres serveurs de cette série de didacticiels, StoServe est un serveur COM à inscription automatique.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-124">Similar to other servers in this tutorial series, StoServe is a self-registering COM server.</span></span> <span data-ttu-id="fcc3e-125">Cela rend le type d’objet de l’élément de rapport disponible pour les clients en tant que composant DllPaper à l’aide \_ d’une inscription DLLPAPER CLSID dans le registre.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-125">It makes the COPaper object type available to clients as the DllPaper component using a CLSID\_DllPaper registration in the Registry.</span></span>

<span data-ttu-id="fcc3e-126">Comme dans le serveur d’ancienneté précédent, les fonctionnalités de l’objet connectable sont prises en charge dans le codocument.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-126">As in the previous CONSERVE server, connectable object features are supported in COPaper.</span></span> <span data-ttu-id="fcc3e-127">L’interface [**IConnectionPointContainer**](/windows/win32/api/ocidl/nn-ocidl-iconnectionpointcontainer) est exposée et un point de connexion approprié est implémenté.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-127">The [**IConnectionPointContainer**](/windows/win32/api/ocidl/nn-ocidl-iconnectionpointcontainer) interface is exposed, and an appropriate connection point is implemented.</span></span> <span data-ttu-id="fcc3e-128">Dans ce contexte, une interface IPaperSink personnalisée sortante est déclarée pour être utilisée lors de l’envoi de notifications au client.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-128">In this context, an outgoing custom IPaperSink interface is declared for use in sending notifications to the client.</span></span>

<span data-ttu-id="fcc3e-129">Les deux interfaces personnalisées [**IPaper**](ipaper-methods.md) et [**IPaperSink**](ipapersink-methods.md) sont déclarées dans IPaper. H situé dans le répertoire Common frère \\ Inc.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-129">The two [**IPaper**](ipaper-methods.md) and [**IPaperSink**](ipapersink-methods.md) custom interfaces are declared in IPAPER.H located in the common sibling \\INC directory.</span></span> <span data-ttu-id="fcc3e-130">Les GUID pour les interfaces et les objets sont définis dans PAPGUIDS. H situé dans ce même répertoire include commun.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-130">The GUIDs for the interfaces and objects are defined in PAPGUIDS.H located in that same common include directory.</span></span>

<span data-ttu-id="fcc3e-131">La fonctionnalité CThreaded dans APPUTIL est utilisée par **StoServe** pour assurer la sécurité des threads, comme c’était le cas dans l’exemple FRESERVE.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-131">The CThreaded facility in APPUTIL is used by **StoServe** to achieve thread safety, as it was in the FRESERVE sample.</span></span> <span data-ttu-id="fcc3e-132">Les objets de copapier sont dérivés de la classe CThreaded et héritent de ses méthodes OwnThis et UnOwnThis.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-132">COPaper objects are derived from the CThreaded class and inherit its OwnThis and UnOwnThis methods.</span></span> <span data-ttu-id="fcc3e-133">Ces méthodes n’autorisent qu’un seul thread à la fois à accéder au serveur **StoServe** et aux objets coimprimés gérés par le serveur.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-133">These methods allow only one thread at a time to have access to the **StoServe** server and to COPaper objects managed by the server.</span></span>

## <a name="copaper-com-object"></a><span data-ttu-id="fcc3e-134">Objet COM de l’article</span><span class="sxs-lookup"><span data-stu-id="fcc3e-134">COPaper COM Object</span></span>

<span data-ttu-id="fcc3e-135">L’objet COM de l’en-papiers est le type d’objet unique géré par ce serveur in-process **StoServe** .</span><span class="sxs-lookup"><span data-stu-id="fcc3e-135">The COPaper COM object is the single object type managed by this **StoServe** in-process server.</span></span> <span data-ttu-id="fcc3e-136">Le coarticle est construit comme un objet COM connectable avec une implémentation de l’interface [**IConnectionPointContainer**](/windows/win32/api/ocidl/nn-ocidl-iconnectionpointcontainer) standard et une implémentation de l’interface [**IPaper**](ipaper-methods.md) personnalisée.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-136">COPaper is constructed as a connectable COM object with an implementation of the standard [**IConnectionPointContainer**](/windows/win32/api/ocidl/nn-ocidl-iconnectionpointcontainer) interface and an implementation of the custom [**IPaper**](ipaper-methods.md) interface.</span></span> <span data-ttu-id="fcc3e-137">Le présentateur expose l’interface **IPaper** afin que les clients puissent exécuter un petit ensemble d’opérations de papier électronique sur une instance du copapier.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-137">COPaper exposes the **IPaper** interface so clients can perform a small set of electronic paper operations on an instance of COPaper.</span></span> <span data-ttu-id="fcc3e-138">Les opérations essentielles démarrent une séquence de dessin d’encre, dessinent les données d’encre sur l’aire de papier virtuel du copapier et terminent la séquence de dessin de l’encre.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-138">The essential operations are starting an ink drawing sequence, drawing the ink data on COPaper virtual paper surface, and ending the ink drawing sequence.</span></span> <span data-ttu-id="fcc3e-139">Dans ce schéma, le client est supposé être une application GUI pilotée par une souris ou un périphérique tablette.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-139">In this scheme, the client is assumed to be a GUI application driven by a mouse or tablet device.</span></span> <span data-ttu-id="fcc3e-140">Le client est responsable de la conversion des mouvements de la souris en demandes en codocument, ce qui permet d’enregistrer ces demandes sous forme de données manuscrites.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-140">The client is responsible for translating mouse movements into requests to COPaper, which saves these requests as ink data.</span></span>

<span data-ttu-id="fcc3e-141">Il existe deux niveaux de sauvegarde des données manuscrites dans le codocument.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-141">There are two levels of ink data saving in COPaper.</span></span> <span data-ttu-id="fcc3e-142">Le codocument enregistre les données d’encre dans un tableau basé sur la mémoire RAM qui représente le dessin actuel, et le colivre enregistre de manière permanente un dessin entier dans un fichier composé.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-142">COPaper saves the ink data in a RAM-based array that represents the current drawing, and COPaper persistently saves an entire drawing into a compound file.</span></span> <span data-ttu-id="fcc3e-143">Les méthodes de l’interface [**IPaper**](ipaper-methods.md) effectuent les deux.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-143">Methods in the [**IPaper**](ipaper-methods.md) interface perform both.</span></span>

<span data-ttu-id="fcc3e-144">Étant donné que le client ne gère pas les données papier dessinées, mais qu’il est chargé de le rendre en tant qu’image à l’écran, l’implémentation [**IPaper**](ipaper-methods.md) dans le codocument doit exposer une méthode qui permet au client d’obtenir les données de dessin.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-144">Because the client does not manage the drawn paper data, but is responsible for rendering it as an image on the screen, the [**IPaper**](ipaper-methods.md) implementation in COPaper must expose a method that enables the client to obtain the drawing data.</span></span> <span data-ttu-id="fcc3e-145">La technologie d’objets connectable dans le coarticle est utilisée à cette fin.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-145">The connectable object technology in COPaper is used for this purpose.</span></span> <span data-ttu-id="fcc3e-146">Un \_ point de connexion CONNPOINT PAPERSINK est implémenté par le colivre afin qu’un client puisse se connecter au colivre pour recevoir les données d’entrée manuscrite pour le dessin.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-146">A CONNPOINT\_PAPERSINK connection point is implemented by COPaper so a client can connect to COPaper to receive the ink data for drawing.</span></span> <span data-ttu-id="fcc3e-147">Le client appelle d’abord la méthode [IPaper :: Redraw](ipaper--redraw.md) sur l’objet du codocument pour demander toutes les données d’encre du dessin actuel.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-147">The client first calls the [IPaper::Redraw](ipaper--redraw.md) method on the COPaper object to request all the ink data of the current drawing.</span></span> <span data-ttu-id="fcc3e-148">L’implémentation du codocument de redessin utilise ensuite l’implémentation cliente de [**IPaperSink**](ipapersink-methods.md) pour transmettre les données au client.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-148">The COPaper implementation of Redraw then uses the client implementation of [**IPaperSink**](ipapersink-methods.md) to pass the data to the client.</span></span>

<span data-ttu-id="fcc3e-149">À mesure que l’utilisateur dessine de manière interactive dans le client, il peint immédiatement les données à l’écran tout en l’envoyant également au colivre pour l’enregistrer.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-149">As the user interactively draws in the client, it paints the data immediately to the screen while also sending it to COPaper for saving.</span></span> <span data-ttu-id="fcc3e-150">Lorsque le client demande que l’écran soit [redessiné](ipaper--redraw.md) , il appelle la méthode de redessin du colivre.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-150">When the client requires the screen to be repainted, it calls the COPaper [Redraw](ipaper--redraw.md) method.</span></span> <span data-ttu-id="fcc3e-151">Ce type de redessin est courant dans les applications.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-151">Such repainting is common in applications.</span></span> <span data-ttu-id="fcc3e-152">Par exemple, le redessin se produit lorsque la fenêtre cliente est superposée par une autre fenêtre d’application.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-152">For example, repainting occurs when the client window is overlaid by another application window.</span></span> <span data-ttu-id="fcc3e-153">Le client dispose d’un rendu bitmap de l’image dessinée, mais le bitmap est facilement perdu et doit souvent être redessiné.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-153">The client has a bitmap rendering of the drawn image, but the bitmap is easily lost and must often be repainted.</span></span> <span data-ttu-id="fcc3e-154">Le client s’appuie sur le colivre dans le serveur pour les données d’encre requises pour le redessin.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-154">The client relies on COPaper in the server for the ink data required for repainting.</span></span>

<span data-ttu-id="fcc3e-155">Il s’agit d’une division client/serveur courante.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-155">This is a common client/server division of labor.</span></span> <span data-ttu-id="fcc3e-156">Cela s’avère particulièrement utile lorsque plusieurs clients ont besoin de partager les données.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-156">It is especially appropriate when there is a requirement for multiple clients to share the data.</span></span> <span data-ttu-id="fcc3e-157">Le colivre est codé pour activer cette.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-157">COPaper is coded to enable this.</span></span> <span data-ttu-id="fcc3e-158">Elle utilise la fonction APPUTIL CThreaded pour assurer la sécurité des threads.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-158">It uses the APPUTIL CThreaded facility to achieve thread safety.</span></span> <span data-ttu-id="fcc3e-159">Une application qui peut exploiter cette conception est une application de tableau blanc partagé, où plusieurs clients peuvent contribuer à un dessin fréquemment consulté.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-159">An application that might exploit this design is a shared whiteboard application, where multiple clients can contribute to a commonly viewed drawing.</span></span> <span data-ttu-id="fcc3e-160">La prise en charge de COM pour DCOM (Distributed COM) prend en charge ce type d’utilisation d’applications de groupe de travail sur le réseau.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-160">COM support for Distributed COM (DCOM) supports this type of workgroup application usage across the network.</span></span> <span data-ttu-id="fcc3e-161">Pour plus d’informations, consultez l’exemple REMCLIEN précédent.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-161">For more information, see the earlier REMCLIEN sample.</span></span>

<span data-ttu-id="fcc3e-162">Une utilisation plus modeste du schéma « client/serveur » est d’intégrer le comportement des objets implémentés dans des applications différentes sur le même ordinateur.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-162">A more modest use of the COPaper client/server scheme is to integrate behavior for objects implemented in different applications on the same computer.</span></span> <span data-ttu-id="fcc3e-163">Dans ce cas, les clients peuvent être un conteneur ActiveX dans des applications distinctes qui partagent des données gérées par le même objet.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-163">In this case, the clients might be an ActiveX container in separate applications that share data managed by the same object.</span></span> <span data-ttu-id="fcc3e-164">Ces données ne sont peut-être pas les données de dessin manuscrits prises en charge par le composant DllPaper.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-164">This data may not be the ink drawing data that the DllPaper component supports.</span></span> <span data-ttu-id="fcc3e-165">**StoServe** peut être utilisé comme infrastructure de départ pour la programmation de composants qui gèrent d’autres types de données partagées.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-165">**StoServe** can be used as a starting framework for programming components that manage other types of shared data.</span></span>

## <a name="support-information"></a><span data-ttu-id="fcc3e-166">Informations de support</span><span class="sxs-lookup"><span data-stu-id="fcc3e-166">Support Information</span></span>

<span data-ttu-id="fcc3e-167">Le Makefile **StoServe** inscrit le composant COM **StoServe** DllPaper dans le registre.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-167">The **StoServe** makefile registers the **StoServe** DllPaper COM component in the registry.</span></span> <span data-ttu-id="fcc3e-168">Ce composant doit être inscrit avant que **StoServe** soit disponible pour les clients COM externes en tant que serveur pour ce composant.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-168">This component must be registered before **StoServe** is available to outside COM clients as a server for that component.</span></span> <span data-ttu-id="fcc3e-169">Cette inscription automatique s’effectue à l’aide de l’utilitaire de Register.exe intégré à l’exemple REGISTER.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-169">This self-registration is done using the Register.exe utility built into the REGISTER sample.</span></span> <span data-ttu-id="fcc3e-170">Pour générer ou exécuter **StoServe**, commencez par générer l’exemple de code Register.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-170">To build or run **StoServe**, first build the REGISTER code example.</span></span>

<span data-ttu-id="fcc3e-171">Pour plus d’informations sur la configuration de votre système pour générer et tester les exemples de code de cette série de didacticiels COM, consultez [Comment générer des exemples](how-to-build-samples.md).</span><span class="sxs-lookup"><span data-stu-id="fcc3e-171">For more information about setting up your system to build and test the code examples in this COM Tutorial series, see [How to Build Samples](how-to-build-samples.md).</span></span> <span data-ttu-id="fcc3e-172">Le Makefile fourni (MAKEFILE) est compatible avec Microsoft NMAKE.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-172">The supplied makefile (MAKEFILE) is Microsoft NMAKE-compatible.</span></span> <span data-ttu-id="fcc3e-173">Pour créer une version Debug, émettez la commande NMAKE dans la fenêtre d’invite de commandes.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-173">To create a debug build, issue the NMAKE command in the command prompt window.</span></span>

<span data-ttu-id="fcc3e-174">Pour une utilisation pratique dans Microsoft Visual Studio, un fichier projet est fourni pour chaque exemple.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-174">For convenient use in Microsoft Visual Studio, a project file is provided for each sample.</span></span> <span data-ttu-id="fcc3e-175">Pour charger le projet pour l’exemple **StoServe** , vous pouvez exécuter Visual Studio à partir de l’invite de commandes dans le répertoire Samples comme suit :</span><span class="sxs-lookup"><span data-stu-id="fcc3e-175">To load the project for the **StoServe** example, you can run Visual Studio at the command prompt in the samples directory as follows:</span></span>

<span data-ttu-id="fcc3e-176">MSDEV STOSERVE. DSP</span><span class="sxs-lookup"><span data-stu-id="fcc3e-176">MSDEV STOSERVE.DSP</span></span>

<span data-ttu-id="fcc3e-177">Vous pouvez également double-cliquer sur le fichier stoserve. DSP dans l’Explorateur Windows pour charger un exemple de projet dans Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-177">You can also double-click the Stoserve.dsp file in Windows Explorer to load a sample project into Visual Studio.</span></span> <span data-ttu-id="fcc3e-178">Dans Visual Studio, vous pouvez parcourir les classes C++ de l’exemple de source et effectuer généralement les autres opérations de modification-compilation-débogage.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-178">In Visual Studio you can browse the C++ classes of the sample source and generally perform the other edit-compile-debug operations.</span></span>

> [!Note]  
> <span data-ttu-id="fcc3e-179">Dans le cadre du kit de développement logiciel (SDK) de la plateforme, la compilation de ces exemples à partir de Visual Studio requiert le paramètre approprié des chemins d’accès aux répertoires dans Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="fcc3e-179">As part of the Platform Software Development Kit (SDK), the compilation of these samples from within Visual Studio requires the proper setting of directory paths in Visual Studio.</span></span> <span data-ttu-id="fcc3e-180">Pour plus d’informations, consultez [Comment générer des exemples](how-to-build-samples.md).</span><span class="sxs-lookup"><span data-stu-id="fcc3e-180">For more information, see [How to Build Samples](how-to-build-samples.md).</span></span>

 

 

 