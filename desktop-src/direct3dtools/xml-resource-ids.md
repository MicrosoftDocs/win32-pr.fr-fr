---
description: ID de chaîne de ressource définis par l’appelant à retourner dans les données XML pour la visualisation des objets.
MS-HAID: vspixengine.Xml\_Resource\_Ids
MSHAttr:
- PreferredSiteName:MSDN
- PreferredLib:/library/windows/desktop
title: Énumération Xml_Resource_Ids
ms.topic: reference
ms.date: 05/31/2018
ms.assetid: 1F2ECC78-73EC-4A59-8CF7-1BB7FB80244E
api_name:
- Xml_Resource_Ids
api_type:
- HeaderDef
api_location:
- vspixengine.h
topic_type:
- APIRef
- kbSyntax
ms.openlocfilehash: 6cde2fe260924702eaeaebad235ebf1b469c73ed
ms.sourcegitcommit: c276a8912787b2cda74dcf54eb96df961bb1188b
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 08/20/2021
ms.locfileid: "122621905"
---
# <a name="span-idvspixenginexml_resource_idsspanxml_resource_ids-enumeration"></a><span id="vspixengine.xml_resource_ids"></span>\_Énumération des ID de ressource XML \_

ID de chaîne de ressource définis par l’appelant à retourner dans les données XML pour la visualisation des objets

## <a name="syntax"></a>Syntax


```C++
};
```

## <a name="constants"></a>Constantes

<span id="XML_DEVICE_INFORMATION"></span><span id="xml_device_information"></span>**\_informations sur l’appareil XML \_**  
ID de ressource pour la chaîne « informations sur l’appareil » utilisé dans les données XML.

<span id="XML_IA_STATE"></span><span id="xml_ia_state"></span>**État de l' \_ IA XML \_**  
ID de ressource pour la chaîne « assembleur d’entrée » utilisé dans les données XML.

<span id="XML_VS_STATE"></span><span id="xml_vs_state"></span>**\_État et \_ XML**  
ID de ressource pour la chaîne « étape du nuanceur de sommets » utilisé dans les données XML.

<span id="XML_GS_STATE"></span><span id="xml_gs_state"></span>**\_État GS \_ XML**  
ID de ressource pour la chaîne « étape de nuanceur de géométrie » utilisé dans les données XML.

<span id="XML_RASTERIZER_STATE"></span><span id="xml_rasterizer_state"></span>**\_État de RASTÉRISEUR \_ XML**  
ID de ressource pour la chaîne « étape du rastériseur » utilisée dans les données XML.

<span id="XML_PS_STATE"></span><span id="xml_ps_state"></span>**\_État de PS XML \_**  
ID de ressource pour la chaîne « étape de nuanceur de pixels » utilisée dans les données XML.

<span id="XML_OM_STATE"></span><span id="xml_om_state"></span>**\_État du modèle d’objet XML \_**  
ID de ressource pour la chaîne « étape de fusion de sortie » utilisée dans les données XML.

<span id="XML_HS_STATE"></span><span id="xml_hs_state"></span>**État de XML \_ HS \_**  
ID de ressource pour la chaîne « étape du nuanceur de coque » utilisé dans les données XML.

<span id="XML_DS_STATE"></span><span id="xml_ds_state"></span>**\_État du DS XML \_**  
ID de ressource pour la chaîne « étape du nuanceur de domaine » utilisée dans les données XML.

<span id="XML_CS_STATE"></span><span id="xml_cs_state"></span>**\_État cs \_ XML**  
ID de ressource pour la chaîne « étape du nuanceur de calcul » utilisée dans les données XML.

<span id="XML_CLASS_INSTANCE_11"></span><span id="xml_class_instance_11"></span>**\_Instance de classe XML \_ \_ 11**  
ID de ressource pour la chaîne « instances de classe » utilisée dans les données XML.

<span id="XML_DEVICE_10"></span><span id="xml_device_10"></span>**\_Périphérique XML \_ 10**  
ID de ressource pour la chaîne « Device 10 » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUT_11"></span><span id="xml_input_layout_11"></span>**\_Disposition d’entrée XML \_ \_ 11**  
ID de ressource pour la chaîne « disposition d’entrée 11 » utilisée dans les données XML.

<span id="XML_SAMPLER_STATE_11"></span><span id="xml_sampler_state_11"></span>**État de l' \_ échantillonneur XML \_ \_ 11**  
ID de ressource pour la chaîne « état de l’échantillonneur 11 » utilisé dans les données XML.

<span id="XML_RASTER_STATE_11"></span><span id="xml_raster_state_11"></span>**\_État du raster \_ XML \_ 11**  
ID de ressource pour la chaîne « État du raster 11 » utilisé dans les données XML.

<span id="XML_BLEND_STATE_11"></span><span id="xml_blend_state_11"></span>**\_État de fusion XML \_ \_ 11**  
ID de ressource pour la chaîne « état de fusion 11 » utilisé dans les données XML.

<span id="XML_DEPTH_STENCIL_STATE_11"></span><span id="xml_depth_stencil_state_11"></span>**\_État du stencil de profondeur XML \_ \_ \_ 11**  
ID de ressource pour la chaîne « État du stencil de profondeur 11 » utilisé dans les données XML.

<span id="XML_UNORDERED_ACCESS_VIEW_11"></span><span id="xml_unordered_access_view_11"></span>**\_Vue d’accès non TRIÉ XML \_ \_ \_ 11**  
ID de ressource pour la chaîne « mode d’accès non ordonné 11 » utilisé dans les données XML.

<span id="XML_SHADER_RESOURCE_VIEW_11"></span><span id="xml_shader_resource_view_11"></span>**\_Vue de ressource de nuanceur XML \_ \_ \_ 11**  
ID de ressource pour la chaîne « vue de ressource de nuanceur 11 » utilisé dans les données XML.

<span id="XML_DEPTH_STENCIL_VIEW_11"></span><span id="xml_depth_stencil_view_11"></span>**\_Vue du stencil de profondeur XML \_ \_ \_ 11**  
ID de ressource pour la chaîne « vue du stencil de profondeur 11 » utilisée dans les données XML.

<span id="XML_RENDER_TARGET_VIEW_11"></span><span id="xml_render_target_view_11"></span>**Vue de la \_ cible de rendu XML \_ \_ \_ 11**  
ID de ressource pour la chaîne « affichage de la cible du rendu 11 » utilisé dans les données XML.

<span id="XML_DEVICE_11"></span><span id="xml_device_11"></span>**\_Périphérique XML \_ 11**  
ID de ressource pour la chaîne « Device 11 » utilisé dans les données XML.

<span id="XML_SWAP_CHAIN"></span><span id="xml_swap_chain"></span>**\_chaîne d’échange XML \_**  
ID de ressource pour la chaîne « swap Chain » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUT_10"></span><span id="xml_input_layout_10"></span>**\_Disposition d’entrée XML \_ \_ 10**  
ID de ressource pour la chaîne « disposition d’entrée 10 » utilisée dans les données XML.

<span id="XML_SAMPLER_STATE_10"></span><span id="xml_sampler_state_10"></span>**État de l' \_ échantillonneur XML \_ \_ 10**  
ID de ressource pour la chaîne « état de l’échantillonneur 10 » utilisé dans les données XML.

<span id="XML_RASTER_STATE_10"></span><span id="xml_raster_state_10"></span>**\_État du raster \_ XML \_ 10**  
ID de ressource pour la chaîne « État raster 10 » utilisé dans les données XML.

<span id="XML_BLEND_STATE_10"></span><span id="xml_blend_state_10"></span>**\_État de fusion XML \_ \_ 10**  
ID de ressource pour la chaîne « état de fusion 10 » utilisé dans les données XML.

<span id="XML_DEPTH_STENCIL_STATE_10"></span><span id="xml_depth_stencil_state_10"></span>**\_État du stencil de profondeur XML \_ \_ \_ 10**  
ID de ressource pour la chaîne « État du stencil de profondeur 10 » utilisé dans les données XML.

<span id="XML_SHADER_RESOURCE_VIEW_10"></span><span id="xml_shader_resource_view_10"></span>**Vue de la \_ ressource de nuanceur XML \_ \_ \_ 10**  
ID de ressource pour la chaîne « vue de ressource de nuanceur 10 » utilisée dans les données XML.

<span id="XML_DEPTH_STENCIL_VIEW_10"></span><span id="xml_depth_stencil_view_10"></span>**\_Vue du stencil de profondeur XML \_ \_ \_ 10**  
ID de ressource pour la chaîne « vue du stencil de profondeur 10 » utilisée dans les données XML.

<span id="XML_RENDER_TARGET_VIEW_10"></span><span id="xml_render_target_view_10"></span>**Vue de la \_ cible de rendu XML \_ \_ \_ 10**  
ID de ressource pour la chaîne « affichage de la cible de rendu 10 » utilisé dans les données XML.

<span id="XML_VERTEX_DECLARATION"></span><span id="xml_vertex_declaration"></span>**\_déclaration de vertex XML \_**  
ID de ressource pour la chaîne « déclaration de vertex » utilisée dans les données XML.

<span id="XML_STATE_BLOCK_STATE_10"></span><span id="xml_state_block_state_10"></span>**\_État du bloc d’État XML \_ \_ \_ 10**  
ID de ressource pour la chaîne « État du bloc d’État 10 » utilisé dans les données XML.

<span id="XML_DEVICE_CONTEXT_STATE_11"></span><span id="xml_device_context_state_11"></span>**\_État du contexte de périphérique XML \_ \_ \_ 11**  
ID de ressource pour la chaîne « État du contexte de périphérique 11 » utilisé dans les données XML.

<span id="XML_DEVICE_STATE_10"></span><span id="xml_device_state_10"></span>**État de l' \_ appareil XML \_ \_ 10**  
ID de ressource pour la chaîne « état de l’appareil 10 » utilisé dans les données XML.

<span id="XML_BACK_FACE"></span><span id="xml_back_face"></span>**\_face arrière \_ XML**  
ID de ressource pour la chaîne « back face » utilisé dans les données XML.

<span id="XML_FRONT_FACE"></span><span id="xml_front_face"></span>**\_face avant \_ XML**  
ID de ressource pour la chaîne « front face » utilisé dans les données XML.

<span id="XML_DEVICE_CREATION_INFORMATION"></span><span id="xml_device_creation_information"></span>**\_ \_ informations sur la création de périphérique XML \_**  
ID de ressource pour la chaîne « informations sur la création du périphérique » utilisé dans les données XML.

<span id="XML_OTHER_INFORMATION"></span><span id="xml_other_information"></span>**\_autres \_ informations XML**  
ID de ressource pour la chaîne « autres informations » utilisé dans les données XML.

<span id="XML_MULTISAMPLING"></span><span id="xml_multisampling"></span>**\_échantillonnage multiple XML**  
ID de ressource pour la chaîne « échantillonnage multiple » utilisé dans les données XML.

<span id="XML_BACK_BUFFER"></span><span id="xml_back_buffer"></span>**\_mémoire tampon d’arrière-plan XML \_**  
ID de ressource pour la chaîne « mémoire tampon d’arrière-plan » utilisé dans les données XML.

<span id="XML_GENERAL_INFORMATION"></span><span id="xml_general_information"></span>**\_informations générales \_ sur XML**  
ID de ressource pour la chaîne « informations générales » utilisé dans les données XML.

<span id="XML_RENDER_TARGETS"></span><span id="xml_render_targets"></span>**\_cibles de rendu XML \_**  
ID de ressource pour la chaîne « cibles de rendu » utilisée dans les données XML.

<span id="XML_BLEND_DESCRIPTION"></span><span id="xml_blend_description"></span>**Description de la \_ fusion XML \_**  
ID de ressource pour la chaîne « Description Blend » utilisée dans les données XML.

<span id="XML_DEPTH_STENCIL"></span><span id="xml_depth_stencil"></span>**\_stencil de profondeur XML \_**  
ID de ressource pour le « stencil de profondeur » de chaîne utilisé dans les données XML.

<span id="XML_FRONT_FACE_STENCIL"></span><span id="xml_front_face_stencil"></span>**\_stencil de \_ face \_ avant XML**  
ID de ressource de la chaîne « front face stencil » utilisé dans les données XML.

<span id="XML_BACK_FACE_STENCIL"></span><span id="xml_back_face_stencil"></span>**\_gabarit de \_ face de retour XML \_**  
ID de ressource pour la chaîne « back face stencil » utilisé dans les données XML.

<span id="XML_DEVICE1_CREATION_INFORMATION"></span><span id="xml_device1_creation_information"></span>**\_ \_ Informations sur la création d’un appareil1 XML \_**  
ID de ressource pour la chaîne « informations de création de l’appareil1 » utilisé dans les données XML.

<span id="XML_VIEWPORTS"></span><span id="xml_viewports"></span>**\_VIEWPORTS XML**  
ID de ressource pour la chaîne « Viewports » utilisé dans les données XML.

<span id="XML_SCISSOR_RECTS"></span><span id="xml_scissor_rects"></span>**\_rectangles de ciseaux XML \_**  
ID de ressource pour la chaîne « ciseaux RECTS » utilisé dans les données XML.

<span id="XML_RASTERIZER"></span><span id="xml_rasterizer"></span>**\_RASTÉRISEUR XML**  
ID de ressource pour la chaîne « rastériseur » utilisé dans les données XML.

<span id="XML_SHADER_GENERAL_INFORMATION"></span><span id="xml_shader_general_information"></span>**\_ \_ informations générales sur le NUANCEur XML \_**  
ID de ressource pour la chaîne « informations générales sur le nuanceur » utilisé dans les données XML.

<span id="XML_SHADER_RESOURCES"></span><span id="xml_shader_resources"></span>**\_ressources de nuanceur XML \_**  
ID de ressource pour la chaîne « ressources de nuanceur » utilisée dans les données XML.

<span id="XML_SAMPLER_STATES"></span><span id="xml_sampler_states"></span>**États de l' \_ échantillonneur XML \_**  
ID de ressource pour la chaîne « États de l’échantillonneur » utilisé dans les données XML.

<span id="XML_CONSTANT_BUFFERS"></span><span id="xml_constant_buffers"></span>**\_ \_ mémoires tampons de constante XML**  
ID de ressource pour la chaîne « mémoires tampons constantes » utilisé dans les données XML.

<span id="XML_STREAM_OUTPUT_BUFFERS"></span><span id="xml_stream_output_buffers"></span>**\_ \_ mémoires tampons de sortie de flux XML \_**  
ID de ressource pour la chaîne « tampons de sortie de flux » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUT_GENERAL_INFORMATION"></span><span id="xml_input_layout_general_information"></span>**\_ \_ \_ informations générales sur la disposition d’entrée XML \_**  
ID de ressource pour la chaîne « informations générales sur la disposition d’entrée » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUTS"></span><span id="xml_input_layouts"></span>**\_dispositions d’entrée XML \_**  
ID de ressource pour la chaîne « dispositions d’entrée » utilisée dans les données XML.

<span id="XML_VERTEX_BUFFERS"></span><span id="xml_vertex_buffers"></span>**\_ \_ mémoires tampons de vertex XML**  
ID de ressource pour la chaîne « vertex buffers » utilisé dans les données XML.

<span id="XML_INDEX_BUFFERS"></span><span id="xml_index_buffers"></span>**\_tampons d’index XML \_**  
ID de ressource pour la chaîne « index buffers » utilisé dans les données XML.

<span id="XML_PRIMITIVE_TOP_GENERAL"></span><span id="xml_primitive_top_general"></span>**XML \_ primitif \_ haut \_ général**  
ID de ressource pour la chaîne « primitive Top General » utilisé dans les données XML.

<span id="XML_BLEND_STATE"></span><span id="xml_blend_state"></span>**\_État de fusion XML \_**  
ID de ressource pour la chaîne « état de fusion » utilisé dans les données XML.

<span id="XML_UNORDERED_ACCESS_VIEWS"></span><span id="xml_unordered_access_views"></span>**\_vues d’accès non ordonnées XML \_ \_**  
ID de ressource pour la chaîne « vues d’accès non ordonnées » utilisée dans les données XML.

<span id="XML_CLASS_INSTANCES"></span><span id="xml_class_instances"></span>**\_instances de classe XML \_**  
ID de ressource pour la chaîne « instances de classe » utilisée dans les données XML.

<span id="XML_INSTANCE_INDEX"></span><span id="xml_instance_index"></span>**\_index d’instance XML \_**  
ID de ressource pour la chaîne « index d’instance » utilisé dans les données XML.

<span id="XML_INSTANCE_NAME"></span><span id="xml_instance_name"></span>**nom de l' \_ instance XML \_**  
ID de ressource pour la chaîne « nom d’instance » utilisé dans les données XML.

<span id="XML_BASE_SAMPLER"></span><span id="xml_base_sampler"></span>**\_échantillonneur de base XML \_**  
ID de ressource pour la chaîne « échantillonneur de base » utilisé dans les données XML.

<span id="XML_BASE_TEXTURE"></span><span id="xml_base_texture"></span>**\_texture de base XML \_**  
ID de ressource pour la chaîne « texture de base » utilisée dans les données XML.

<span id="XML_BASE_CONSTANT_BUFFER_OFFSET"></span><span id="xml_base_constant_buffer_offset"></span>**\_décalage de \_ la \_ mémoire tampon constante de base XML \_**  
ID de ressource pour la chaîne « base constant de la mémoire tampon » utilisé dans les données XML.

<span id="XML_CONSTANT_BUFFER"></span><span id="xml_constant_buffer"></span>**\_mémoire tampon de constante XML \_**  
ID de ressource pour la chaîne « constante buffer » utilisée dans les données XML.

<span id="XML_TYPE_ID"></span><span id="xml_type_id"></span>**\_ID de type XML \_**  
ID de ressource pour la chaîne « ID de type » utilisée dans les données XML.

<span id="XML_INSTANCE_ID"></span><span id="xml_instance_id"></span>**\_ID d’instance XML \_**  
ID de ressource pour la chaîne « ID d’instance » utilisé dans les données XML.

<span id="XML_TYPE_NAME"></span><span id="xml_type_name"></span>**\_nom du type XML \_**  
ID de ressource pour la chaîne « nom de type » utilisée dans les données XML.

<span id="XML_CREATED_BY"></span><span id="xml_created_by"></span>**XML \_ créé \_ par**  
ID de ressource pour la chaîne « créé par » utilisé dans les données XML.

<span id="XML_CLASS_LINKAGE"></span><span id="xml_class_linkage"></span>**\_liaison de classe XML \_**  
ID de ressource pour la chaîne « liaison de classe » utilisée dans les données XML.

<span id="XML_DESCRIPTION"></span><span id="xml_description"></span>**\_description XML**  
ID de ressource pour la chaîne « Description » utilisée dans les données XML.

<span id="XML_SEMANTIC_NAME"></span><span id="xml_semantic_name"></span>**nom de la \_ sémantique XML \_**  
ID de ressource pour la chaîne « nom sémantique » utilisé dans les données XML.

<span id="XML_SEMANTIC_INDEX"></span><span id="xml_semantic_index"></span>**\_index sémantique \_ XML**  
ID de ressource pour la chaîne « index sémantique » utilisé dans les données XML.

<span id="XML_FORMAT"></span><span id="xml_format"></span>**\_format XML**  
ID de ressource pour la chaîne « format » utilisé dans les données XML.

<span id="XML_INPUT_SLOT"></span><span id="xml_input_slot"></span>**\_emplacement d’entrée XML \_**  
ID de ressource pour la chaîne « emplacement d’entrée » utilisé dans les données XML.

<span id="XML_ALIGNED_BYTE_OFFSET"></span><span id="xml_aligned_byte_offset"></span>**\_décalage d' \_ octet \_ aligné XML**  
ID de ressource pour la chaîne « décalage d’octets alignés » utilisée dans les données XML.

<span id="XML_INPUT_SLOT_CLASS"></span><span id="xml_input_slot_class"></span>**\_classe d' \_ emplacement d’entrée XML \_**  
ID de ressource pour la chaîne « classe d’emplacement d’entrée » utilisée dans les données XML.

<span id="XML_INSTANCE_DATA_STEP_RATE"></span><span id="xml_instance_data_step_rate"></span>**\_taux d' \_ étapes des données d’instance XML \_ \_**  
ID de ressource pour la chaîne « taux d’étapes des données d’instance » utilisé dans les données XML.

<span id="XML_ADDRESS_U"></span><span id="xml_address_u"></span>**adresse XML- \_ \_ U**  
ID de ressource pour la chaîne « adresse u » utilisée dans les données XML.

<span id="XML_ADDRESS_V"></span><span id="xml_address_v"></span>**\_adresse XML \_ V**  
ID de ressource pour la chaîne « Address v » utilisé dans les données XML.

<span id="XML_ADDRESS_W"></span><span id="xml_address_w"></span>**\_adresse XML \_ W**  
ID de ressource pour la chaîne « Address w » utilisé dans les données XML.

<span id="XML_MIN_LOD"></span><span id="xml_min_lod"></span>**XML \_ Min \_ LOD**  
ID de ressource pour la chaîne « min LOD » utilisé dans les données XML.

<span id="XML_MAX_LOD"></span><span id="xml_max_lod"></span>**XML \_ Max \_ LOD**  
ID de ressource pour la chaîne « Max LOD » utilisé dans les données XML.

<span id="XML_FILTER"></span><span id="xml_filter"></span>**\_filtre XML**  
ID de ressource pour la chaîne « filtre » utilisé dans les données XML.

<span id="XML_MIP_SLICE"></span><span id="xml_mip_slice"></span>**\_segment MIP \_ XML**  
ID de ressource pour la chaîne « segment MIP » utilisée dans les données XML.

<span id="XML_MOST_DETAILED_MIP"></span><span id="xml_most_detailed_mip"></span>**\_MIP le plus \_ détaillé \_ XML**  
ID de ressource pour la chaîne « le MIP le plus détaillé » utilisé dans les données XML.

<span id="XML_MIP_LEVELS"></span><span id="xml_mip_levels"></span>**\_niveaux MIP \_ XML**  
ID de ressource pour la chaîne « niveaux MIP » utilisé dans les données XML.

<span id="XML_FIRST_ARRAY_SLICE"></span><span id="xml_first_array_slice"></span>**tranche XML du \_ premier \_ tableau \_**  
ID de ressource pour la chaîne « premier tableau Slice » utilisé dans les données XML.

<span id="XML_ARRAY_SIZE"></span><span id="xml_array_size"></span>**\_taille du tableau XML \_**  
ID de ressource pour la chaîne « taille du tableau » utilisée dans les données XML.

<span id="XML_RESOURCE"></span><span id="xml_resource"></span>**\_ressource XML**  
ID de ressource pour la chaîne « ressource » utilisée dans les données XML.

<span id="XML_RESOURCE_TYPE"></span><span id="xml_resource_type"></span>**\_type de ressource XML \_**  
ID de ressource pour la chaîne « type de ressource » utilisé dans les données XML.

<span id="XML_ELEMENT_OFFSET"></span><span id="xml_element_offset"></span>**décalage de l' \_ élément XML \_**  
ID de ressource pour la chaîne « décalage d’élément » utilisé dans les données XML.

<span id="XML_ELEMENT_WIDTH"></span><span id="xml_element_width"></span>**largeur de l' \_ élément XML \_**  
ID de ressource pour la chaîne « largeur d’élément » utilisée dans les données XML.

<span id="XML_FIRST_WITH_SLICE"></span><span id="xml_first_with_slice"></span>**\_tout d’abord XML \_ avec \_ Slice**  
ID de ressource pour la chaîne « First with Slice » utilisé dans les données XML.

<span id="XML_BORDER_COLOR"></span><span id="xml_border_color"></span>**couleur de la \_ bordure XML \_**  
ID de ressource pour la chaîne « couleur de bordure » utilisée dans les données XML.

<span id="XML_SLOT"></span><span id="xml_slot"></span>**\_emplacement XML**  
ID de ressource pour la chaîne « emplacement » utilisé dans les données XML.

<span id="XML_COMPARISON_FUNCTION"></span><span id="xml_comparison_function"></span>**\_fonction de comparaison XML \_**  
ID de ressource pour la chaîne « fonction de comparaison » utilisée dans les données XML.

<span id="XML_MAX_ANISOTROPY"></span><span id="xml_max_anisotropy"></span>**\_anisotrope max. XML \_**  
ID de ressource pour la chaîne « maximum anisotrope » utilisé dans les données XML.

<span id="XML_MIP_LOD_BIAS"></span><span id="xml_mip_lod_bias"></span>**\_biais XML MIP \_ LOD \_**  
ID de ressource pour la chaîne « décalage MIP LOD » utilisé dans les données XML.

<span id="XML_FILL_MODE"></span><span id="xml_fill_mode"></span>**\_mode de remplissage XML \_**  
ID de ressource pour la chaîne « mode de remplissage » utilisé dans les données XML.

<span id="XML_CULL_MODE"></span><span id="xml_cull_mode"></span>**\_mode d’élimination XML \_**  
ID de ressource pour la chaîne « mode d’élimination » utilisé dans les données XML.

<span id="XML_FRONT_COUNTER_CLOCKWISE"></span><span id="xml_front_counter_clockwise"></span>**\_compteur XML avant dans le sens des aiguilles d’une \_ \_ montre**  
ID de ressource pour la chaîne « compteur avant dans le sens inverse des aiguilles d’une montre » utilisé dans les données XML.

<span id="XML_DEPTH_BIAS"></span><span id="xml_depth_bias"></span>**\_décalage de profondeur XML \_**  
ID de ressource pour la chaîne « décalage de profondeur » utilisé dans les données XML.

<span id="XML_DEPTH_BIAS_CLAMP"></span><span id="xml_depth_bias_clamp"></span>**\_collier de \_ décalage de profondeur XML \_**  
ID de ressource pour la chaîne « collier de décalage de profondeur » utilisé dans les données XML.

<span id="XML_SLOPE_SCALE_DEPTH_BIAS"></span><span id="xml_slope_scale_depth_bias"></span>**décalage de profondeur de l' \_ échelle de pente XML \_ \_ \_**  
ID de ressource pour la chaîne « décalage de profondeur d’échelle de pente » utilisé dans les données XML.

<span id="XML_DEPTH_CLIP_ENABLE"></span><span id="xml_depth_clip_enable"></span>**\_activation du \_ clip de profondeur XML \_**  
ID de ressource pour la chaîne « activer le clip de profondeur » utilisé dans les données XML.

<span id="XML_SCISSOR_ENABLE"></span><span id="xml_scissor_enable"></span>**activation de la \_ ciseaux XML \_**  
ID de ressource pour la chaîne « ciseaux Enable » utilisé dans les données XML.

<span id="XML_MULTISAMPLE_ENABLE"></span><span id="xml_multisample_enable"></span>**multiéchantillonne XML, \_ \_ activer**  
ID de ressource pour la chaîne « multiéchantillonne Enable » utilisé dans les données XML.

<span id="XML_ANTIALIASED_LINE_ENABLE"></span><span id="xml_antialiased_line_enable"></span>**activation de la \_ ligne avec anticrénelage XML \_ \_**  
ID de ressource pour la chaîne « activation de ligne avec anticrénelage » utilisée dans les données XML.

<span id="XML_FUNCTION"></span><span id="xml_function"></span>**\_fonction XML**  
ID de ressource pour la chaîne « Function » utilisée dans les données XML.

<span id="XML_PASS_OP"></span><span id="xml_pass_op"></span>**\_passive \_ XML**  
ID de ressource pour la chaîne « Pass op » utilisé dans les données XML.

<span id="XML_FAIL_OP"></span><span id="xml_fail_op"></span>**échec de l' \_ \_ opération XML**  
ID de ressource pour la chaîne « Fail op » utilisé dans les données XML.

<span id="XML_DEPTH_FAIL_OP"></span><span id="xml_depth_fail_op"></span>**échec de la \_ profondeur XML \_ \_**  
ID de ressource pour la chaîne « échec de l’opération de profondeur » utilisé dans les données XML.

<span id="XML_DEPTH_ENABLE"></span><span id="xml_depth_enable"></span>**activation de la \_ profondeur XML \_**  
ID de ressource pour la chaîne « profondeur Enable » utilisée dans les données XML.

<span id="XML_DEPTH_FUNCTION"></span><span id="xml_depth_function"></span>**\_fonction de profondeur XML \_**  
ID de ressource pour la fonction de profondeur de chaîne utilisée dans les données XML.

<span id="XML_DEPTH_WRITE_MASK"></span><span id="xml_depth_write_mask"></span>**\_masque d' \_ écriture de profondeur XML \_**  
ID de ressource pour le « masque d’écriture de profondeur » utilisé dans les données XML.

<span id="XML_STENCIL_ENABLE"></span><span id="xml_stencil_enable"></span>**\_activation du stencil XML \_**  
ID de ressource pour la chaîne « activation du stencil » utilisée dans les données XML.

<span id="XML_STENCIL_READ_MASK"></span><span id="xml_stencil_read_mask"></span>**\_masque de \_ lecture du stencil XML \_**  
ID de ressource pour la chaîne « masque de lecture de stencil » utilisé dans les données XML.

<span id="XML_STENCIL_WRITE_MASK"></span><span id="xml_stencil_write_mask"></span>**\_masque d' \_ écriture de stencil XML \_**  
ID de ressource pour la chaîne « masque d’écriture de stencil » utilisé dans les données XML.

<span id="XML_FIRST_ELEMENT"></span><span id="xml_first_element"></span>**\_élément XML First \_**  
ID de ressource pour la chaîne « premier élément » utilisé dans les données XML.

<span id="XML_NUM_ELEMENTS"></span><span id="xml_num_elements"></span>**\_éléments de nombre XML \_**  
ID de ressource pour la chaîne « nombre d’éléments » utilisé dans les données XML.

<span id="XML_FLAGS"></span><span id="xml_flags"></span>**\_indicateurs XML**  
ID de ressource pour la chaîne « Flags » utilisé dans les données XML.

<span id="XML_W_SIZE"></span><span id="xml_w_size"></span>**\_taille XML W \_**  
ID de ressource pour la chaîne « taille w » utilisée dans les données XML.

<span id="XML_FIRST_2D_ARRAY_FACE"></span><span id="xml_first_2d_array_face"></span>**\_Visage XML \_ premier \_ tableau \_ 2D**  
ID de ressource pour la chaîne « First 2D Array face » utilisé dans les données XML.

<span id="XML_NUM_CUBES"></span><span id="xml_num_cubes"></span>**\_nombre de \_ CUBEs XML**  
ID de ressource pour la chaîne « num cubes » utilisé dans les données XML.

<span id="XML_FACTORY"></span><span id="xml_factory"></span>**\_fabrique XML**  
ID de ressource pour la chaîne « Factory » utilisé dans les données XML.

<span id="XML_ADAPTER"></span><span id="xml_adapter"></span>**\_adaptateur XML**  
ID de ressource pour la chaîne « adaptateur » utilisé dans les données XML.

<span id="XML_HEIGHT"></span><span id="xml_height"></span>**\_hauteur XML**  
ID de ressource pour la chaîne « Height » utilisée dans les données XML.

<span id="XML_WIDTH"></span><span id="xml_width"></span>**\_largeur XML**  
ID de ressource pour la chaîne « Width » utilisée dans les données XML.

<span id="XML_CREATION_FLAGS"></span><span id="xml_creation_flags"></span>**\_indicateurs de création XML \_**  
ID de ressource pour la chaîne « indicateurs de création » utilisé dans les données XML.

<span id="XML_SDK_VERSION"></span><span id="xml_sdk_version"></span>**\_version du SDK XML \_**  
ID de ressource pour la chaîne « version du kit de développement logiciel » utilisée dans les données XML.

<span id="XML_DRIVER_TYPE"></span><span id="xml_driver_type"></span>**\_type de pilote XML \_**  
ID de ressource pour la chaîne « type de pilote » utilisé dans les données XML.

<span id="XML_FEATURE_LEVEL"></span><span id="xml_feature_level"></span>**\_niveau de fonctionnalité XML \_**  
ID de ressource pour la chaîne « niveau de fonctionnalité » utilisé dans les données XML.

<span id="XML_IMMEDIATE_CONTEXT"></span><span id="xml_immediate_context"></span>**\_contexte immédiat \_ XML**  
ID de ressource pour la chaîne « contexte immédiat » utilisé dans les données XML.

<span id="XML_EXCEPTION_MODE"></span><span id="xml_exception_mode"></span>**\_mode d’exception XML \_**  
ID de ressource pour la chaîne « mode d’exception » utilisé dans les données XML.

<span id="XML_REFRESH_RATE"></span><span id="xml_refresh_rate"></span>**\_taux d’actualisation XML \_**  
ID de ressource pour la chaîne « taux de rafraîchissement » utilisé dans les données XML.

<span id="XML_SCANLINE_ORDERING"></span><span id="xml_scanline_ordering"></span>**\_classement numérisation \_ XML**  
ID de ressource pour la chaîne « numérisation Ordering » utilisé dans les données XML.

<span id="XML_SCALING"></span><span id="xml_scaling"></span>**\_mise à l’échelle XML**  
ID de ressource pour la « mise à l’échelle » de la chaîne utilisée dans les données XML.

<span id="XML_USAGE"></span><span id="xml_usage"></span>**utilisation de XML \_**  
ID de ressource pour la chaîne « usage » utilisé dans les données XML.

<span id="XML_BUFFER_COUNT"></span><span id="xml_buffer_count"></span>**\_nombre de mémoires tampons XML \_**  
ID de ressource pour la chaîne « nombre de mémoires tampons » utilisé dans les données XML.

<span id="XML_WINDOWED"></span><span id="xml_windowed"></span>**XML avec \_ fenêtre**  
ID de ressource pour la chaîne « Windowed » utilisé dans les données XML.

<span id="XML_SWAP_EFFECT"></span><span id="xml_swap_effect"></span>**\_effet d’échange XML \_**  
ID de ressource pour la chaîne « effet swap » utilisé dans les données XML.

<span id="XML_STREAM"></span><span id="xml_stream"></span>**\_flux XML**  
ID de ressource pour la chaîne « Stream » utilisé dans les données XML.

<span id="XML_OFFSET"></span><span id="xml_offset"></span>**\_décalage XML**  
ID de ressource pour la chaîne « offset » utilisé dans les données XML.

<span id="XML_TYPE"></span><span id="xml_type"></span>**\_type XML**  
ID de ressource pour la chaîne « type » utilisé dans les données XML.

<span id="XML_METHOD"></span><span id="xml_method"></span>**\_méthode XML**  
ID de ressource pour la chaîne « méthode » utilisée dans les données XML.

<span id="XML_USAGE_INDEX"></span><span id="xml_usage_index"></span>**\_index d’utilisation XML \_**  
ID de ressource pour la chaîne « index d’utilisation » utilisé dans les données XML.

<span id="XML_BLEND_SOURCE"></span><span id="xml_blend_source"></span>**\_source de fusion XML \_**  
ID de ressource pour la chaîne « Blend source » utilisé dans les données XML.

<span id="XML_BLEND_DESTINATION"></span><span id="xml_blend_destination"></span>**\_destination de fusion XML \_**  
ID de ressource pour la chaîne « Blend destination » utilisé dans les données XML.

<span id="XML_BLEND_OP"></span><span id="xml_blend_op"></span>**XML \_ Blend \_ op**  
ID de ressource pour la chaîne « Blend op » utilisé dans les données XML.

<span id="XML_ALPHA_BLEND_SOURCE"></span><span id="xml_alpha_blend_source"></span>**\_source de \_ lissage \_ alpha XML**  
ID de ressource pour la chaîne « alpha Blend source » utilisé dans les données XML.

<span id="XML_ALPHA_BLEND_DEST"></span><span id="xml_alpha_blend_dest"></span>**\_destination de \_ fusion \_ alpha XML**  
ID de ressource pour la chaîne « alpha Blend dest » utilisé dans les données XML.

<span id="XML_ALPHA_BLEND_OP"></span><span id="xml_alpha_blend_op"></span>**XML \_ alpha \_ Blend \_ op**  
ID de ressource pour la chaîne « alpha Blend op » utilisé dans les données XML.

<span id="XML_RENDER_TARGET_WRITE_MASK"></span><span id="xml_render_target_write_mask"></span>**masque d’écriture de la \_ cible de rendu XML \_ \_ \_**  
ID de ressource pour la chaîne « masque d’écriture de la cible de rendu » utilisé dans les données XML.

<span id="XML_BLEND_ENABLE"></span><span id="xml_blend_enable"></span>**\_activation de Blend XML \_**  
ID de ressource pour la chaîne « Blend Enable » utilisé dans les données XML.

<span id="XML_ALPHA_TO_COVERAGE_ENABLE"></span><span id="xml_alpha_to_coverage_enable"></span>**XML \_ alpha \_ à \_ couverture \_ activée**  
ID de ressource pour la chaîne « alpha to Coverage Enable » utilisé dans les données XML.

<span id="XML_BLEND_STATE_1"></span><span id="xml_blend_state_1"></span>**\_État de fusion XML \_ \_ 1**  
ID de ressource pour la chaîne « état de fusion 1 » utilisé dans les données XML.

<span id="XML_INDEPENDENT_BLEND_ENABLE"></span><span id="xml_independent_blend_enable"></span>**\_activation de \_ Blend \_ indépendant XML**  
ID de ressource pour la chaîne « Independent Blend Enable » utilisé dans les données XML.

<span id="XML_RENDER_TARGET_NUMBER"></span><span id="xml_render_target_number"></span>**\_numéro de \_ cible de rendu XML \_**  
ID de ressource pour la chaîne « Render Target Number » utilisée dans les données XML.

<span id="XML_BUFFER"></span><span id="xml_buffer"></span>**\_mémoire tampon XML**  
ID de ressource pour la chaîne « buffer » utilisé dans les données XML.

<span id="XML_PRIMITIVE_TOPOLOGY"></span><span id="xml_primitive_topology"></span>**\_topologie de primitive XML \_**  
ID de ressource pour la chaîne « topologie primitive » utilisée dans les données XML.

<span id="XML_STRIDE"></span><span id="xml_stride"></span>**\_Stride XML**  
ID de ressource pour la chaîne « Stride » utilisée dans les données XML.

<span id="XML_TOP"></span><span id="xml_top"></span>**XML en \_ haut**  
ID de ressource pour la chaîne « Top » utilisée dans les données XML.

<span id="XML_RIGHT"></span><span id="xml_right"></span>**XML- \_ droite**  
ID de ressource pour la chaîne « Right » utilisé dans les données XML.

<span id="XML_BOTTOM"></span><span id="xml_bottom"></span>**XML en \_ bas**  
ID de ressource pour la chaîne « Bottom » utilisée dans les données XML.

<span id="XML_LEFT"></span><span id="xml_left"></span>**XML à \_ gauche**  
ID de ressource pour la chaîne « Left » utilisé dans les données XML.

<span id="XML_TOP_LEFT_X"></span><span id="xml_top_left_x"></span>**XML en \_ haut à \_ gauche \_ X**  
ID de ressource pour la chaîne « haut gauche x » utilisé dans les données XML.

<span id="XML_TOP_LEFT_Y"></span><span id="xml_top_left_y"></span>**XML en \_ haut à \_ gauche \_**  
ID de ressource pour la chaîne « haut gauche y » utilisé dans les données XML.

<span id="XML_MIN_DEPTH"></span><span id="xml_min_depth"></span>**\_profondeur minimale \_ XML**  
ID de ressource pour la chaîne « Depth min » utilisé dans les données XML.

<span id="XML_MAX_DEPTH"></span><span id="xml_max_depth"></span>**\_profondeur max. XML \_**  
ID de ressource pour la chaîne « max depth » utilisé dans les données XML.

<span id="XML_STENCIL_REFERENCE_VALUE"></span><span id="xml_stencil_reference_value"></span>**\_valeur de \_ Référence du stencil XML \_**  
ID de ressource pour la chaîne « valeur de référence du stencil » utilisée dans les données XML.

<span id="XML_DEPTH_BUFFER"></span><span id="xml_depth_buffer"></span>**\_mémoire tampon de profondeur XML \_**  
ID de ressource pour la chaîne « Depth buffer » utilisé dans les données XML.

<span id="XML_BLEND_FACTOR"></span><span id="xml_blend_factor"></span>**\_facteur de fusion XML \_**  
ID de ressource pour la chaîne « facteur de fusion » utilisé dans les données XML.

<span id="XML_SAMPLE_MASK"></span><span id="xml_sample_mask"></span>**\_exemple de \_ masque XML**  
ID de ressource pour la chaîne « exemple de masque » utilisé dans les données XML.

<span id="XML_DEPTH_STENCIL_STATE"></span><span id="xml_depth_stencil_state"></span>**\_État du \_ stencil de profondeur XML \_**  
ID de ressource pour la chaîne « État du stencil de profondeur » utilisé dans les données XML.

<span id="XML_SCISSOR_RECT"></span><span id="xml_scissor_rect"></span>**\_rectangle de ciseaux XML \_**  
ID de ressource pour la chaîne « ciseaux Rect » utilisé dans les données XML.

<span id="XML_SAMPLER"></span><span id="xml_sampler"></span>**\_échantillonneur XML**  
ID de ressource pour la chaîne « échantillonneur » utilisé dans les données XML.

<span id="XML_SHADER"></span><span id="xml_shader"></span>**\_nuanceur XML**  
ID de ressource pour la chaîne « Shader » utilisé dans les données XML.

<span id="XML_DEPTH_STENCIL_VIEW"></span><span id="xml_depth_stencil_view"></span>**\_vue du \_ stencil de profondeur XML \_**  
ID de ressource pour la « vue du stencil de profondeur » de la chaîne utilisée dans les données XML.

<span id="XML_SHADER_RESOURCE_VIEW"></span><span id="xml_shader_resource_view"></span>**\_affichage des ressources du nuanceur XML \_ \_**  
ID de ressource pour la chaîne « affichage des ressources de nuanceur » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUT"></span><span id="xml_input_layout"></span>**\_disposition d’entrée XML \_**  
ID de ressource pour la chaîne « disposition d’entrée » utilisée dans les données XML.

<span id="XML_CLASS_INSTANCE"></span><span id="xml_class_instance"></span>**\_instance de classe XML \_**  
ID de ressource pour la chaîne « instance de classe » utilisée dans les données XML.

<span id="XML_STREAM_OUTPUT_BUFFER"></span><span id="xml_stream_output_buffer"></span>**\_ \_ mémoire tampon de sortie de flux XML \_**  
ID de ressource pour la chaîne « tampon de sortie de flux » utilisé dans les données XML.

<span id="XML_SHADER_RESOURCE"></span><span id="xml_shader_resource"></span>**\_ressource de nuanceur XML \_**  
ID de ressource pour la chaîne « ressource de nuanceur » utilisée dans les données XML.

<span id="XML_RENDER_TARGET_INSTANCE"></span><span id="xml_render_target_instance"></span>**\_instance de \_ cible de rendu XML \_**  
ID de ressource pour la chaîne « rendu de l’instance cible » utilisé dans les données XML.

<span id="XML_VERTEX"></span><span id="xml_vertex"></span>**\_vertex XML**  
ID de ressource pour la chaîne « vertex » utilisé dans les données XML.

<span id="XML_VERTEX_BUFFER"></span><span id="xml_vertex_buffer"></span>**\_mémoire tampon de vertex XML \_**  
ID de ressource pour la chaîne « vertex buffer » utilisé dans les données XML.

<span id="XML_INSTANCE"></span><span id="xml_instance"></span>**\_instance XML**  
ID de ressource pour la chaîne « instance » utilisé dans les données XML.

<span id="XML_VIEWPORT"></span><span id="xml_viewport"></span>**\_VIEWPORT XML**  
ID de ressource pour la chaîne « Viewport » utilisé dans les données XML.

<span id="XML_FIRSTCONSTANT"></span><span id="xml_firstconstant"></span>**\_FIRSTCONSTANT XML**  
ID de ressource pour la chaîne « firstconstant » utilisée dans les données XML.

<span id="XML_NUMCONSTANTS"></span><span id="xml_numconstants"></span>**\_NUMCONSTANTS XML**  
ID de ressource pour la chaîne « numconstants » utilisée dans les données XML.

<span id="XML_LOGIC_OP"></span><span id="xml_logic_op"></span>**\_logique XML \_**  
ID de ressource pour la chaîne « Logic op » utilisée dans les données XML.

<span id="XML_LOGICOPS_ENABLE"></span><span id="xml_logicops_enable"></span>**LOGICOPS XML- \_ \_ activer**  
ID de ressource pour la chaîne « logicops Enable » utilisé dans les données XML.

<span id="XML_SHADER_FILE"></span><span id="xml_shader_file"></span>**\_fichier de nuanceur XML \_**  
ID de ressource pour la chaîne « fichier shader » utilisé dans les données XML.

<span id="XML_FILE_NAME"></span><span id="xml_file_name"></span>**\_nom du fichier XML \_**  
ID de ressource pour la chaîne « nom de fichier » utilisée dans les données XML.

<span id="XML_10TEXTURE1D_DESC"></span><span id="xml_10texture1d_desc"></span>**XML \_ 10TEXTURE1D \_ desc**  
ID de ressource pour la chaîne « 10texture1d DESC » utilisé dans les données XML.

<span id="XML_10TEXTURE2D_DESC"></span><span id="xml_10texture2d_desc"></span>**XML \_ 10TEXTURE2D \_ desc**  
ID de ressource pour la chaîne « 10texture2d DESC » utilisé dans les données XML.

<span id="XML_10TEXTURE3D_DESC"></span><span id="xml_10texture3d_desc"></span>**XML \_ 10TEXTURE3D \_ desc**  
ID de ressource pour la chaîne « 10texture3d DESC » utilisé dans les données XML.

<span id="XML_TEXTURE_ARRAYSIZE"></span><span id="xml_texture_arraysize"></span>**\_arraySize de texture XML \_**  
ID de ressource pour la chaîne « texture arraySize » utilisée dans les données XML.

<span id="XML_TEXTURE_MIPLEVEL"></span><span id="xml_texture_miplevel"></span>**\_MIPLEVEL de texture XML \_**  
ID de ressource pour la chaîne « texture miplevel » utilisée dans les données XML.

<span id="XML_CPUFLAGS"></span><span id="xml_cpuflags"></span>**\_CPUFLAGS XML**  
ID de ressource pour la chaîne « cpuflags » utilisée dans les données XML.

<span id="XML_MISCFLAGS"></span><span id="xml_miscflags"></span>**\_MISCFLAGS XML**  
ID de ressource pour la chaîne « miscflags » utilisée dans les données XML.

<span id="XML_BINDFLAGS"></span><span id="xml_bindflags"></span>**\_BINDFLAGS XML**  
ID de ressource pour la chaîne « bindflags » utilisée dans les données XML.

<span id="XML_SAMPLEDESC_QUALITY"></span><span id="xml_sampledesc_quality"></span>**\_qualité SAMPLEDESC \_ XML**  
ID de ressource pour la chaîne « sampledesc Quality » utilisé dans les données XML.

<span id="XML_SAMPLEDESC_COUNT"></span><span id="xml_sampledesc_count"></span>**\_nombre de SAMPLEDESC XML \_**  
ID de ressource pour la chaîne « sampledesc Count » utilisée dans les données XML.

<span id="XML_DEPTH"></span><span id="xml_depth"></span>**\_profondeur XML**  
ID de ressource pour la « profondeur » de chaîne utilisée dans les données XML.

<span id="XML_11TEXTURE1D_DESC"></span><span id="xml_11texture1d_desc"></span>**XML \_ 11TEXTURE1D \_ desc**  
ID de ressource pour la chaîne « 11texture1d DESC » utilisé dans les données XML.

<span id="XML_11TEXTURE2D_DESC"></span><span id="xml_11texture2d_desc"></span>**XML \_ 11TEXTURE2D \_ desc**  
ID de ressource pour la chaîne « 11texture2d DESC » utilisé dans les données XML.

<span id="XML_11TEXTURE3D_DESC"></span><span id="xml_11texture3d_desc"></span>**XML \_ 11TEXTURE3D \_ desc**  
ID de ressource pour la chaîne « 11texture3d DESC » utilisé dans les données XML.

<span id="XML_BYTEWIDTH"></span><span id="xml_bytewidth"></span>**\_BYTEWIDTH XML**  
ID de ressource pour la chaîne « bytewidth » utilisée dans les données XML.

<span id="XML_10BUFFER_DESC"></span><span id="xml_10buffer_desc"></span>**XML \_ 10BUFFER \_ desc**  
ID de ressource pour la chaîne « 10buffer DESC » utilisé dans les données XML.

<span id="XML_11BUFFER_DESC"></span><span id="xml_11buffer_desc"></span>**XML \_ 11BUFFER \_ desc**  
ID de ressource pour la chaîne « 11Buffer DESC » utilisé dans les données XML.

<span id="XML_BYTESTRIDE"></span><span id="xml_bytestride"></span>**\_BYTESTRIDE XML**  
ID de ressource pour la chaîne « bytestride » utilisée dans les données XML.

<span id="XML_STEREO"></span><span id="xml_stereo"></span>**\_stéréo XML**  
ID de ressource pour la chaîne « stéréo » utilisé dans les données XML.

<span id="XML_BACKGROUNDCOLOR"></span><span id="xml_backgroundcolor"></span>**\_BACKGROUNDCOLOR XML**  
ID de ressource pour la chaîne « BackgroundColor » utilisée dans les données XML.

<span id="XML_ALPHA_MODE"></span><span id="xml_alpha_mode"></span>**\_mode Alpha \_ XML**  
ID de ressource pour la chaîne « mode Alpha » utilisé dans les données XML.

<span id="XML_RESTRICTTOOUTPUT"></span><span id="xml_restricttooutput"></span>**\_RESTRICTTOOUTPUT XML**  
ID de ressource pour la chaîne « restricttooutput » utilisée dans les données XML.

<span id="XML_MODEROTATION"></span><span id="xml_moderotation"></span>**\_MODEROTATION XML**  
ID de ressource pour la chaîne « moderotation » utilisée dans les données XML.

<span id="XML_ISTEMPORARYMONOSUPPORTED"></span><span id="xml_istemporarymonosupported"></span>**\_ISTEMPORARYMONOSUPPORTED XML**  
ID de ressource pour la chaîne « istemporarymonosupported » utilisée dans les données XML.

<span id="XML_FORCED_SAMPLE_COUNT"></span><span id="xml_forced_sample_count"></span>**\_nombre d' \_ échantillons FORCÉs XML \_**  
ID de ressource pour la chaîne « nombre d’échantillons forcés » utilisé dans les données XML.

<span id="XML_QUERY"></span><span id="xml_query"></span>**\_requête XML**  
ID de ressource pour la chaîne « requête » utilisé dans les données XML.

<span id="XML_10QUERY_DESC"></span><span id="xml_10query_desc"></span>**XML \_ 10QUERY \_ desc**  
ID de ressource pour la chaîne « 10query DESC » utilisé dans les données XML.

<span id="XML_11QUERY_DESC"></span><span id="xml_11query_desc"></span>**XML \_ 11QUERY \_ desc**  
ID de ressource pour la chaîne « 11query DESC » utilisé dans les données XML.

<span id="XML_FLAGS_NONE"></span><span id="xml_flags_none"></span>**\_indicateurs XML \_ aucun**  
ID de ressource pour la chaîne « Flags None » utilisé dans les données XML.

<span id="XML_BUFFERUSAGE"></span><span id="xml_bufferusage"></span>**\_BUFFERUSAGE XML**  
ID de ressource pour la chaîne « BufferUsage » utilisée dans les données XML.

<span id="XML_UNKNOWN"></span><span id="xml_unknown"></span>**XML \_ inconnu**  
ID de ressource pour la chaîne « Unknown » utilisé dans les données XML.

<span id="XML_MAXFRAMELATENCY"></span><span id="xml_maxframelatency"></span>**\_MAXFRAMELATENCY XML**  
ID de ressource pour la chaîne « maxframelatency » utilisée dans les données XML.

<span id="XML_SOURCESIZEWIDTH"></span><span id="xml_sourcesizewidth"></span>**\_SOURCESIZEWIDTH XML**  
ID de ressource pour la chaîne « sourcesizewidth » utilisée dans les données XML.

<span id="XML_SOURCESIZEHEIGHT"></span><span id="xml_sourcesizeheight"></span>**\_SOURCESIZEHEIGHT XML**  
ID de ressource pour la chaîne « sourcesizeheight » utilisée dans les données XML.

<span id="XML_MATRIXTRANSFORM"></span><span id="xml_matrixtransform"></span>**\_MATRIXTRANSFORM XML**  
ID de ressource pour la chaîne « MatrixTransform » utilisée dans les données XML.

<span id="XML_TILED_RESOURCE_INFO"></span><span id="xml_tiled_resource_info"></span>**\_ \_ informations sur les ressources en mosaïque XML \_**  
ID de ressource pour la chaîne « informations sur les ressources en mosaïque » utilisé dans les données XML.

<span id="XML_TILES_RESOURCE_SUBRESOURCE_TILINGS"></span><span id="xml_tiles_resource_subresource_tilings"></span>**mosaïques XML en \_ \_ \_ \_ mosaïques de ressources sous-ressources**  
ID de ressource pour la chaîne « mosaïques de sous-ressources de ressources » utilisées dans les données XML.

<span id="XML_DEVICE_CHILD_INFORMATION"></span><span id="xml_device_child_information"></span>**\_ \_ informations sur les enfants de périphérique XML \_**  
ID de ressource pour la chaîne « informations de l’enfant de l’appareil » utilisé dans les données XML.

<span id="XML_DEVICE"></span><span id="xml_device"></span>**\_appareil XML**  
ID de ressource pour la chaîne « Device » utilisé dans les données XML.

<span id="XML_OTHER"></span><span id="xml_other"></span>**\_autre XML**  
ID de ressource pour la chaîne « Other » utilisée dans les données XML.

<span id="XML_PREDICATE"></span><span id="xml_predicate"></span>**\_PRÉDICAT XML**  
ID de ressource pour la chaîne « Predicate » utilisé dans les données XML.

<span id="XML_PREDICATE_VALUE"></span><span id="xml_predicate_value"></span>**\_valeur de PRÉDICAT \_ XML**  
ID de ressource pour la chaîne « valeur de prédicat » utilisée dans les données XML.

<span id="XML_DEVICE_CONTEXT_TYPE"></span><span id="xml_device_context_type"></span>**\_type de \_ contexte de périphérique XML \_**  
ID de ressource pour la chaîne « type de contexte de périphérique » utilisé dans les données XML.

<span id="XML_BUFFER_HANDLE"></span><span id="xml_buffer_handle"></span>**\_handle de mémoire tampon XML \_**  
ID de ressource pour la chaîne « descripteur de tampon » utilisé dans les données XML.

<span id="XML_DEVICE_12"></span><span id="xml_device_12"></span>**\_Périphérique XML \_ 12**  
ID de ressource pour la chaîne « Device 12 » utilisé dans les données XML.

<span id="XML_MAX_RECORDING_COMMAND_LISTS"></span><span id="xml_max_recording_command_lists"></span>**\_listes de \_ commandes d’enregistrement Max \_ . XML \_**  
ID de ressource pour la chaîne « listes de commandes d’enregistrement maximale » utilisé dans les données XML.

<span id="XML_DEFAULT_COMMAND_QUEUE"></span><span id="xml_default_command_queue"></span>**\_ \_ file d’attente de commandes par défaut XML \_**  
ID de ressource pour la chaîne « file d’attente de commandes par défaut » utilisée dans les données XML.

<span id="XML_D3D11_DEVICE_INFORMATION"></span><span id="xml_d3d11_device_information"></span>**\_ \_ Informations sur l’appareil d3d11 XML \_**  
ID de ressource pour la chaîne « informations de l’appareil d3d11 » utilisé dans les données XML.

<span id="XML_D3D12_DEVICE_INFORMATION"></span><span id="xml_d3d12_device_information"></span>**\_ \_ Informations sur l’appareil D3D12 XML \_**  
ID de ressource pour la chaîne « informations de l’appareil d3d12 » utilisé dans les données XML.

<span id="XML_CONSERVATIVE_RASTER"></span><span id="xml_conservative_raster"></span>**raster (raster) \_ classique XML \_**  
ID de ressource pour la chaîne « raster conservateur » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUT_12"></span><span id="xml_input_layout_12"></span>**\_Disposition d’entrée XML \_ \_ 12**  
ID de ressource pour la chaîne « disposition d’entrée 12 » utilisée dans les données XML.

<span id="XML_GRAPHICS_PIPELINE_STATE_12"></span><span id="xml_graphics_pipeline_state_12"></span>**\_ \_ \_ État 12 du PIPELINe graphique XML \_**  
ID de ressource pour la chaîne « État du pipeline de graphiques 12 » utilisé dans les données XML.

<span id="XML_BLEND_STATE_12"></span><span id="xml_blend_state_12"></span>**\_État de fusion XML \_ \_ 12**  
ID de ressource pour la chaîne « état de fusion 12 » utilisé dans les données XML.

<span id="XML_ROOT_SIGNATURE_12"></span><span id="xml_root_signature_12"></span>**SIGNATURE de la \_ racine XML \_ \_ 12**  
ID de ressource pour la chaîne « signature racine 12 » utilisé dans les données XML.

<span id="XML_STREAM_OUTPUT_12"></span><span id="xml_stream_output_12"></span>**\_Sortie de flux XML \_ \_ 12**  
ID de ressource pour la chaîne « flux de sortie 12 » utilisé dans les données XML.

<span id="XML_RASTERIZED_STREAM"></span><span id="xml_rasterized_stream"></span>**\_flux PIXELLISÉ XML \_**  
ID de ressource pour la chaîne « flux pixellisé » utilisé dans les données XML.

<span id="XML_SO_DECLARATION_ENTRY_12"></span><span id="xml_so_declaration_entry_12"></span>**XML \_ , \_ entrée de Déclaration \_ \_ 12**  
ID de ressource pour la chaîne « so entrée de déclaration 12 » utilisée dans les données XML.

<span id="XML_START_COMPONENT"></span><span id="xml_start_component"></span>**\_composant de démarrage XML \_**  
ID de ressource pour la chaîne « démarrer le composant » utilisé dans les données XML.

<span id="XML_COMPONENT_COUNT"></span><span id="xml_component_count"></span>**\_nombre de composants XML \_**  
ID de ressource pour la chaîne « nombre de composants » utilisé dans les données XML.

<span id="XML_OUTPUT_SLOT"></span><span id="xml_output_slot"></span>**\_emplacement de sortie XML \_**  
ID de ressource pour la chaîne « emplacement de sortie » utilisé dans les données XML.

<span id="XML_COMPUTE_PIPELINE_STATE_12"></span><span id="xml_compute_pipeline_state_12"></span>**\_État du pipeline de calcul XML \_ \_ \_ 12**  
ID de ressource pour la chaîne « Compute pipeline State 12 » utilisé dans les données XML.

<span id="XML_D3D12_SHADER_BYTECODE"></span><span id="xml_d3d12_shader_bytecode"></span>**\_ \_ Bytecode de nuanceur D3D12 XML \_**  
ID de ressource pour la chaîne « d3d12 Shader bytecode » utilisé dans les données XML.

<span id="XML_D3D12_SHADER_BYTECODE_PTR"></span><span id="xml_d3d12_shader_bytecode_ptr"></span>**\_Pointeur de \_ bytecode de nuanceur D3D12 XML \_ \_**  
ID de ressource pour la chaîne « d3d12 Shader bytecod PTR » utilisé dans les données XML.

<span id="XML_D3D12_SHADER_BYTECODE_LENGTH"></span><span id="xml_d3d12_shader_bytecode_length"></span>**\_Longueur du \_ bytecode du nuanceur D3D12 XML \_ \_**  
ID de ressource pour la chaîne « longueur du bytecode du nuanceur d3d12 » utilisé dans les données XML.

<span id="XML_D3D12_VIEW_DIMENSION"></span><span id="xml_d3d12_view_dimension"></span>**\_Dimension de \_ vue \_ D3D12 XML**  
ID de ressource pour la chaîne « d3d12 View dimension » utilisé dans les données XML.

<span id="XML_D3D12_NOTHING_TO_DEFINE"></span><span id="xml_d3d12_nothing_to_define"></span>**XML \_ D3D12 \_ rien \_ à \_ définir**  
ID de ressource pour la chaîne « d3d12 rien à définir » utilisé dans les données XML.

<span id="XML_OFFSET_IN_BYTES"></span><span id="xml_offset_in_bytes"></span>**\_décalage XML \_ en \_ octets**  
ID de ressource pour la chaîne « offset en octets » utilisé dans les données XML.

<span id="XML_SIZE_IN_BYTES"></span><span id="xml_size_in_bytes"></span>**\_taille XML \_ en \_ octets**  
ID de ressource pour la chaîne « taille en octets » utilisée dans les données XML.

<span id="XML_SO_CUR_OFFSET"></span><span id="xml_so_cur_offset"></span>**décalage de code XML \_ so \_ \_**  
ID de ressource pour la chaîne « so cur offset » utilisé dans les données XML.

<span id="XML_ROOT_SIGNATURE"></span><span id="xml_root_signature"></span>**\_signature racine \_ XML**  
ID de ressource pour la chaîne « signature racine » utilisé dans les données XML.

<span id="XML_NUM_PARAMETERS"></span><span id="xml_num_parameters"></span>**\_paramètres de nombre XML \_**  
ID de ressource pour la chaîne « num Parameters » utilisée dans les données XML.

<span id="XML_ROOT_PARAMETERS"></span><span id="xml_root_parameters"></span>**\_paramètres racine \_ XML**  
ID de ressource pour la chaîne « paramètres racine » utilisé dans les données XML.

<span id="XML_ROOT_PARAMETER"></span><span id="xml_root_parameter"></span>**\_paramètre racine \_ XML**  
ID de ressource pour la chaîne « paramètre racine » utilisé dans les données XML.

<span id="XML_ROOT_PARAMETER_TYPE"></span><span id="xml_root_parameter_type"></span>**\_type de \_ paramètre \_ racine XML**  
ID de ressource pour la chaîne « type de paramètre racine » utilisé dans les données XML.

<span id="XML_ROOT_PARAMETER_SHADER_VISIBILITY"></span><span id="xml_root_parameter_shader_visibility"></span>**\_visibilité du \_ \_ nuanceur de paramètres racine XML \_**  
ID de ressource pour la chaîne « visibilité du nuanceur de paramètre racine » utilisé dans les données XML.

<span id="XML_DESCRIPTOR_HANDLE"></span><span id="xml_descriptor_handle"></span>**\_handle de descripteur XML \_**  
ID de ressource pour la chaîne « handle de descripteur » utilisé dans les données XML.

<span id="XML_DESCRIPTOR_HANDLE_PTR"></span><span id="xml_descriptor_handle_ptr"></span>**\_ptr de handle de descripteur XML \_ \_**  
ID de ressource pour la chaîne « pointeur de descripteur PTR » utilisé dans les données XML.

<span id="XML_ROOT_CONSTANTS"></span><span id="xml_root_constants"></span>**\_ \_ constantes racine XML**  
ID de ressource pour la chaîne « constantes racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR"></span><span id="xml_root_descriptor"></span>**\_descripteur racine XML \_**  
ID de ressource pour la chaîne « descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_SHADER_REGISTER"></span><span id="xml_root_descriptor_shader_register"></span>**\_Registre de \_ nuanceur de descripteurs racine XML \_ \_**  
ID de ressource pour la chaîne « Registre de nuanceurs de descripteurs racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_REGISTER_SPACE"></span><span id="xml_root_descriptor_register_space"></span>**\_espace de \_ Registre du descripteur racine XML \_ \_**  
ID de ressource pour la chaîne « espace Registre du descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_TABLE"></span><span id="xml_root_descriptor_table"></span>**\_tableau de \_ descripteurs racine XML \_**  
ID de ressource de la chaîne « table descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_NUM_RANGES"></span><span id="xml_root_descriptor_num_ranges"></span>**\_nombre de \_ plages de descripteurs racine XML \_ \_**  
ID de ressource pour la chaîne « plages de nombre de descripteurs racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_RANGES"></span><span id="xml_root_descriptor_ranges"></span>**\_plages de \_ descripteurs racine XML \_**  
ID de ressource pour la chaîne « plages du descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE"></span><span id="xml_root_descriptor_range"></span>**\_plage du \_ descripteur racine XML \_**  
ID de ressource pour la chaîne « plage du descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_TYPE"></span><span id="xml_root_descriptor_range_type"></span>**\_type de \_ plage du descripteur racine XML \_ \_**  
ID de ressource pour la chaîne « type de plage du descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_COUNT"></span><span id="xml_root_descriptor_range_count"></span>**\_nombre de \_ plages du descripteur racine XML \_ \_**  
ID de ressource pour la chaîne « nombre de plages du descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_BASE_SHADER_REGISTER"></span><span id="xml_root_descriptor_range_base_shader_register"></span>**\_Registre de \_ \_ \_ \_ nuanceur de base \_ de la plage de descripteurs racine XML**  
ID de ressource pour la chaîne « Registre de métadonnées de base de la plage de descripteurs racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_REGISTER_SPACE"></span><span id="xml_root_descriptor_range_register_space"></span>**espace du registre de la \_ \_ plage du descripteur racine \_ \_ XML \_**  
ID de ressource pour la chaîne « espace du registre de la plage du descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_OFFSET"></span><span id="xml_root_descriptor_range_offset"></span>**décalage de la \_ \_ plage du descripteur racine \_ XML \_**  
ID de ressource pour la chaîne « décalage de la plage du descripteur racine » utilisé dans les données XML.

<span id="XML_ROOT_CONSTANTS_SHADER_REGISTER"></span><span id="xml_root_constants_shader_register"></span>**\_Registre de \_ nuanceur de constantes racine XML \_ \_**  
ID de ressource pour la chaîne « racine des constantes de nuanceur » utilisé dans les données XML.

<span id="XML_ROOT_CONSTANTS_REGISTER_SPACE"></span><span id="xml_root_constants_register_space"></span>**\_ \_ espace Registre des constantes racine XML \_ \_**  
ID de ressource pour la chaîne « racine constantes de l’espace de registre » utilisé dans les données XML.

<span id="XML_ROOT_CONSTANTS_INT32_COUNT"></span><span id="xml_root_constants_int32_count"></span>**\_Nombre de constantes de la racine XML \_ \_ Int32 \_**  
ID de ressource pour la chaîne « root constants Int32 Count » utilisé dans les données XML.

<span id="XML_NOT_SET"></span><span id="xml_not_set"></span>**XML \_ non \_ défini**  
ID de ressource pour la chaîne « non défini » utilisé dans les données XML.

<span id="XML_ERROR_RETRIEVING_VALUE"></span><span id="xml_error_retrieving_value"></span>**erreur XML lors de la récupération de la \_ \_ \_ valeur**  
ID de ressource pour la chaîne « erreur lors de la récupération de la valeur » utilisée dans les données XML.

<span id="XML_VERTEX_SHADER"></span><span id="xml_vertex_shader"></span>**\_nuanceur de vertex XML \_**  
ID de ressource pour la chaîne « vertex shader » utilisé dans les données XML.

<span id="XML_PIXEL_SHADER"></span><span id="xml_pixel_shader"></span>**\_nuanceur de pixels XML \_**  
ID de ressource pour la chaîne « nuanceur de pixels » utilisée dans les données XML.

<span id="XML_DOMAIN_SHADER"></span><span id="xml_domain_shader"></span>**\_nuanceur de domaine XML \_**  
ID de ressource pour la chaîne « nuanceur de domaine » utilisé dans les données XML.

<span id="XML_HULL_SHADER"></span><span id="xml_hull_shader"></span>**\_nuanceur de coque XML \_**  
ID de ressource pour la chaîne « nuanceur de coque » utilisé dans les données XML.

<span id="XML_GEOMETRY_SHADER"></span><span id="xml_geometry_shader"></span>**\_nuanceur de géométrie XML \_**  
ID de ressource pour la chaîne « nuanceur de géométrie » utilisé dans les données XML.

<span id="XML_ROOT_SIGNATURE_OBJECT"></span><span id="xml_root_signature_object"></span>**\_objet de \_ signature \_ racine XML**  
ID de ressource pour la chaîne « objet de signature racine » utilisé dans les données XML.

<span id="XML_ROOT_SIGNATURE_DESERIALIZED_DESC"></span><span id="xml_root_signature_deserialized_desc"></span>**\_ \_ \_ Description désérialisée de la signature racine XML \_**  
ID de ressource pour la chaîne « la signature racine désérialisée DESC » utilisé dans les données XML.

<span id="XML_COMMAND_QUEUE"></span><span id="xml_command_queue"></span>**\_ \_ file d’attente de commandes XML**  
ID de ressource pour la chaîne « file d’attente de commandes » utilisée dans les données XML.

<span id="XML_D3D12_COMMAND_LIST_TYPE_TYPE"></span><span id="xml_d3d12_command_list_type_type"></span>**\_Type de liste de commandes D3D12 XML ( \_ \_ \_ \_ type)**  
ID de ressource pour la chaîne « type de liste de commandes d3d12 type » utilisé dans les données XML.

<span id="XML_COMMAND_QUEUE_PRIORITY"></span><span id="xml_command_queue_priority"></span>**priorité de la \_ \_ file d’attente des commandes XML \_**  
ID de ressource pour la chaîne « priorité de file d’attente de commandes » utilisée dans les données XML.

<span id="XML_COMMAND_QUEUE_IS_DEFAULT"></span><span id="xml_command_queue_is_default"></span>**la \_ \_ file d’attente de commandes XML \_ est la \_ valeur par défaut**  
ID de ressource pour la chaîne « la file d’attente de commandes est la valeur par défaut » utilisée dans les données XML.

<span id="XML_COMMAND_ALLOCATOR"></span><span id="xml_command_allocator"></span>**\_ALlocateur de commandes XML \_**  
ID de ressource pour la chaîne « allocateur de commande » utilisé dans les données XML.

<span id="XML_D3D12_RESOURCE"></span><span id="xml_d3d12_resource"></span>**\_Ressource D3D12 \_ XML**  
ID de ressource pour la chaîne « d3d12 Resource » utilisée dans les données XML.

<span id="XML_D3D12_DESCRIPTOR_HEAP"></span><span id="xml_d3d12_descriptor_heap"></span>**\_Tas du \_ descripteur D3D12 XML \_**  
ID de ressource pour la chaîne « d3d12 descripteur Heap » utilisé dans les données XML.

<span id="XML_D3D12_DESCRIPTOR"></span><span id="xml_d3d12_descriptor"></span>**\_Descripteur D3D12 XML \_**  
ID de ressource pour la chaîne « descripteur d3d12 » utilisé dans les données XML.

<span id="XML_D3D12_RTV"></span><span id="xml_d3d12_rtv"></span>**\_D3D12 \_ RTV XML**  
ID de ressource pour la chaîne « d3d12 RTV » utilisé dans les données XML.

<span id="XML_D3D12_VBV"></span><span id="xml_d3d12_vbv"></span>**XML \_ D3D12 \_ VBV**  
ID de ressource pour la chaîne « d3d12 VBV » utilisé dans les données XML.

<span id="XML_D3D12_IBV"></span><span id="xml_d3d12_ibv"></span>**XML \_ D3D12 \_ IBV**  
ID de ressource pour la chaîne « d3d12 IBV » utilisé dans les données XML.

<span id="XML_D3D12_CBV"></span><span id="xml_d3d12_cbv"></span>**XML \_ D3D12 \_ CBV**  
ID de ressource pour la chaîne « d3d12 CBV » utilisé dans les données XML.

<span id="XML_D3D12_SRV"></span><span id="xml_d3d12_srv"></span>**XML \_ D3D12 \_ SRV**  
ID de ressource pour la chaîne « d3d12 SRV » utilisé dans les données XML.

<span id="XML_D3D12_UAV"></span><span id="xml_d3d12_uav"></span>**XML \_ D3D12 \_ UAV**  
ID de ressource pour la chaîne « d3d12 UAV » utilisé dans les données XML.

<span id="XML_D3D12_SOV"></span><span id="xml_d3d12_sov"></span>**XML \_ D3D12 \_ SOV**  
ID de ressource pour la chaîne « d3d12 SOV » utilisé dans les données XML.

<span id="XML_D3D12_SAMPLER"></span><span id="xml_d3d12_sampler"></span>**\_ \_ Échantillonneur D3D12 XML**  
ID de ressource pour la chaîne « échantillonneur d3d12 » utilisé dans les données XML.

<span id="XML_D3D12_DSV"></span><span id="xml_d3d12_dsv"></span>**\_DSV D3D12 \_ XML**  
ID de ressource pour la chaîne « d3d12 DSV » utilisé dans les données XML.

<span id="XML_TEXTURE_1D"></span><span id="xml_texture_1d"></span>**\_Texture XML \_ 1D**  
ID de ressource pour la chaîne « texture 1D » utilisée dans les données XML.

<span id="XML_TEXTURE_1DARRAY"></span><span id="xml_texture_1darray"></span>**\_1DARRAY de texture XML \_**  
ID de ressource pour la chaîne « texture 1darray » utilisée dans les données XML.

<span id="XML_TEXTURE_2D"></span><span id="xml_texture_2d"></span>**\_Texture XML \_ 2D**  
ID de ressource pour la chaîne « texture 2D » utilisée dans les données XML.

<span id="XML_TEXTURE_2DARRAY"></span><span id="xml_texture_2darray"></span>**\_2DARRAY de texture XML \_**  
ID de ressource pour la chaîne « texture 2DArray » utilisée dans les données XML.

<span id="XML_TEXTURE_2DMS"></span><span id="xml_texture_2dms"></span>**\_2DMS de texture XML \_**  
ID de ressource pour la chaîne « texture 2dms » utilisée dans les données XML.

<span id="XML_TEXTURE_2DMSARRAY"></span><span id="xml_texture_2dmsarray"></span>**\_2DMSARRAY de texture XML \_**  
ID de ressource pour la chaîne « texture 2dmsarray » utilisée dans les données XML.

<span id="XML_TEXTURE_3D"></span><span id="xml_texture_3d"></span>**\_Texture XML \_ 3D**  
ID de ressource pour la chaîne « texture 3D » utilisé dans les données XML.

<span id="XML_MISCELLANEOUS"></span><span id="xml_miscellaneous"></span>**XML \_ divers**  
ID de ressource pour la chaîne « divers » utilisé dans les données XML.

<span id="XML_RENDER_TARGET_VIEW_12"></span><span id="xml_render_target_view_12"></span>**Vue de la \_ cible de rendu XML \_ \_ \_ 12**  
ID de ressource pour la chaîne « affichage de la cible de rendu 12 » utilisé dans les données XML.

<span id="XML_STRIDE_COUNT"></span><span id="xml_stride_count"></span>**\_nombre de Strides XML \_**  
ID de ressource pour la chaîne « nombre Stride » utilisé dans les données XML.

<span id="XML_SHADER_ROOT_SIGNATURES"></span><span id="xml_shader_root_signatures"></span>**\_signatures racines de nuanceur XML \_ \_**  
ID de ressource pour la chaîne « signatures racines de nuanceur » utilisée dans les données XML.

<span id="XML_CMDLIST_ROOT_SIGNATURE_OBJECT"></span><span id="xml_cmdlist_root_signature_object"></span>**\_objet de \_ \_ signature racine CMDLIST XML \_**  
ID de ressource pour la chaîne « cmdlist root signature Object » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_COUNT"></span><span id="xml_input_layout_element_count"></span>**\_nombre d’éléments de la disposition d’entrée XML \_ \_ \_**  
ID de ressource pour la chaîne « nombre d’éléments de disposition d’entrée » utilisé dans les données XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_DESCRIPTIONS"></span><span id="xml_input_layout_element_descriptions"></span>**\_ \_ \_ descriptions des éléments de disposition d’entrée XML \_**  
ID de ressource pour la chaîne « descriptions des éléments de disposition d’entrée » utilisée dans les données XML.

<span id="XML_SHADER_DESCRIPTION"></span><span id="xml_shader_description"></span>**\_Description du nuanceur XML \_**  
ID de ressource pour la chaîne « Description du nuanceur » utilisée dans les données XML.

<span id="XML_PSO_ROOT_SIGNATURE_OBJECT"></span><span id="xml_pso_root_signature_object"></span>**\_objet de \_ \_ signature racine XML PSO \_**  
ID de ressource pour la chaîne « objet de signature racine PSO » utilisé dans les données XML.

<span id="XML_BUFFER_LOCATION"></span><span id="xml_buffer_location"></span>**emplacement de la \_ mémoire tampon XML \_**  
ID de ressource pour la chaîne « emplacement de la mémoire tampon » utilisé dans les données XML.

<span id="XML_BUFFER_FILLED_LOCATION"></span><span id="xml_buffer_filled_location"></span>**emplacement rempli par la \_ mémoire tampon XML \_ \_**  
ID de ressource pour la chaîne « emplacement rempli par la mémoire tampon » utilisé dans les données XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS"></span><span id="xml_d3d12_virtual_address"></span>**\_ \_ Adresse virtuelle D3D12 \_ XML**  
ID de ressource pour la chaîne « adresse virtuelle d3d12 » utilisée dans les données XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS_OFFSET"></span><span id="xml_d3d12_virtual_address_offset"></span>**\_Décalage d' \_ \_ adresse virtuelle D3D12 XML \_**  
ID de ressource pour la chaîne « d3d12 Virtual Address offset » utilisé dans les données XML.

<span id="XML_D3D12_OFFSET_IN_BYTES"></span><span id="xml_d3d12_offset_in_bytes"></span>**\_Décalage D3D12 \_ XML \_ en \_ octets**  
ID de ressource pour la chaîne « décalage d3d12 en octets » utilisé dans les données XML.

<span id="XML_MAPPED_TILES"></span><span id="xml_mapped_tiles"></span>**\_vignettes mappées XML \_**  
ID de ressource pour la chaîne « vignettes mappées » utilisée dans les données XML.

<span id="XML_POOL_TILES"></span><span id="xml_pool_tiles"></span>**\_vignettes de pool XML \_**  
ID de ressource pour la chaîne « vignettes de pool » utilisée dans les données XML.

<span id="XML_TILE"></span><span id="xml_tile"></span>**\_vignette XML**  
ID de ressource pour la chaîne « Tile » utilisée dans les données XML.

<span id="XML_OPTIONS"></span><span id="xml_options"></span>**\_options XML**  
ID de ressource pour la chaîne « options » utilisée dans les données XML.

<span id="XML_TILE_LIST_MODE"></span><span id="xml_tile_list_mode"></span>**\_mode liste des vignettes XML \_ \_**  
ID de ressource pour la chaîne « mode de liste de mosaïques » utilisé dans les données XML.

<span id="XML_TILE_POOL_SLOT"></span><span id="xml_tile_pool_slot"></span>**\_emplacement du pool de mosaïques XML \_ \_**  
ID de ressource pour la chaîne « emplacement du pool de mosaïques » utilisé dans les données XML.

<span id="XML_TILE_COORDINATE"></span><span id="xml_tile_coordinate"></span>**\_coordonnée de vignette XML \_**  
ID de ressource pour la chaîne « coordination des vignettes » utilisé dans les données XML.

<span id="XML_HEAP_TYPE"></span><span id="xml_heap_type"></span>**\_type de tas XML \_**  
ID de ressource pour la chaîne « type de segment » utilisé dans les données XML.

<span id="XML_RESOURCE_USAGE"></span><span id="xml_resource_usage"></span>**\_utilisation des ressources XML \_**  
ID de ressource pour la chaîne « utilisation des ressources » utilisée dans les données XML.

<span id="XML_RESOURCE_USAGES"></span><span id="xml_resource_usages"></span>**\_utilisations des ressources XML \_**  
ID de ressource pour la chaîne « utilisations des ressources » utilisée dans les données XML.

<span id="XML_SUBRESOURCE"></span><span id="xml_subresource"></span>**sous- \_ ressource XML**  
ID de ressource pour la chaîne « Resource » utilisée dans les données XML.

<span id="XML_SUBRESOURCE_USAGE"></span><span id="xml_subresource_usage"></span>**utilisation des sous- \_ ressources XML \_**  
ID de ressource pour la chaîne « utilisation de sous-ressources » utilisée dans les données XML.

<span id="XML_COMPUTE_SHADER"></span><span id="xml_compute_shader"></span>**\_nuanceur de calcul XML \_**  
ID de ressource pour la chaîne « nuanceur de calcul » utilisée dans les données XML.

<span id="XML_COMMAND_LIST_12"></span><span id="xml_command_list_12"></span>**\_Liste de commandes XML \_ \_ 12**  
ID de ressource pour la chaîne « liste de commandes 12 » utilisée dans les données XML.

<span id="XML_PSO_RENDER_TARGET_FORMATS"></span><span id="xml_pso_render_target_formats"></span>**\_ \_ formats cibles de rendu XML PSO \_ \_**  
ID de ressource pour la chaîne « PSO Render cible formats » utilisé dans les données XML.

<span id="XML_RENDER_TARGET_VIEWS"></span><span id="xml_render_target_views"></span>**vues de la \_ cible de rendu XML \_ \_**  
ID de ressource pour la chaîne « affichage de la cible de rendu » utilisé dans les données XML.

<span id="XML_SHADER_RESOURCE_VIEWS"></span><span id="xml_shader_resource_views"></span>**\_vues des ressources de nuanceur XML \_ \_**  
ID de ressource pour la chaîne « vues des ressources de nuanceur » utilisée dans les données XML.

## <a name="requirements"></a>Configuration requise

<table><colgroup><col  /><col  /></colgroup><tbody><tr class="odd"><td><p>En-tête</p></td><td>Vspixengine. h</td></tr></tbody></table>

 

 



