---
description: 'En savoir plus sur : fonction JetMakeKey'
title: Fonction JetMakeKey
TOCTitle: JetMakeKey Function
ms:assetid: 8c1cff2f-2f24-4990-a9d8-fb4f822e50b1
ms:mtpsurl: https://msdn.microsoft.com/library/Gg269329(v=EXCHG.10)
ms:contentKeyID: 32765619
ms.date: 04/11/2016
ms.topic: reference
api_name:
- JetMakeKey
topic_type:
- apiref
- kbArticle
api_type:
- COM
- DLLExport
api_location:
- ESENT.DLL
ROBOTS: INDEX,FOLLOW
ms.openlocfilehash: e3d121ed83f096baad249aab8677bb9f5e72e301
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 01/08/2021
ms.locfileid: "104210099"
---
# <a name="jetmakekey-function"></a><span data-ttu-id="9876e-103">Fonction JetMakeKey</span><span class="sxs-lookup"><span data-stu-id="9876e-103">JetMakeKey Function</span></span>


<span data-ttu-id="9876e-104">_**S’applique à :** Windows | Serveur Windows_</span><span class="sxs-lookup"><span data-stu-id="9876e-104">_**Applies to:** Windows | Windows Server_</span></span>

## <a name="jetmakekey-function"></a><span data-ttu-id="9876e-105">Fonction JetMakeKey</span><span class="sxs-lookup"><span data-stu-id="9876e-105">JetMakeKey Function</span></span>

<span data-ttu-id="9876e-106">La fonction **JetMakeKey** construit des clés de recherche qui peuvent ensuite être utilisées pour rechercher un ensemble d’entrées dans un index par certains critères de recherche simples sur les valeurs de leurs colonnes clés.</span><span class="sxs-lookup"><span data-stu-id="9876e-106">The **JetMakeKey** function constructs search keys that may then be used to find a set of entries in an index by some simple search criteria on their key column values.</span></span> <span data-ttu-id="9876e-107">Une clé de recherche est également l’une des propriétés intrinsèques d’un curseur et est utilisée par les opérations [JetSeek](./jetseek-function.md) et [JetSetIndexRange](./jetsetindexrange-function.md) pour localiser les entrées d’index correspondant à ces critères de recherche sur l’index actuel de ce curseur.</span><span class="sxs-lookup"><span data-stu-id="9876e-107">A search key is also one of the intrinsic properties of a cursor and is used by the [JetSeek](./jetseek-function.md) and [JetSetIndexRange](./jetsetindexrange-function.md) operations to locate index entries matching these search criteria on the current index of that cursor.</span></span> <span data-ttu-id="9876e-108">Une clé de recherche complète est créée dans une série d’appels **JetMakeKey** où chaque appel est utilisé pour charger la valeur de colonne pour la colonne clé suivante de l’index actuel d’un curseur.</span><span class="sxs-lookup"><span data-stu-id="9876e-108">A complete search key is built up in a series of **JetMakeKey** calls where each call is used to load the column value for the next key column of the current index of a cursor.</span></span> <span data-ttu-id="9876e-109">Il est également possible de charger une clé de recherche précédemment construite qui a été récupérée à partir du curseur à l’aide de [JetRetrieveKey](./jetretrievekey-function.md).</span><span class="sxs-lookup"><span data-stu-id="9876e-109">It is also possible to load a previously constructed search key that has been retrieved from the cursor using [JetRetrieveKey](./jetretrievekey-function.md).</span></span>

```cpp
    JET_ERR JET_API JetMakeKey(
      __in          JET_SESID sesid,
      __in          JET_TABLEID tableid,
      __in_opt      const void* pvData,
      __in          unsigned long cbData,
      __in          JET_GRBIT grbit
    );
```

### <a name="parameters"></a><span data-ttu-id="9876e-110">Paramètres</span><span class="sxs-lookup"><span data-stu-id="9876e-110">Parameters</span></span>

<span data-ttu-id="9876e-111">*sesid*</span><span class="sxs-lookup"><span data-stu-id="9876e-111">*sesid*</span></span>

<span data-ttu-id="9876e-112">Session à utiliser pour cet appel.</span><span class="sxs-lookup"><span data-stu-id="9876e-112">The session to use for this call.</span></span>

<span data-ttu-id="9876e-113">*TableID*</span><span class="sxs-lookup"><span data-stu-id="9876e-113">*tableid*</span></span>

<span data-ttu-id="9876e-114">Curseur à utiliser pour cet appel.</span><span class="sxs-lookup"><span data-stu-id="9876e-114">The cursor to use for this call.</span></span>

<span data-ttu-id="9876e-115">*pvData*</span><span class="sxs-lookup"><span data-stu-id="9876e-115">*pvData*</span></span>

<span data-ttu-id="9876e-116">Mémoire tampon d’entrée contenant les données de colonne pour la colonne clé actuelle de l’index actuel du curseur pour lequel la clé de recherche est construite.</span><span class="sxs-lookup"><span data-stu-id="9876e-116">The input buffer containing the column data for the current key column of the current index of the cursor for which the search key is being constructed.</span></span>

<span data-ttu-id="9876e-117">Le type de données des données de colonne dans la mémoire tampon d’entrée doit correspondre exactement au type de données et à d’autres propriétés de la définition de colonne de la colonne clé actuelle.</span><span class="sxs-lookup"><span data-stu-id="9876e-117">The data type of the column data in the input buffer must exactly match the data type and other properties of the column definition of the current key column.</span></span> <span data-ttu-id="9876e-118">Aucune contrainte de type n’est effectuée sur les données de colonne.</span><span class="sxs-lookup"><span data-stu-id="9876e-118">No type coercion is performed on the column data whatsoever.</span></span>

<span data-ttu-id="9876e-119">Si JET_bitNormalizedKey est spécifié dans le paramètre *Grbit* , la mémoire tampon d’entrée doit contenir une clé de recherche précédemment construite.</span><span class="sxs-lookup"><span data-stu-id="9876e-119">If JET_bitNormalizedKey is specified in the *grbit* parameter, the input buffer must contain a previously constructed search key.</span></span> <span data-ttu-id="9876e-120">Ces clés sont obtenues à l’aide d’un appel à [JetRetrieveKey](./jetretrievekey-function.md).</span><span class="sxs-lookup"><span data-stu-id="9876e-120">Such keys are obtained using a call to [JetRetrieveKey](./jetretrievekey-function.md).</span></span>

<span data-ttu-id="9876e-121">*cbData*</span><span class="sxs-lookup"><span data-stu-id="9876e-121">*cbData*</span></span>

<span data-ttu-id="9876e-122">Taille en octets des données de colonne fournies dans la mémoire tampon d’entrée.</span><span class="sxs-lookup"><span data-stu-id="9876e-122">The size in bytes of the column data provided in the input buffer.</span></span>

<span data-ttu-id="9876e-123">Si JET_bitNormalizedKey est spécifié dans le paramètre *Grbit* , il s’agit de la taille de la clé de recherche fournie dans la mémoire tampon d’entrée.</span><span class="sxs-lookup"><span data-stu-id="9876e-123">If JET_bitNormalizedKey is specified in the *grbit* parameter, this is the size of the search key provided in the input buffer.</span></span>

<span data-ttu-id="9876e-124">Si la taille des données de la colonne est égale à zéro, le contenu de la mémoire tampon d’entrée est ignoré.</span><span class="sxs-lookup"><span data-stu-id="9876e-124">If the size of the column data is zero then the contents of the input buffer are ignored.</span></span> <span data-ttu-id="9876e-125">Si JET_bitKeyDataZeroLength est spécifié dans le paramètre *Grbit* et que la colonne clé actuelle de l’index actuel du curseur est une colonne de longueur variable, les données de la colonne d’entrée sont supposées être une valeur de longueur nulle.</span><span class="sxs-lookup"><span data-stu-id="9876e-125">If JET_bitKeyDataZeroLength is specified in the *grbit* parameter and the current key column of the current index of the cursor is a variable length column, the input column data is presumed to be a zero length value.</span></span> <span data-ttu-id="9876e-126">Dans le cas contraire, les données de la colonne d’entrée sont supposées être une valeur NULL.</span><span class="sxs-lookup"><span data-stu-id="9876e-126">Otherwise, the input column data is presumed to be a NULL value.</span></span>

<span data-ttu-id="9876e-127">*grbit*</span><span class="sxs-lookup"><span data-stu-id="9876e-127">*grbit*</span></span>

<span data-ttu-id="9876e-128">Groupe de bits spécifiant zéro ou plusieurs des options suivantes.</span><span class="sxs-lookup"><span data-stu-id="9876e-128">A group of bits specifying zero or more of the following options.</span></span>

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th><p><span data-ttu-id="9876e-129">Valeur</span><span class="sxs-lookup"><span data-stu-id="9876e-129">Value</span></span></p></th>
<th><p><span data-ttu-id="9876e-130">Signification</span><span class="sxs-lookup"><span data-stu-id="9876e-130">Meaning</span></span></p></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><p><span data-ttu-id="9876e-131">JET_bitFullColumnEndLimit</span><span class="sxs-lookup"><span data-stu-id="9876e-131">JET_bitFullColumnEndLimit</span></span></p></td>
<td><p><span data-ttu-id="9876e-132">La clé de recherche doit être construite de telle sorte que toutes les colonnes clés qui viennent après la colonne clé actuelle soient considérées comme des caractères génériques.</span><span class="sxs-lookup"><span data-stu-id="9876e-132">The search key should be constructed in such a way that any key columns that come after the current key column are considered to be wildcards.</span></span> <span data-ttu-id="9876e-133">Cela signifie que la clé de recherche construite peut être utilisée pour mettre en correspondance les entrées d’index qui présentent les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9876e-133">This means that the constructed search key can be used to match index entries that have the following:</span></span></p>
<ul>
<li><p><span data-ttu-id="9876e-134">Valeurs de colonne exactes fournies pour cette colonne clé et toutes les colonnes clés précédentes.</span><span class="sxs-lookup"><span data-stu-id="9876e-134">The exact column values provided for this key column and all previous key columns.</span></span></p></li>
<li><p><span data-ttu-id="9876e-135">Toutes les valeurs de colonne nécessaires pour les colonnes clés suivantes.</span><span class="sxs-lookup"><span data-stu-id="9876e-135">Any column values needed for subsequent key columns.</span></span></p></li>
</ul>
<p><span data-ttu-id="9876e-136">Cette option doit être utilisée lors de la création de clés de recherche de caractères génériques à utiliser pour rechercher les entrées d’index les plus proches de la fin d’un index.</span><span class="sxs-lookup"><span data-stu-id="9876e-136">This option should be used when building wildcard search keys to use for finding index entries closest to the end of an index.</span></span> <span data-ttu-id="9876e-137">La fin de l’index correspond à l’entrée d’index trouvée lors du déplacement vers le dernier enregistrement de cet index.</span><span class="sxs-lookup"><span data-stu-id="9876e-137">The end of the index is the index entry that is found when moving to the last record in that index.</span></span> <span data-ttu-id="9876e-138">La fin de l’index n’est pas identique à la fin de l’index, qui peut varier en fonction de l’ordre de tri des colonnes clés dans l’index.</span><span class="sxs-lookup"><span data-stu-id="9876e-138">The end of the index is not the same as the high end of the index, which can change depending on the sort order of the key columns in the index.</span></span></p>
<p><span data-ttu-id="9876e-139">Cette option est disponible uniquement sur Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-139">This option is only available on Windows XP and later releases.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-140">JETbitFullColumnStartLimit</span><span class="sxs-lookup"><span data-stu-id="9876e-140">JETbitFullColumnStartLimit</span></span></p></td>
<td><p><span data-ttu-id="9876e-141">La clé de recherche doit être construite de telle sorte que toutes les colonnes clés qui viennent après la colonne clé actuelle soient considérées comme des caractères génériques.</span><span class="sxs-lookup"><span data-stu-id="9876e-141">The search key should be constructed such that any key columns that come after the current key column should be considered to be wildcards.</span></span> <span data-ttu-id="9876e-142">Cela signifie que la clé de recherche construite peut être utilisée pour mettre en correspondance les entrées d’index qui présentent les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9876e-142">This means that the constructed search key can be used to match index entries that have the following:</span></span></p>
<ul>
<li><p><span data-ttu-id="9876e-143">Valeurs de colonne exactes fournies pour cette colonne clé et toutes les colonnes clés précédentes.</span><span class="sxs-lookup"><span data-stu-id="9876e-143">The exact column values provided for this key column and all previous key columns.</span></span></p></li>
<li><p><span data-ttu-id="9876e-144">Toutes les valeurs de colonne nécessaires pour les colonnes clés suivantes.</span><span class="sxs-lookup"><span data-stu-id="9876e-144">Any column values needed for subsequent key columns.</span></span></p></li>
</ul>
<p><span data-ttu-id="9876e-145">Cette option doit être utilisée lors de la création de clés de recherche de caractères génériques à utiliser pour rechercher les entrées d’index les plus proches du début d’un index.</span><span class="sxs-lookup"><span data-stu-id="9876e-145">This option should be used when building wildcard search keys to use for finding index entries closest to the start of an index.</span></span> <span data-ttu-id="9876e-146">Le début de l’index correspond à l’entrée d’index trouvée lors du déplacement vers le premier enregistrement de cet index.</span><span class="sxs-lookup"><span data-stu-id="9876e-146">The start of the index is the index entry that is found when moving to the first record in that index.</span></span> <span data-ttu-id="9876e-147">Le début de l’index n’est pas le même que le bas de l’index, ce qui peut varier en fonction de l’ordre de tri des colonnes clés dans l’index.</span><span class="sxs-lookup"><span data-stu-id="9876e-147">The start of the index is not the same as the low end of the index, which can change depending on the sort order of the key columns in the index.</span></span></p>
<p><span data-ttu-id="9876e-148">Cette option est disponible uniquement sur Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-148">This option is only available on Windows XP and later releases.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-149">JET_bitKeyDataZeroLength</span><span class="sxs-lookup"><span data-stu-id="9876e-149">JET_bitKeyDataZeroLength</span></span></p></td>
<td><p><span data-ttu-id="9876e-150">Si la taille de la mémoire tampon d’entrée est égale à zéro et que la colonne clé actuelle est une colonne de longueur variable, cette option indique que la mémoire tampon d’entrée contient une valeur de longueur égale à zéro.</span><span class="sxs-lookup"><span data-stu-id="9876e-150">If the size of the input buffer is zero and the current key column is a variable length column, this option indicates that the input buffer contains a zero length value.</span></span> <span data-ttu-id="9876e-151">Dans le cas contraire, une taille de mémoire tampon d’entrée égale à zéro indiquerait une valeur NULL.</span><span class="sxs-lookup"><span data-stu-id="9876e-151">Otherwise, an input buffer size of zero would indicate a NULL value.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-152">JET_bitNewKey</span><span class="sxs-lookup"><span data-stu-id="9876e-152">JET_bitNewKey</span></span></p></td>
<td><p><span data-ttu-id="9876e-153">Une nouvelle clé de recherche doit être construite.</span><span class="sxs-lookup"><span data-stu-id="9876e-153">A new search key should be constructed.</span></span> <span data-ttu-id="9876e-154">Toute clé de recherche existante est ignorée.</span><span class="sxs-lookup"><span data-stu-id="9876e-154">Any previously existing search key is discarded.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-155">JET_bitNormalizedKey</span><span class="sxs-lookup"><span data-stu-id="9876e-155">JET_bitNormalizedKey</span></span></p></td>
<td><p><span data-ttu-id="9876e-156">Quand cette option est spécifiée, toutes les autres options sont ignorées, toute clé de recherche précédemment existante est ignorée et le contenu de la mémoire tampon d’entrée est chargé en tant que nouvelle clé de recherche.</span><span class="sxs-lookup"><span data-stu-id="9876e-156">When this option is specified, all other options are ignored, any previously existing search key is discarded, and the contents of the input buffer are loaded as the new search key.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-157">JET_bitPartialColumnEndLimit</span><span class="sxs-lookup"><span data-stu-id="9876e-157">JET_bitPartialColumnEndLimit</span></span></p></td>
<td><p><span data-ttu-id="9876e-158">La clé de recherche doit être construite de telle sorte que la colonne clé actuelle soit considérée comme un caractère générique de préfixe et que toutes les colonnes clés qui viennent après la colonne clé actuelle soient considérées comme des caractères génériques.</span><span class="sxs-lookup"><span data-stu-id="9876e-158">The search key should be constructed such that the current key column is considered to be a prefix wildcard and that any key columns that come after the current key column should be considered to be wildcards.</span></span> <span data-ttu-id="9876e-159">Cela signifie que la clé de recherche construite peut être utilisée pour mettre en correspondance les entrées d’index qui présentent les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9876e-159">This means that the constructed search key can be used to match index entries that have the following:</span></span></p>
<ul>
<li><p><span data-ttu-id="9876e-160">Valeurs de colonne exactes fournies pour cette colonne clé et toutes les colonnes clés précédentes.</span><span class="sxs-lookup"><span data-stu-id="9876e-160">The exact column values provided for this key column and all previous key columns.</span></span></p></li>
<li><p><span data-ttu-id="9876e-161">Toutes les valeurs de colonne nécessaires pour les colonnes clés suivantes.</span><span class="sxs-lookup"><span data-stu-id="9876e-161">Any column values needed for subsequent key columns.</span></span></p></li>
</ul>
<p><span data-ttu-id="9876e-162">Cette option doit être utilisée lors de la création de clés de recherche de caractères génériques à utiliser pour rechercher les entrées d’index les plus proches de la fin d’un index.</span><span class="sxs-lookup"><span data-stu-id="9876e-162">This option should be used when building wildcard search keys to use for finding index entries closest to the end of an index.</span></span> <span data-ttu-id="9876e-163">La fin de l’index correspond à l’entrée d’index trouvée lors du déplacement vers le dernier enregistrement de cet index.</span><span class="sxs-lookup"><span data-stu-id="9876e-163">The end of the index is the index entry that is found when moving to the last record in that index.</span></span> <span data-ttu-id="9876e-164">La fin de l’index n’est pas identique à la fin de l’index, qui peut varier en fonction de l’ordre de tri des colonnes clés dans l’index.</span><span class="sxs-lookup"><span data-stu-id="9876e-164">The end of the index is not the same as the high end of the index, which can change depending on the sort order of the key columns in the index.</span></span></p>
<p><span data-ttu-id="9876e-165">Cette option ne peut pas être utilisée lorsque la colonne clé actuelle n’est pas une colonne de texte ou une colonne binaire de variable.</span><span class="sxs-lookup"><span data-stu-id="9876e-165">This option cannot be used when the current key column is not a text column or a variable binary column.</span></span> <span data-ttu-id="9876e-166">L’opération échouera avec JET_errInvalidgrbit si cette tentative est effectuée.</span><span class="sxs-lookup"><span data-stu-id="9876e-166">The operation will fail with JET_errInvalidgrbit if this is attempted.</span></span></p>
<p><span data-ttu-id="9876e-167">Cette option est disponible uniquement sur Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-167">This option is only available on Windows XP and later releases.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-168">JET_bitPartialColumnStartLimit</span><span class="sxs-lookup"><span data-stu-id="9876e-168">JET_bitPartialColumnStartLimit</span></span></p></td>
<td><p><span data-ttu-id="9876e-169">Cette option indique que la clé de recherche doit être construite de manière à ce que la colonne clé actuelle soit considérée comme un caractère générique de préfixe et que toutes les colonnes clés qui viennent après la colonne clé actuelle soient considérées comme des caractères génériques.</span><span class="sxs-lookup"><span data-stu-id="9876e-169">This option indicates that the search key should be constructed such that the current key column is considered to be a prefix wildcard and that any key columns that come after the current key column should be considered to be wildcards.</span></span> <span data-ttu-id="9876e-170">Cela signifie que la clé de recherche construite peut être utilisée pour mettre en correspondance les entrées d’index qui présentent les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9876e-170">This means that the constructed search key can be used to match index entries that have the following:</span></span></p>
<ul>
<li><p><span data-ttu-id="9876e-171">Valeurs de colonne exactes fournies pour cette colonne clé et toutes les colonnes clés précédentes.</span><span class="sxs-lookup"><span data-stu-id="9876e-171">The exact column values provided for this key column and all previous key columns.</span></span></p></li>
<li><p><span data-ttu-id="9876e-172">Toutes les valeurs de colonne nécessaires pour les colonnes clés suivantes.</span><span class="sxs-lookup"><span data-stu-id="9876e-172">Any column values needed for subsequent key columns.</span></span></p></li>
</ul>
<p><span data-ttu-id="9876e-173">Cette option doit être utilisée lors de la création de clés de recherche de caractères génériques à utiliser pour rechercher les entrées d’index les plus proches du début d’un index.</span><span class="sxs-lookup"><span data-stu-id="9876e-173">This option should be used when building wildcard search keys to use for finding index entries closest to the start of an index.</span></span> <span data-ttu-id="9876e-174">Le début de l’index correspond à l’entrée d’index trouvée lors du déplacement vers le premier enregistrement de cet index.</span><span class="sxs-lookup"><span data-stu-id="9876e-174">The start of the index is the index entry that is found when moving to the first record in that index.</span></span> <span data-ttu-id="9876e-175">Le début de l’index n’est pas le même que le bas de l’index, ce qui peut varier en fonction de l’ordre de tri des colonnes clés dans l’index.</span><span class="sxs-lookup"><span data-stu-id="9876e-175">The start of the index is not the same as the low end of the index, which can change depending on the sort order of the key columns in the index.</span></span></p>
<p><span data-ttu-id="9876e-176">Cette option ne peut pas être utilisée lorsque la colonne clé actuelle n’est pas une colonne de texte ou une colonne binaire de variable.</span><span class="sxs-lookup"><span data-stu-id="9876e-176">This option cannot be used when the current key column is not a text column or a variable binary column.</span></span> <span data-ttu-id="9876e-177">L’opération échouera avec JET_errInvalidgrbit si cette tentative est effectuée.</span><span class="sxs-lookup"><span data-stu-id="9876e-177">The operation will fail with JET_errInvalidgrbit if this is attempted.</span></span></p>
<p><span data-ttu-id="9876e-178">Cette option est disponible uniquement sur Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-178">This option is only available on Windows XP and later releases.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-179">JET_bitStrLimit</span><span class="sxs-lookup"><span data-stu-id="9876e-179">JET_bitStrLimit</span></span></p></td>
<td><p><span data-ttu-id="9876e-180">Cette option indique que la clé de recherche doit être construite de telle sorte que toutes les colonnes clés qui viennent après la colonne clé actuelle soient considérées comme des caractères génériques.</span><span class="sxs-lookup"><span data-stu-id="9876e-180">This option indicates that the search key should be constructed such that any key columns that come after the current key column should be considered to be wildcards.</span></span> <span data-ttu-id="9876e-181">Cela signifie que la clé de recherche construite peut être utilisée pour mettre en correspondance les entrées d’index qui présentent les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9876e-181">This means that the constructed search key can be used to match index entries that have the following:</span></span></p>
<ul>
<li><p><span data-ttu-id="9876e-182">Valeurs de colonne exactes fournies pour cette colonne clé et toutes les colonnes clés précédentes.</span><span class="sxs-lookup"><span data-stu-id="9876e-182">The exact column values provided for this key column and all previous key columns.</span></span></p></li>
<li><p><span data-ttu-id="9876e-183">Toutes les valeurs de colonne nécessaires pour les colonnes clés suivantes.</span><span class="sxs-lookup"><span data-stu-id="9876e-183">Any column values needed for subsequent key columns.</span></span></p></li>
</ul>
<p><span data-ttu-id="9876e-184">Cette option doit être utilisée lors de la création de clés de recherche de caractères génériques à utiliser pour rechercher les entrées d’index les plus proches de la fin d’un index.</span><span class="sxs-lookup"><span data-stu-id="9876e-184">This option should be used when building wildcard search keys to use for finding index entries closest to the end of an index.</span></span> <span data-ttu-id="9876e-185">La fin de l’index correspond à l’entrée d’index trouvée lors du déplacement vers le dernier enregistrement de cet index.</span><span class="sxs-lookup"><span data-stu-id="9876e-185">The end of the index is the index entry that is found when moving to the last record in that index.</span></span> <span data-ttu-id="9876e-186">La fin de l’index n’est pas identique à la fin de l’index, qui peut varier en fonction de l’ordre de tri des colonnes clés dans l’index.</span><span class="sxs-lookup"><span data-stu-id="9876e-186">The end of the index is not the same as the high end of the index, which can change depending on the sort order of the key columns in the index.</span></span></p>
<p><span data-ttu-id="9876e-187">Quand cette option est spécifiée en combinaison avec JET_bitSubStrLimit et que la colonne clé actuelle est une colonne de texte, cette option est ignorée.</span><span class="sxs-lookup"><span data-stu-id="9876e-187">When this option is specified in combination with JET_bitSubStrLimit and the current key column is a text column, this option will be ignored.</span></span> <span data-ttu-id="9876e-188">Ce comportement est destiné à permettre au type de la colonne clé actuelle d’être déduit lors de la génération de la clé de recherche.</span><span class="sxs-lookup"><span data-stu-id="9876e-188">This behavior is intended to allow the type of the current key column to be inferred when building the search key.</span></span></p>
<p><span data-ttu-id="9876e-189">Si vous souhaitez créer une clé de recherche similaire pour le début d’un index, un appel similaire à <strong>JetMakeKey</strong> doit être effectué pour la dernière colonne clé qui n’est pas un caractère générique, mais sans options génériques spécifiées.</span><span class="sxs-lookup"><span data-stu-id="9876e-189">If you want to build a similar search key for the start of an index, a similar call to <strong>JetMakeKey</strong> should be made for the last key column that is not a wildcard, but with no wildcard options specified.</span></span> <span data-ttu-id="9876e-190">La clé de recherche est alors dans un état approprié à utiliser pour une telle recherche.</span><span class="sxs-lookup"><span data-stu-id="9876e-190">The search key is then in an appropriate state to use for such a search.</span></span> <span data-ttu-id="9876e-191">Cela est similaire à l’utilisation de JET_bitFullColumnStartLimit, à la différence que la clé de recherche n’est pas correctement terminée, car elle est effectuée après l’utilisation d’une option de caractère générique.</span><span class="sxs-lookup"><span data-stu-id="9876e-191">This is analogous to using JET_bitFullColumnStartLimit, except that the search key is not cleanly finished as it is after the use of a wildcard option.</span></span></p>
<p><span data-ttu-id="9876e-192">Cette option est dépréciée pour Windows XP et les versions ultérieures afin de répondre à cette sémantique maladroite.</span><span class="sxs-lookup"><span data-stu-id="9876e-192">This option has been deprecated for Windows XP and later releases in order to address this awkward semantic.</span></span> <span data-ttu-id="9876e-193">JET_bitFullColumnStartLimit et JET_bitFullColumnEndLimit doivent être utilisés à la place, dans la mesure du possible.</span><span class="sxs-lookup"><span data-stu-id="9876e-193">JET_bitFullColumnStartLimit and JET_bitFullColumnEndLimit should be used instead wherever possible.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-194">JET_bitSubStrLimit</span><span class="sxs-lookup"><span data-stu-id="9876e-194">JET_bitSubStrLimit</span></span></p></td>
<td><p><span data-ttu-id="9876e-195">Cette option indique que la clé de recherche doit être construite de manière à ce que la colonne clé actuelle soit considérée comme un caractère générique de préfixe et que toutes les colonnes clés qui viennent après la colonne clé actuelle soient considérées comme des caractères génériques.</span><span class="sxs-lookup"><span data-stu-id="9876e-195">This option indicates that the search key should be constructed such that the current key column is considered to be a prefix wildcard and that any key columns that come after the current key column should be considered to be wildcards.</span></span> <span data-ttu-id="9876e-196">Cela signifie que la clé de recherche construite peut être utilisée pour mettre en correspondance les entrées d’index qui présentent les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9876e-196">This means that the constructed search key can be used to match index entries that have the following:</span></span></p>
<ul>
<li><p><span data-ttu-id="9876e-197">Valeurs de colonne exactes fournies pour toutes les colonnes clés précédentes.</span><span class="sxs-lookup"><span data-stu-id="9876e-197">The exact column values provided for all previous key columns.</span></span></p></li>
<li><p><span data-ttu-id="9876e-198">Données de la colonne spécifiée en tant que préfixe de la valeur de leur colonne pour la colonne clé actuelle.</span><span class="sxs-lookup"><span data-stu-id="9876e-198">The specified column data as a prefix of their column value for the current key column.</span></span></p></li>
<li><p><span data-ttu-id="9876e-199">Toutes les valeurs de colonne pour les colonnes clés suivantes.</span><span class="sxs-lookup"><span data-stu-id="9876e-199">Any column values for subsequent key columns.</span></span></p></li>
</ul>
<p><span data-ttu-id="9876e-200">Cette option doit être utilisée lors de la création de clés de recherche de caractères génériques à utiliser pour rechercher les entrées d’index les plus proches de la fin d’un index.</span><span class="sxs-lookup"><span data-stu-id="9876e-200">This option should be used when building wildcard search keys to use for finding index entries closest to the end of an index.</span></span> <span data-ttu-id="9876e-201">La fin de l’index correspond à l’entrée d’index trouvée lors du déplacement vers le dernier enregistrement de cet index.</span><span class="sxs-lookup"><span data-stu-id="9876e-201">The end of the index is the index entry that is found when moving to the last record in that index.</span></span> <span data-ttu-id="9876e-202">La fin de l’index n’est pas identique à la fin de l’index, qui peut varier en fonction de l’ordre de tri des colonnes clés dans l’index.</span><span class="sxs-lookup"><span data-stu-id="9876e-202">The end of the index is not the same as the high end of the index, which can change depending on the sort order of the key columns in the index.</span></span></p>
<p><span data-ttu-id="9876e-203">Quand cette option est spécifiée en combinaison avec JET_bitStrLimit et que la colonne clé actuelle est une colonne de texte, cette option est prioritaire.</span><span class="sxs-lookup"><span data-stu-id="9876e-203">When this option is specified in combination with JET_bitStrLimit and the current key column is a text column, this option will take precedence.</span></span> <span data-ttu-id="9876e-204">Cette option est ignorée lorsque la colonne clé actuelle n’est pas une colonne de texte.</span><span class="sxs-lookup"><span data-stu-id="9876e-204">This option is ignored when the current key column is not a text column.</span></span> <span data-ttu-id="9876e-205">Ce comportement est destiné à permettre au type de la colonne clé actuelle d’être déduit lors de la génération de la clé de recherche.</span><span class="sxs-lookup"><span data-stu-id="9876e-205">This behavior is intended to allow the type of the current key column to be inferred when building the search key.</span></span></p>
<p><span data-ttu-id="9876e-206">Si vous souhaitez créer une clé de recherche similaire pour le début d’un index, un appel similaire à <strong>JetMakeKey</strong> doit être effectué pour la colonne clé qui doit être le caractère générique de préfixe, mais sans aucune option générique spécifiée.</span><span class="sxs-lookup"><span data-stu-id="9876e-206">If you want to build a similar search key for the start of an index, a similar call to <strong>JetMakeKey</strong> should be made for the key column that is to be the prefix wildcard, but with that no wildcard options specified.</span></span> <span data-ttu-id="9876e-207">La clé de recherche est alors dans un état approprié à utiliser pour une telle recherche.</span><span class="sxs-lookup"><span data-stu-id="9876e-207">The search key is then in an appropriate state to use for such a search.</span></span> <span data-ttu-id="9876e-208">Cela est similaire à l’utilisation de JET_bitPartialColumnStartLimit, à la différence que la clé de recherche n’est pas correctement terminée, car elle est effectuée après l’utilisation d’une option de caractère générique.</span><span class="sxs-lookup"><span data-stu-id="9876e-208">This is analogous to using JET_bitPartialColumnStartLimit, except that the search key is not cleanly finished as it is after the use of a wildcard option.</span></span></p>
<p><span data-ttu-id="9876e-209">Cette option est dépréciée pour Windows XP et les versions ultérieures afin de répondre à cette sémantique maladroite.</span><span class="sxs-lookup"><span data-stu-id="9876e-209">This option has been deprecated for Windows XP and later releases in order to address this awkward semantic.</span></span> <span data-ttu-id="9876e-210">JET_bitPartialColumnStartLimit et JET_bitPartialColumnEndLimit doivent être utilisés à la place, lorsque cela est possible.</span><span class="sxs-lookup"><span data-stu-id="9876e-210">JET_bitPartialColumnStartLimit and JET_bitPartialColumnEndLimit should be used instead, when possible.</span></span></p></td>
</tr>
</tbody>
</table>


### <a name="return-value"></a><span data-ttu-id="9876e-211">Valeur renvoyée</span><span class="sxs-lookup"><span data-stu-id="9876e-211">Return Value</span></span>

<span data-ttu-id="9876e-212">Cette fonction retourne le type de données [JET_ERR](./jet-err.md) avec l’un des codes de retour suivants.</span><span class="sxs-lookup"><span data-stu-id="9876e-212">This function returns the [JET_ERR](./jet-err.md) datatype with one of the following return codes.</span></span> <span data-ttu-id="9876e-213">Pour plus d’informations sur les erreurs ESE possibles, consultez [Erreurs du moteur de stockage extensible](./extensible-storage-engine-errors.md) et [paramètres de gestion des erreurs](./error-handling-parameters.md).</span><span class="sxs-lookup"><span data-stu-id="9876e-213">For more information about the possible ESE errors, see [Extensible Storage Engine Errors](./extensible-storage-engine-errors.md) and [Error Handling Parameters](./error-handling-parameters.md).</span></span>

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th><p><span data-ttu-id="9876e-214">Code de retour</span><span class="sxs-lookup"><span data-stu-id="9876e-214">Return code</span></span></p></th>
<th><p><span data-ttu-id="9876e-215">Description</span><span class="sxs-lookup"><span data-stu-id="9876e-215">Description</span></span></p></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><p><span data-ttu-id="9876e-216">JET_errSuccess</span><span class="sxs-lookup"><span data-stu-id="9876e-216">JET_errSuccess</span></span></p></td>
<td><p><span data-ttu-id="9876e-217">L’opération s’est terminée avec succès.</span><span class="sxs-lookup"><span data-stu-id="9876e-217">The operation completed successfully.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-218">JET_errClientRequestToStopJetService</span><span class="sxs-lookup"><span data-stu-id="9876e-218">JET_errClientRequestToStopJetService</span></span></p></td>
<td><p><span data-ttu-id="9876e-219">Il n’est pas possible de terminer l’opération, car toute activité sur l’instance associée à la session a été interrompue suite à un appel à <a href="gg269240(v=exchg.10).md">JetStopService</a>.</span><span class="sxs-lookup"><span data-stu-id="9876e-219">It is not possible to complete the operation because all activity on the instance associated with the session has ceased as a result of a call to <a href="gg269240(v=exchg.10).md">JetStopService</a>.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-220">JET_errIndexTuplesKeyTooSmall</span><span class="sxs-lookup"><span data-stu-id="9876e-220">JET_errIndexTuplesKeyTooSmall</span></span></p></td>
<td><p><span data-ttu-id="9876e-221">Les données de la colonne fournie sont insuffisantes pour créer une clé valide pour l’index actuel, car cet index est un index de tuple et la taille minimale du tuple était supérieure aux données de la colonne fournie.</span><span class="sxs-lookup"><span data-stu-id="9876e-221">The provided column data was too small to build a valid key for the current index because that index is a tuple index and the minimum tuple size was larger than the provided column data.</span></span> <span data-ttu-id="9876e-222">Pour plus d’informations sur les index de tuple, consultez <a href="gg294099(v=exchg.10).md">JetCreateIndex</a> .</span><span class="sxs-lookup"><span data-stu-id="9876e-222">See <a href="gg294099(v=exchg.10).md">JetCreateIndex</a> for more information on tuple indexes.</span></span> <span data-ttu-id="9876e-223">Cette erreur est renvoyée uniquement par Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-223">This error will only be returned by Windows XP and later releases.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-224">JET_errInstanceUnavailable</span><span class="sxs-lookup"><span data-stu-id="9876e-224">JET_errInstanceUnavailable</span></span></p></td>
<td><p><span data-ttu-id="9876e-225">Il n’est pas possible de terminer l’opération, car l’instance associée à la session a rencontré une erreur irrécupérable qui requiert que l’accès à toutes les données soit révoqué pour protéger l’intégrité de ces données.</span><span class="sxs-lookup"><span data-stu-id="9876e-225">It is not possible to complete the operation because the instance associated with the session has encountered a fatal error that requires that access to all data be revoked to protect the integrity of that data.</span></span> <span data-ttu-id="9876e-226">Cette erreur est renvoyée uniquement par Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-226">This error will only be returned by Windows XP and later releases.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-227">JET_errInvalidBufferSize</span><span class="sxs-lookup"><span data-stu-id="9876e-227">JET_errInvalidBufferSize</span></span></p></td>
<td><p><span data-ttu-id="9876e-228">Les données de colonne fournies ne correspondent pas à la taille requise par la définition de colonne.</span><span class="sxs-lookup"><span data-stu-id="9876e-228">The column data provided did not match the size required by the column definition.</span></span> <span data-ttu-id="9876e-229">Cela peut se produire si le type de données de la colonne est intrinsèquement une certaine taille.</span><span class="sxs-lookup"><span data-stu-id="9876e-229">This can happen if the column's data type is intrinsically a certain size.</span></span> <span data-ttu-id="9876e-230">Cela peut également se produire si le type de données de la colonne n’est pas intrinsèquement une certaine taille, mais que la définition de la colonne le déclare comme étant de longueur fixe.</span><span class="sxs-lookup"><span data-stu-id="9876e-230">This can also happen if the column's data type is not intrinsically a certain size but the column's definition declares it to be of fixed length.</span></span> <span data-ttu-id="9876e-231">La seule exception à cela est que cette erreur ne se produira pas quand un trop petit nombre de données est fourni pour une colonne de texte de longueur fixe parce que les données manquantes sont automatiquement complétées par des espaces.</span><span class="sxs-lookup"><span data-stu-id="9876e-231">One exception to this is that this error will not occur when too little data is provided for a fixed length text column because any missing data will be automatically padded with spaces.</span></span> <span data-ttu-id="9876e-232">Une deuxième exception à cela est que cette erreur ne se produit pas quand un trop petit nombre de données est fourni pour une colonne binaire de longueur fixe, car les données manquantes sont automatiquement complétées par des zéros.</span><span class="sxs-lookup"><span data-stu-id="9876e-232">A second exception to this is that this error will not occur when too little data is provided for a fixed length binary column because any missing data will be automatically padded with zeroes.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-233">JET_errInvalidgrbit</span><span class="sxs-lookup"><span data-stu-id="9876e-233">JET_errInvalidgrbit</span></span></p></td>
<td><p><span data-ttu-id="9876e-234">L’une des options demandées n’est pas valide, utilisée de manière non conforme ou non implémentée.</span><span class="sxs-lookup"><span data-stu-id="9876e-234">One of the options requested was invalid, used in an illegal manner, or not implemented.</span></span> <span data-ttu-id="9876e-235">Cela peut se produire pour <strong>JetMakeKey</strong> dans les cas suivants :</span><span class="sxs-lookup"><span data-stu-id="9876e-235">This can happen for <strong>JetMakeKey</strong> when:</span></span></p>
<ul>
<li><p><span data-ttu-id="9876e-236">JET_bitPartialColumnStartLimit ou JET_bitPartialColumnEndLimit sont spécifiés et la colonne clé correspondante n’est pas une colonne de texte et n’est pas une colonne binaire de longueur variable.</span><span class="sxs-lookup"><span data-stu-id="9876e-236">JET_bitPartialColumnStartLimit or JET_bitPartialColumnEndLimit are specified and the corresponding key column is not a text column and is not a variable length binary column.</span></span> <span data-ttu-id="9876e-237">Ce cas se produit uniquement sur Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-237">This case only occurs on Windows XP and later releases.</span></span></p></li>
<li><p><span data-ttu-id="9876e-238">Une tentative d’utilisation conjointe de plusieurs des options suivantes est effectuée : JET_bitPartialColumnStartLimit, JET_bitPartialColumnEndLimit, JET_bitFullColumnStartLimit et JET_bitFullColumnEndLimit.</span><span class="sxs-lookup"><span data-stu-id="9876e-238">An attempt is made to use more than one of the following options together: JET_bitPartialColumnStartLimit, JET_bitPartialColumnEndLimit, JET_bitFullColumnStartLimit, and JET_bitFullColumnEndLimit.</span></span> <span data-ttu-id="9876e-239">Ce cas se produit uniquement sur Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-239">This case only occurs on Windows XP and later releases.</span></span></p></li>
<li><p><span data-ttu-id="9876e-240">Une tentative est faite pour utiliser JET_bitStrLimit ou JET_bitSubStrLimit lorsque l’une des options suivantes est utilisée : JET_bitPartialColumnStartLimit, JET_bitPartialColumnEndLimit, JET_bitFullColumnStartLimit et JET_bitFullColumnEndLimit.</span><span class="sxs-lookup"><span data-stu-id="9876e-240">An attempt is made to use JET_bitStrLimit or JET_bitSubStrLimit when one of the following options is used: JET_bitPartialColumnStartLimit, JET_bitPartialColumnEndLimit, JET_bitFullColumnStartLimit and JET_bitFullColumnEndLimit.</span></span> <span data-ttu-id="9876e-241">Ce cas se produit uniquement sur Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-241">This case only occurs on Windows XP and later releases.</span></span></p></li>
</ul></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-242">JET_errInvalidParameter</span><span class="sxs-lookup"><span data-stu-id="9876e-242">JET_errInvalidParameter</span></span></p></td>
<td><p><span data-ttu-id="9876e-243">L’un des paramètres fournis contenait une valeur inattendue ou contenait une valeur qui n’a pas de sens lorsqu’elle était associée à la valeur d’un autre paramètre.</span><span class="sxs-lookup"><span data-stu-id="9876e-243">One of the parameters provided contained an unexpected value or contained a value that did not make sense when combined with the value of another parameter.</span></span></p>
<p><span data-ttu-id="9876e-244">Cela peut se produire pour <strong>JetMakeKey</strong> lorsque JET_bitNormalizedKey a été spécifié et que la valeur fournie dans la mémoire tampon d’entrée est trop grande pour être une clé de recherche valide.</span><span class="sxs-lookup"><span data-stu-id="9876e-244">This can happen for <strong>JetMakeKey</strong> when JET_bitNormalizedKey was specified and the value provided in the input buffer was too large to be a valid search key.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-245">JET_errKeyIsMade</span><span class="sxs-lookup"><span data-stu-id="9876e-245">JET_errKeyIsMade</span></span></p></td>
<td><p><span data-ttu-id="9876e-246">Les données de colonne fournies à <strong>JetMakeKey</strong> ont été rejetées, car des données de colonne ont déjà été fournies pour toutes les colonnes clés de l’index actuel.</span><span class="sxs-lookup"><span data-stu-id="9876e-246">The column data provided to <strong>JetMakeKey</strong> was rejected because column data has already been provided for all the key columns in the current index.</span></span> <span data-ttu-id="9876e-247">Cela peut se produire de trois manières.</span><span class="sxs-lookup"><span data-stu-id="9876e-247">This can happen in three ways.</span></span> <span data-ttu-id="9876e-248">La première consiste à faire en sorte que les données de colonne soient fournies pour chaque colonne clé de l’index actuel.</span><span class="sxs-lookup"><span data-stu-id="9876e-248">The first way is if column data is provided for each key column in the current index.</span></span> <span data-ttu-id="9876e-249">La seconde méthode est si des données de colonne ont été fournies pour au moins une colonne clé et qu’une option de caractère générique a été choisie pour le dernier appel.</span><span class="sxs-lookup"><span data-stu-id="9876e-249">The second way is if column data has been provided for at least one key column and a wildcard option was chosen for the last call.</span></span> <span data-ttu-id="9876e-250">La troisième méthode consiste à fournir une clé de recherche précédemment construite à l’aide de JET_bitNormalizedKey, qui couvre toutes les colonnes clés.</span><span class="sxs-lookup"><span data-stu-id="9876e-250">The third way is if a previously constructed search key was provided using JET_bitNormalizedKey, which covers all key columns.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-251">JET_errKeyNotMade</span><span class="sxs-lookup"><span data-stu-id="9876e-251">JET_errKeyNotMade</span></span></p></td>
<td><p><span data-ttu-id="9876e-252">Il n’existe pas de clé de recherche pour le curseur.</span><span class="sxs-lookup"><span data-stu-id="9876e-252">There is no current search key for the cursor.</span></span> <span data-ttu-id="9876e-253">Cela se produit pour <strong>JetMakeKey</strong> si JET_bitNewKey n’est pas spécifié et qu’une clé de recherche n’a pas été construite pour ce curseur à l’aide d’un appel antérieur à <strong>JetMakeKey</strong>.</span><span class="sxs-lookup"><span data-stu-id="9876e-253">This will happen for <strong>JetMakeKey</strong> if JET_bitNewKey is not specified and a search key has not been constructed for this cursor using a prior call to <strong>JetMakeKey</strong>.</span></span> <span data-ttu-id="9876e-254">La clé de recherche sera supprimée par un appel précédent à n’importe quelle API de navigation sur le curseur autre que <a href="gg294117(v=exchg.10).md">JetMove</a>.</span><span class="sxs-lookup"><span data-stu-id="9876e-254">The search key will be deleted by a prior call to any navigational API on the cursor other than <a href="gg294117(v=exchg.10).md">JetMove</a>.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-255">JET_errNoCurrentIndex</span><span class="sxs-lookup"><span data-stu-id="9876e-255">JET_errNoCurrentIndex</span></span></p></td>
<td><p><span data-ttu-id="9876e-256">Il n’y a pas d’index actuel pour le curseur.</span><span class="sxs-lookup"><span data-stu-id="9876e-256">There is no current index for the cursor.</span></span> <span data-ttu-id="9876e-257">Cela se produit pour <strong>JetMakeKey</strong> si le curseur se trouve sur l’index cluster d’une table, qu’un index primaire n’a pas été défini et que JET_bitNormalizedKey n’a pas été spécifié.</span><span class="sxs-lookup"><span data-stu-id="9876e-257">This will happen for <strong>JetMakeKey</strong> if the cursor is on the clustered index of a table, a primary index has not been defined, and JET_bitNormalizedKey was not specified.</span></span> <span data-ttu-id="9876e-258">Il n’est pas possible de construire une clé de recherche si le curseur se trouve sur un index qui n’a pas de colonnes clés, sauf si une clé de recherche précédemment construite est fournie.</span><span class="sxs-lookup"><span data-stu-id="9876e-258">It is not possible to construct a search key if the cursor is on an index that does not have any key columns unless a previously constructed search key is provided.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-259">JET_errNotInitialized</span><span class="sxs-lookup"><span data-stu-id="9876e-259">JET_errNotInitialized</span></span></p></td>
<td><p><span data-ttu-id="9876e-260">Impossible de terminer l’opération, car l’instance associée à la session n’a pas encore été initialisée.</span><span class="sxs-lookup"><span data-stu-id="9876e-260">It is not possible to complete the operation because the instance associated with the session has not been initialized yet.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-261">JET_errRestoreInProgress</span><span class="sxs-lookup"><span data-stu-id="9876e-261">JET_errRestoreInProgress</span></span></p></td>
<td><p><span data-ttu-id="9876e-262">Il n’est pas possible de terminer l’opération, car une opération de restauration est en cours sur l’instance associée à la session.</span><span class="sxs-lookup"><span data-stu-id="9876e-262">It is not possible to complete the operation because a restore operation is in progress on the instance associated with the session.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-263">JET_errSessionSharingViolation</span><span class="sxs-lookup"><span data-stu-id="9876e-263">JET_errSessionSharingViolation</span></span></p></td>
<td><p><span data-ttu-id="9876e-264">La même session ne peut pas être utilisée simultanément pour plusieurs threads.</span><span class="sxs-lookup"><span data-stu-id="9876e-264">The same session cannot be used for more than one thread at the same time.</span></span> <span data-ttu-id="9876e-265">Cette erreur est renvoyée uniquement par Windows XP et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-265">This error will only be returned by Windows XP and later releases.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-266">JET_errTermInProgress</span><span class="sxs-lookup"><span data-stu-id="9876e-266">JET_errTermInProgress</span></span></p></td>
<td><p><span data-ttu-id="9876e-267">Il n’est pas possible de terminer l’opération, car l’instance associée à la session est en cours d’arrêt.</span><span class="sxs-lookup"><span data-stu-id="9876e-267">It is not possible to complete the operation because the instance associated with the session is being shut down.</span></span></p></td>
</tr>
</tbody>
</table>


<span data-ttu-id="9876e-268">En cas de réussite, si JET_bitNormalizedKey et JET_bitNewKey n’ont pas été spécifiés, la clé de recherche est générée par les critères de recherche pour une colonne clé supplémentaire dans l’index actuel.</span><span class="sxs-lookup"><span data-stu-id="9876e-268">On success, if JET_bitNormalizedKey and JET_bitNewKey were not specified, the search key will have been built up by the search criteria for one more key column in the current index.</span></span> <span data-ttu-id="9876e-269">Si JET_bitNormalizedKey n’a pas été spécifié et que JET_bitNewKey a été spécifié, toute clé de recherche précédemment existante était ignorée et une nouvelle a été créée par les critères de recherche de la première colonne clé de l’index actuel.</span><span class="sxs-lookup"><span data-stu-id="9876e-269">If JET_bitNormalizedKey was not specified and JET_bitNewKey was specified, any previously existing search key was discarded and a new one will have been built up by the search criteria for the first key column in the current index.</span></span> <span data-ttu-id="9876e-270">Si JET_bitNormalizedKey a été spécifié, toute clé de recherche précédemment existante était ignorée et une nouvelle est chargée à partir de la mémoire tampon d’entrée.</span><span class="sxs-lookup"><span data-stu-id="9876e-270">If JET_bitNormalizedKey was specified, any previously existing search key was discarded and a new one loaded from the input buffer.</span></span> <span data-ttu-id="9876e-271">Dans tous les cas, aucune modification de l’état de la base de données ne se produit.</span><span class="sxs-lookup"><span data-stu-id="9876e-271">In any case, no change to the database state will occur.</span></span>

<span data-ttu-id="9876e-272">En cas d’échec, si JET_bitNormalizedKey ou JET_bitNewKey a été spécifié, l’état de la clé de recherche n’est pas défini.</span><span class="sxs-lookup"><span data-stu-id="9876e-272">On failure, if JET_bitNormalizedKey or JET_bitNewKey was specified, the state of the search key is undefined.</span></span> <span data-ttu-id="9876e-273">Si ni JET_bitNormalizedKey ni JET_bitNewKey n’ont été spécifiés, aucune modification de l’état de la clé de recherche ne se produit.</span><span class="sxs-lookup"><span data-stu-id="9876e-273">If neither JET_bitNormalizedKey nor JET_bitNewKey were specified, no change to the state of the search key will occur.</span></span> <span data-ttu-id="9876e-274">Dans tous les cas, aucune modification de l’état de la base de données ne se produit.</span><span class="sxs-lookup"><span data-stu-id="9876e-274">In any case, no change to the database state will occur.</span></span>

#### <a name="remarks"></a><span data-ttu-id="9876e-275">Notes</span><span class="sxs-lookup"><span data-stu-id="9876e-275">Remarks</span></span>

<span data-ttu-id="9876e-276">Les clés doivent être traitées comme des blocs de données opaques.</span><span class="sxs-lookup"><span data-stu-id="9876e-276">Keys should be treated as opaque chunks of data.</span></span> <span data-ttu-id="9876e-277">Aucune tentative n’est faite pour exploiter la structure interne de ces données.</span><span class="sxs-lookup"><span data-stu-id="9876e-277">No attempt should be made to exploit the internal structure of this data.</span></span> <span data-ttu-id="9876e-278">Toutefois, les éléments suivants sont connus de toutes les clés ESENT :</span><span class="sxs-lookup"><span data-stu-id="9876e-278">However, the following is known about all ESENT keys:</span></span>

  - <span data-ttu-id="9876e-279">Les clés peuvent être comparées les unes aux autres à l’aide de [memcmp](/previous-versions/visualstudio/visual-studio-6.0/aa246467(v=vs.60)) pour établir leur ordre relatif dans l’index d’origine sur la table des entrées d’index source.</span><span class="sxs-lookup"><span data-stu-id="9876e-279">Keys may be compared against each other using [memcmp](/previous-versions/visualstudio/visual-studio-6.0/aa246467(v=vs.60)) to establish their relative ordering in the originating index over the table of the source index entries.</span></span>

  - <span data-ttu-id="9876e-280">Il est inutile de comparer les clés des entrées d’index de différents index.</span><span class="sxs-lookup"><span data-stu-id="9876e-280">It is meaningless to compare keys of index entries from different indexes against each other.</span></span>

  - <span data-ttu-id="9876e-281">Une clé est toujours inférieure ou égale à JET_cbKeyMost (255) octets dans une longueur antérieure à Windows Vista.</span><span class="sxs-lookup"><span data-stu-id="9876e-281">A key is always less than or equal to JET_cbKeyMost (255) bytes in length prior to Windows Vista.</span></span> <span data-ttu-id="9876e-282">Sur Windows Vista et les versions ultérieures, les clés peuvent être plus volumineuses.</span><span class="sxs-lookup"><span data-stu-id="9876e-282">On Windows Vista and later releases, keys can be larger.</span></span> <span data-ttu-id="9876e-283">La taille maximale d’une clé est égale à la valeur actuelle de JET_paramKeyMost.</span><span class="sxs-lookup"><span data-stu-id="9876e-283">The maximum size of a key is equal to the current value of JET_paramKeyMost.</span></span>

<span data-ttu-id="9876e-284">Les clés de recherche peuvent prendre plus d’un octet si une option générique a été utilisée.</span><span class="sxs-lookup"><span data-stu-id="9876e-284">Search keys can be one byte longer if a wildcard option was used.</span></span> <span data-ttu-id="9876e-285">Dans ce cas, la clé de recherche sera jusqu’à JET_cbLimitKeyMost (256) octets pour les versions antérieures à Windows Vista et JET_paramKeyMost + 1 octets pour Windows Vista et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="9876e-285">In that case, the search key will be up to JET_cbLimitKeyMost (256) bytes for releases prior to Windows Vista and JET_paramKeyMost + 1 bytes for Windows Vista and later releases.</span></span>

<span data-ttu-id="9876e-286">Il existe une ramification très importante pour cette taille de clé maximale.</span><span class="sxs-lookup"><span data-stu-id="9876e-286">There is a very important ramification to this maximum key size.</span></span> <span data-ttu-id="9876e-287">Lorsqu’il existe une entrée d’index qui a des valeurs de colonne suffisamment grandes pour provoquer la génération d’une clé pour cet index qui, autrement, serait plus grande que cette taille maximale, cette clé est tronquée en silence à cette taille maximale.</span><span class="sxs-lookup"><span data-stu-id="9876e-287">Whenever there is an index entry that has column values that are large enough to cause a key to be generated for that index that would otherwise be larger than this maximum size, that key is silently truncated at that maximum size.</span></span> <span data-ttu-id="9876e-288">Cela entraîne deux effets :</span><span class="sxs-lookup"><span data-stu-id="9876e-288">This causes two effects:</span></span>

  - <span data-ttu-id="9876e-289">Pour les entrées dans un index unique, les entrées qui, autrement, seraient uniques sont déclarées en tant que doublons.</span><span class="sxs-lookup"><span data-stu-id="9876e-289">For entries in a unique index, it will cause entries that would otherwise be unique to be declared as duplicates.</span></span>

  - <span data-ttu-id="9876e-290">Pour les entrées dans tous les index, la troncation de clé entraînera la déclaration des entrées d’index qui, autrement, ne correspondent pas aux critères de recherche d’une clé de recherche donnée à déclarer comme correspondances.</span><span class="sxs-lookup"><span data-stu-id="9876e-290">For entries in all indexes, key truncation will cause index entries that would otherwise not match the search criteria of a given search key to be declared as matches.</span></span>

<span data-ttu-id="9876e-291">Les applications doivent anticiper cette troncation et l’éviter ou compenser ses effets.</span><span class="sxs-lookup"><span data-stu-id="9876e-291">Applications must anticipate this truncation and either avoid it or compensate for its effects.</span></span> <span data-ttu-id="9876e-292">Dans Windows Vista, un nouvel indicateur d’index, JET_bitIndexDisallowTruncation, a été ajouté pour faciliter la tâche des applications afin d’éviter les troncations de clé.</span><span class="sxs-lookup"><span data-stu-id="9876e-292">In Windows Vista, a new index flag, JET_bitIndexDisallowTruncation, has been added to make it easier for applications to prevent key truncations.</span></span> <span data-ttu-id="9876e-293">Pour plus d’informations sur cette option d’indexation, consultez la structure [JET_INDEXCREATE](./jet-indexcreate-structure.md) .</span><span class="sxs-lookup"><span data-stu-id="9876e-293">See the [JET_INDEXCREATE](./jet-indexcreate-structure.md) structure for more information on this indexing option.</span></span>

#### <a name="requirements"></a><span data-ttu-id="9876e-294">Configuration requise</span><span class="sxs-lookup"><span data-stu-id="9876e-294">Requirements</span></span>

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<tbody>
<tr class="odd">
<td><p><span data-ttu-id="9876e-295"><strong>Client</strong></span><span class="sxs-lookup"><span data-stu-id="9876e-295"><strong>Client</strong></span></span></p></td>
<td><p><span data-ttu-id="9876e-296">Nécessite Windows Vista, Windows XP ou Windows 2000 professionnel.</span><span class="sxs-lookup"><span data-stu-id="9876e-296">Requires Windows Vista, Windows XP, or Windows 2000 Professional.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-297"><strong>Serveur</strong></span><span class="sxs-lookup"><span data-stu-id="9876e-297"><strong>Server</strong></span></span></p></td>
<td><p><span data-ttu-id="9876e-298">Requiert Windows Server 2008, Windows Server 2003 ou Windows 2000 Server.</span><span class="sxs-lookup"><span data-stu-id="9876e-298">Requires Windows Server 2008, Windows Server 2003, or Windows 2000 Server.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-299"><strong>En-tête</strong></span><span class="sxs-lookup"><span data-stu-id="9876e-299"><strong>Header</strong></span></span></p></td>
<td><p><span data-ttu-id="9876e-300">Déclaré dans esent. h.</span><span class="sxs-lookup"><span data-stu-id="9876e-300">Declared in Esent.h.</span></span></p></td>
</tr>
<tr class="even">
<td><p><span data-ttu-id="9876e-301"><strong>Bibliothèque</strong></span><span class="sxs-lookup"><span data-stu-id="9876e-301"><strong>Library</strong></span></span></p></td>
<td><p><span data-ttu-id="9876e-302">Utilisez ESENT. lib.</span><span class="sxs-lookup"><span data-stu-id="9876e-302">Use ESENT.lib.</span></span></p></td>
</tr>
<tr class="odd">
<td><p><span data-ttu-id="9876e-303"><strong>DLL</strong></span><span class="sxs-lookup"><span data-stu-id="9876e-303"><strong>DLL</strong></span></span></p></td>
<td><p><span data-ttu-id="9876e-304">Requiert ESENT.dll.</span><span class="sxs-lookup"><span data-stu-id="9876e-304">Requires ESENT.dll.</span></span></p></td>
</tr>
</tbody>
</table>


#### <a name="see-also"></a><span data-ttu-id="9876e-305">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="9876e-305">See Also</span></span>

[<span data-ttu-id="9876e-306">JET_ERR</span><span class="sxs-lookup"><span data-stu-id="9876e-306">JET_ERR</span></span>](./jet-err.md)  
[<span data-ttu-id="9876e-307">JET_GRBIT</span><span class="sxs-lookup"><span data-stu-id="9876e-307">JET_GRBIT</span></span>](./jet-grbit.md)  
[<span data-ttu-id="9876e-308">JET_SESID</span><span class="sxs-lookup"><span data-stu-id="9876e-308">JET_SESID</span></span>](./jet-sesid.md)  
[<span data-ttu-id="9876e-309">JET_TABLEID</span><span class="sxs-lookup"><span data-stu-id="9876e-309">JET_TABLEID</span></span>](./jet-tableid.md)  
[<span data-ttu-id="9876e-310">JetCreateIndex</span><span class="sxs-lookup"><span data-stu-id="9876e-310">JetCreateIndex</span></span>](./jetcreateindex-function.md)  
[<span data-ttu-id="9876e-311">JetRetrieveKey</span><span class="sxs-lookup"><span data-stu-id="9876e-311">JetRetrieveKey</span></span>](./jetretrievekey-function.md)  
[<span data-ttu-id="9876e-312">JetSeek</span><span class="sxs-lookup"><span data-stu-id="9876e-312">JetSeek</span></span>](./jetseek-function.md)  
[<span data-ttu-id="9876e-313">JetSetIndexRange</span><span class="sxs-lookup"><span data-stu-id="9876e-313">JetSetIndexRange</span></span>](./jetsetindexrange-function.md)
