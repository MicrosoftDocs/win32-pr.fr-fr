---
description: Le type binaire de type sémantique est l’un des types de format de clé. Ce type est constitué d’une clé dans la table binaire fournie par l’utilisateur.
ms.assetid: b6a25100-9f3e-4207-b56f-0c27ee16f188
title: Type binaire
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: fda0711b53f865bbd844514ed2429d97d91e07a3
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 01/07/2021
ms.locfileid: "104528858"
---
# <a name="binary-type"></a><span data-ttu-id="6f283-104">Type binaire</span><span class="sxs-lookup"><span data-stu-id="6f283-104">Binary Type</span></span>

<span data-ttu-id="6f283-105">Le type binaire de [type sémantique](semantic-types.md) est l’un des [types de format de clé](key-format-types.md).</span><span class="sxs-lookup"><span data-stu-id="6f283-105">The Binary Type of [semantic type](semantic-types.md) is one of the [Key Format Types](key-format-types.md).</span></span> <span data-ttu-id="6f283-106">Ce type est constitué d’une clé dans la [table binaire](binary-table.md) fournie par l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="6f283-106">This type consists of a key into the [Binary table](binary-table.md) provided by the user.</span></span>

<span data-ttu-id="6f283-107">L’outil de fusion doit substituer un [identificateur](identifier.md) de Windows Installer valide pour les éléments de ce type.</span><span class="sxs-lookup"><span data-stu-id="6f283-107">The merge tool must substitute a valid Windows Installer [Identifier](identifier.md) for items of this type.</span></span> <span data-ttu-id="6f283-108">Mergemod.dll n’applique pas cette restriction et c’est à l’outil de fusion de s’assurer que l’utilisateur fournit une clé valide dans la table binaire.</span><span class="sxs-lookup"><span data-stu-id="6f283-108">Mergemod.dll does not enforce this restriction and it is up to the merge tool to ensure that the user provides a valid key into the Binary table.</span></span>

<span data-ttu-id="6f283-109">NULL est une valeur valide pour ce type, sauf si msmConfigItemNonNullable a été inclus dans le champ Attributes de la [table ModuleConfiguration](moduleconfiguration-table.md).</span><span class="sxs-lookup"><span data-stu-id="6f283-109">Null is a valid value for this type unless the msmConfigItemNonNullable has been included in the Attributes field of the [ModuleConfiguration table](moduleconfiguration-table.md).</span></span>

<span data-ttu-id="6f283-110">Le type binaire peut être utilisé avec les types de ContextData suivants.</span><span class="sxs-lookup"><span data-stu-id="6f283-110">The Binary type may be used with the following kinds of ContextData.</span></span>

<span data-ttu-id="6f283-111">**ContextData bitmap**</span><span class="sxs-lookup"><span data-stu-id="6f283-111">**Bitmap ContextData**</span></span>

<span data-ttu-id="6f283-112">Un module de fusion configurable peut utiliser ce type pour permettre à l’utilisateur de fournir une clé étrangère à une ligne de la table binaire qui contient une image bitmap.</span><span class="sxs-lookup"><span data-stu-id="6f283-112">A configurable merge module may use this type to enable the user to provide a foreign key to a row in the Binary Table that contains a bitmap image.</span></span> <span data-ttu-id="6f283-113">Mergmod.dll ne garantit pas une taille ou un type de bitmap spécifique et l’outil de fusion doit s’assurer que les données sont une image valide.</span><span class="sxs-lookup"><span data-stu-id="6f283-113">Mergmod.dll does not guarantee any specific size or type of bitmap and the merge tool must ensure that the data is a valid image.</span></span> <span data-ttu-id="6f283-114">Pour spécifier un élément configurable de ce type, les auteurs du module doivent entrer le nom de l’élément configurable dans la colonne nom, entrer « 1 » dans la colonne format, entrer « binaire » dans la colonne Type, puis entrer « bitmap » dans la colonne ContextData de la table ModuleConfiguration.</span><span class="sxs-lookup"><span data-stu-id="6f283-114">To specify a configurable item of this type, module authors should enter the name of the configurable item into the Name column, enter "1" into the Format column, enter "Binary" into the Type column, and enter "Bitmap" into the ContextData column of the ModuleConfiguration table.</span></span>

<span data-ttu-id="6f283-115">**Icône ContextData**</span><span class="sxs-lookup"><span data-stu-id="6f283-115">**Icon ContextData**</span></span>

<span data-ttu-id="6f283-116">Un module de fusion configurable peut utiliser ce type pour permettre à l’utilisateur de fournir une clé étrangère à une ligne de la table binaire qui contient une image d’icône.</span><span class="sxs-lookup"><span data-stu-id="6f283-116">A configurable merge module may use this type to enable the user to provide a foreign key to a row in the Binary Table that contains an icon image.</span></span> <span data-ttu-id="6f283-117">Mergmod.dll ne garantit pas une taille ou un type d’icône spécifique et l’outil de fusion doit s’assurer que les données sont une image valide.</span><span class="sxs-lookup"><span data-stu-id="6f283-117">Mergmod.dll does not guarantee any specific size or type of icon and the merge tool must ensure that the data is a valid image.</span></span> <span data-ttu-id="6f283-118">Pour spécifier un élément configurable de ce type, les auteurs du module doivent entrer le nom de l’élément configurable dans la colonne nom, entrer « 1 » dans la colonne format, entrer « binaire » dans la colonne type et entrer « Icon » dans la colonne ContextData de la table ModuleConfiguration.</span><span class="sxs-lookup"><span data-stu-id="6f283-118">To specify a configurable item of this type, module authors should enter the name of the configurable item into the Name column, enter "1" into the Format column, enter "Binary" into the Type column, and enter "Icon" into the ContextData column of the ModuleConfiguration table.</span></span> <span data-ttu-id="6f283-119">Ce type n’est pas approprié pour une utilisation dans une table de publication.</span><span class="sxs-lookup"><span data-stu-id="6f283-119">This type is not appropriate for use in an advertisement table.</span></span>

<span data-ttu-id="6f283-120">**EXE ContextData**</span><span class="sxs-lookup"><span data-stu-id="6f283-120">**EXE ContextData**</span></span>

<span data-ttu-id="6f283-121">Un module de fusion configurable peut utiliser ce type pour permettre à l’utilisateur de fournir une clé étrangère à une ligne de la table binaire qui contient une image exécutable 32 bits.</span><span class="sxs-lookup"><span data-stu-id="6f283-121">A configurable merge module may use this type to enable the user to provide a foreign key to a row in the Binary Table that contains a 32-bit executable image.</span></span> <span data-ttu-id="6f283-122">Mergmod.dll ne valide pas les données sont valides et l’outil de fusion doit s’assurer que les données sont un fichier PE valide.</span><span class="sxs-lookup"><span data-stu-id="6f283-122">Mergmod.dll does not validate the data is valid and the merge tool must ensure that the data is a valid PE file.</span></span> <span data-ttu-id="6f283-123">Pour spécifier un élément configurable de ce type, les auteurs du module doivent entrer le nom de l’élément configurable dans la colonne nom, entrer « 1 » dans la colonne format, entrer « binaire » dans la colonne type et entrer « EXE » dans la colonne ContextData de la table ModuleConfiguration.</span><span class="sxs-lookup"><span data-stu-id="6f283-123">To specify a configurable item of this type, module authors should enter the name of the configurable item into the Name column, enter "1" into the Format column, enter "Binary" into the Type column, and enter "EXE" into the ContextData column of the ModuleConfiguration table.</span></span>

<span data-ttu-id="6f283-124">**EXE64 ContextData**</span><span class="sxs-lookup"><span data-stu-id="6f283-124">**EXE64 ContextData**</span></span>

<span data-ttu-id="6f283-125">Un module de fusion configurable peut utiliser ce type pour permettre à l’utilisateur de fournir une clé étrangère à une ligne de la table binaire qui contient une image exécutable 32 bits ou 64 bits.</span><span class="sxs-lookup"><span data-stu-id="6f283-125">A configurable merge module may use this type to enable the user to provide a foreign key to a row in the Binary Table that contains either a 32-bit or 64-bit executable image.</span></span> <span data-ttu-id="6f283-126">Mergmod.dll ne valide pas les données sont valides et l’outil de fusion doit s’assurer que les données sont un fichier PE valide.</span><span class="sxs-lookup"><span data-stu-id="6f283-126">Mergmod.dll does not validate the data is valid and the merge tool must ensure that the data is a valid PE file.</span></span> <span data-ttu-id="6f283-127">Pour spécifier un élément configurable de ce type, les auteurs du module doivent entrer le nom de l’élément configurable dans la colonne nom, entrer « 1 » dans la colonne format, entrer « binaire » dans la colonne Type, puis entrer « EXE64 » dans la colonne ContextData de la table ModuleConfiguration.</span><span class="sxs-lookup"><span data-stu-id="6f283-127">To specify a configurable item of this type, module authors should enter the name of the configurable item into the Name column, enter "1" into the Format column, enter "Binary" into the Type column, and enter "EXE64" into the ContextData column of the ModuleConfiguration table.</span></span>

 

 



