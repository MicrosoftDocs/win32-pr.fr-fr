---
description: Les modules de fusion (fichiers. msm) peuvent être créés pour contenir des attributs qui peuvent être configurés par le consommateur du module de fusion.
ms.assetid: 461436f0-3a91-4a49-934a-f975bf13df40
title: Modules de fusion configurables
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 716688d947ae84279dc3409bf97abe4eb58a69d0
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 01/08/2021
ms.locfileid: "106531397"
---
# <a name="configurable-merge-modules"></a><span data-ttu-id="9f2f6-103">Modules de fusion configurables</span><span class="sxs-lookup"><span data-stu-id="9f2f6-103">Configurable Merge Modules</span></span>

<span data-ttu-id="9f2f6-104">Les modules de fusion (fichiers. msm) peuvent être créés pour contenir des attributs qui peuvent être configurés par le consommateur du module de fusion.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-104">Merge modules (.msm files) may be authored to contain attributes that are configurable by the consumer of the merge module.</span></span> <span data-ttu-id="9f2f6-105">Cela permet de configurer le module de fusion au moment où le package d’installation et le module sont fusionnés et installés par l’utilisateur final.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-105">This enables the merge module to be configured at the time the installation package and module are merged and installed by the end-user.</span></span> <span data-ttu-id="9f2f6-106">Les modules de fusion configurables requièrent Mergemod.dll version 2,0, mais ils peuvent s’exécuter sur n’importe quelle version du Windows Installer.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-106">Configurable merge modules require Mergemod.dll version 2.0 but can run on any version of the Windows Installer.</span></span>

<span data-ttu-id="9f2f6-107">L’implémentation de modules de fusion configurables se compose de deux parties.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-107">The implementation of configurable merge modules consists of two parts.</span></span> <span data-ttu-id="9f2f6-108">Tout d’abord, lors de la création du module de fusion (fichier. msm), l’auteur du module de fusion ajoute des informations à la base de données du module qui spécifie les éléments qui peuvent être modifiés et la façon dont ces éléments peuvent être configurés par l’utilisateur du module.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-108">First, when creating the merge module (.msm file), the merge module author adds information to the module database that specifies which items can be modified and how these items can be configured by the module user.</span></span> <span data-ttu-id="9f2f6-109">L’auteur ajoute des entrées aux [tables de base de données de module de fusion](merge-module-database-tables.md) qui sont réservées aux informations configurables ([table ModuleConfiguration](moduleconfiguration-table.md) et [table ModuleSubstitution](modulesubstitution-table.md)), met à jour la [ \_ table de validation](-validation-table.md)et ajoute des entrées pour les tables de module de fusion configurables à la [table ModuleIgnoreTable](moduleignoretable-table.md).</span><span class="sxs-lookup"><span data-stu-id="9f2f6-109">The author adds entries to the [Merge Module Database Tables](merge-module-database-tables.md) that are reserved for configurable information ([ModuleConfiguration table](moduleconfiguration-table.md) and [ModuleSubstitution table](modulesubstitution-table.md)), updates the [\_Validation table](-validation-table.md), and adds entries for the configurable merge module tables to the [ModuleIgnoreTable table](moduleignoretable-table.md).</span></span> <span data-ttu-id="9f2f6-110">Les ajouts à la table ModuleIgnore sont nécessaires pour rendre le module compatible avec les versions de Mergemod.dll antérieures à 2,0.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-110">The additions to the ModuleIgnore table are required to make the module compatible with Mergemod.dll versions earlier than 2.0.</span></span>

<span data-ttu-id="9f2f6-111">Deuxièmement, lors de la fusion du module dans un package d’installation (fichier. msi), l’utilisateur final du module utilise un outil de fusion.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-111">Second, when merging the module into an installation package (.msi file), the end-user of the module uses a merge tool.</span></span> <span data-ttu-id="9f2f6-112">L’outil de fusion appelle Mergemod.dll pour exposer les informations de configuration du module à un outil de configuration du client.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-112">The merge tool calls Mergemod.dll to expose the configuration information in the module to a client configuration tool.</span></span> <span data-ttu-id="9f2f6-113">L’outil de configuration peut interagir avec l’utilisateur final, mais il n’est pas nécessaire pour exposer toutes les options de configuration possibles.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-113">The configuration tool may interact with the end-user but is not required to expose all possible configuration options.</span></span> <span data-ttu-id="9f2f6-114">Si l’utilisateur refuse de fournir une sélection pour un élément configurable, le module peut fournir une valeur par défaut.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-114">If the user declines to provide a selection for a configurable item, the module may provide a default value.</span></span> <span data-ttu-id="9f2f6-115">Une fois que l’utilisateur a effectué les sélections de l’outil de configuration, l’outil de fusion appelle Mergemod.dll pour effectuer la fusion.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-115">After the user gives the configuration tool his selections, the merge tool calls Mergemod.dll to perform the merge.</span></span>

<span data-ttu-id="9f2f6-116">Les modules de fusion configurables sont entièrement compatibles avec les outils antérieurs à la version 2,0 de Mergemod.dll.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-116">Configurable merge modules are fully compatible with tools earlier than Mergemod.dll version 2.0.</span></span> <span data-ttu-id="9f2f6-117">Dans ce cas, l’outil utilise les valeurs par défaut du module.</span><span class="sxs-lookup"><span data-stu-id="9f2f6-117">In these cases, the tool uses the default values in the module.</span></span>

<span data-ttu-id="9f2f6-118">Pour plus d’informations, consultez [utilisation des modules de fusion configurables](using-configurable-merge-modules.md).</span><span class="sxs-lookup"><span data-stu-id="9f2f6-118">For more information, see [Using Configurable Merge Modules](using-configurable-merge-modules.md).</span></span>

 

 



