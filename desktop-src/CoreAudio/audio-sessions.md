---
description: Sessions audio
ms.assetid: b8a1b656-a582-4112-99e9-bd575719ebb3
title: Sessions audio
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 57ae67a3eafe7a76add2fad192823868304e860d
ms.sourcegitcommit: c7add10d695482e1ceb72d62b8a4ebd84ea050f7
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 01/07/2021
ms.locfileid: "104111558"
---
# <a name="audio-sessions"></a><span data-ttu-id="5318a-103">Sessions audio</span><span class="sxs-lookup"><span data-stu-id="5318a-103">Audio Sessions</span></span>

<span data-ttu-id="5318a-104">Une *session audio* est un groupe de flux audio associés qu’un client WASAPI peut gérer collectivement.</span><span class="sxs-lookup"><span data-stu-id="5318a-104">An *audio session* is a group of related audio streams that a WASAPI client can manage collectively.</span></span> <span data-ttu-id="5318a-105">Les clients peuvent contrôler le niveau de volume et l’État muet de chaque session individuelle.</span><span class="sxs-lookup"><span data-stu-id="5318a-105">Clients can control the volume level and muting state of each individual session.</span></span> <span data-ttu-id="5318a-106">Le système applique les paramètres de volume et de silence spécifiés par le client uniformément à tous les flux de la session.</span><span class="sxs-lookup"><span data-stu-id="5318a-106">The system applies client-specified volume and mute settings uniformly to all of the streams in the session.</span></span>

<span data-ttu-id="5318a-107">Lorsqu’un client Initialise un flux audio, il affecte le flux audio à une session audio.</span><span class="sxs-lookup"><span data-stu-id="5318a-107">When a client initializes an audio stream, it assigns the audio stream to an audio session.</span></span> <span data-ttu-id="5318a-108">Pour plus d’informations, consultez [**IAudioClient :: Initialize**](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-initialize).</span><span class="sxs-lookup"><span data-stu-id="5318a-108">For more information, see [**IAudioClient::Initialize**](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-initialize).</span></span>

<span data-ttu-id="5318a-109">Une session audio contient des flux de rendu ou des flux de capture, mais pas les deux.</span><span class="sxs-lookup"><span data-stu-id="5318a-109">An audio session contains either rendering streams or capture streams, but not both.</span></span> <span data-ttu-id="5318a-110">Par défaut, les paramètres volume et muet pour une session de rendu sont persistants entre les redémarrages du système.</span><span class="sxs-lookup"><span data-stu-id="5318a-110">By default, the volume and mute settings for a rendering session are persistent across system restarts.</span></span> <span data-ttu-id="5318a-111">Les paramètres de volume et de sourdine pour une session de capture ne sont pas persistants.</span><span class="sxs-lookup"><span data-stu-id="5318a-111">The volume and mute settings for a capture session are not persistent.</span></span> <span data-ttu-id="5318a-112">(Une session qui contient des flux qui fonctionnent en mode de bouclage est traitée de la même manière qu’une session de capture.</span><span class="sxs-lookup"><span data-stu-id="5318a-112">(A session that contains streams that operate in loopback mode is treated the same as a capture session.</span></span> <span data-ttu-id="5318a-113">Autrement dit, les paramètres de session ne sont pas persistants.</span><span class="sxs-lookup"><span data-stu-id="5318a-113">That is, the session settings are not persistent.</span></span> <span data-ttu-id="5318a-114">Pour plus d’informations sur le mode de bouclage, consultez [enregistrement en boucle](loopback-recording.md).)</span><span class="sxs-lookup"><span data-stu-id="5318a-114">For more information about loopback mode, see [Loopback Recording](loopback-recording.md).)</span></span>

<span data-ttu-id="5318a-115">Chaque flux audio appartient à une seule session.</span><span class="sxs-lookup"><span data-stu-id="5318a-115">Every audio stream belongs to exactly one session.</span></span> <span data-ttu-id="5318a-116">Un client assigne un flux audio à une session particulière au moment où il initialise l’objet de flux.</span><span class="sxs-lookup"><span data-stu-id="5318a-116">A client assigns an audio stream to a particular session at the time that it initializes the stream object.</span></span> <span data-ttu-id="5318a-117">Le flux conserve son appartenance dans la session pendant la durée de vie du flux.</span><span class="sxs-lookup"><span data-stu-id="5318a-117">The stream retains its membership in the session for the lifetime of the stream.</span></span> <span data-ttu-id="5318a-118">Après la création d’un objet de flux, l’objet existe jusqu’à ce qu’un client libère la dernière référence comptée à l’objet, puis l’objet est supprimé.</span><span class="sxs-lookup"><span data-stu-id="5318a-118">After a stream object is created, the object exists until a client releases the last counted reference to the object, and then the object is deleted.</span></span>

<span data-ttu-id="5318a-119">Bien qu’un client ne puisse pas modifier la session à laquelle un flux existant est affecté, il peut obtenir un effet similaire en supprimant le flux (en publiant toutes les références à celui-ci), en créant un nouveau flux pour remplacer le flux supprimé et en assignant le nouveau flux à une autre session.</span><span class="sxs-lookup"><span data-stu-id="5318a-119">Although a client cannot change the session to which an existing stream is assigned, it can achieve a similar effect by deleting the stream (by releasing all references to it), creating a new stream to replace the deleted stream, and assigning the new stream to another session.</span></span>

<span data-ttu-id="5318a-120">Chaque session de rendu représente un sous-ensemble des flux qui forment la combinaison globale qui s’exécute via un [périphérique de point de terminaison audio](audio-endpoint-devices.md)particulier.</span><span class="sxs-lookup"><span data-stu-id="5318a-120">Each rendering session represents a subset of the streams that form the global mix that plays through a particular [audio endpoint device](audio-endpoint-devices.md).</span></span> <span data-ttu-id="5318a-121">La combinaison globale combine toutes les sessions de toutes les applications qui partagent l’appareil.</span><span class="sxs-lookup"><span data-stu-id="5318a-121">The global mix combines all of the sessions from all of the applications that share the device.</span></span>

<span data-ttu-id="5318a-122">Souvent, une application avec plusieurs flux affecte tous ses flux à la même session.</span><span class="sxs-lookup"><span data-stu-id="5318a-122">Frequently, an application with several streams assigns all of its streams to the same session.</span></span> <span data-ttu-id="5318a-123">Toutefois, l’application peut, en option, assigner différents flux à différentes sessions.</span><span class="sxs-lookup"><span data-stu-id="5318a-123">However, the application can, as an option, assign different streams to different sessions.</span></span> <span data-ttu-id="5318a-124">Tout flux que l’application n’attribue pas explicitement à une session appartient à la session par défaut.</span><span class="sxs-lookup"><span data-stu-id="5318a-124">Any stream that the application does not explicitly assign to a session belongs to the default session.</span></span>

<span data-ttu-id="5318a-125">Les applications audio typiques doivent éviter de modifier les paramètres de volume et de sourdine pour les sessions.</span><span class="sxs-lookup"><span data-stu-id="5318a-125">Typical audio applications should avoid modifying the volume and mute settings for sessions.</span></span> <span data-ttu-id="5318a-126">Au lieu de cela, les utilisateurs contrôlent ces paramètres par le biais des interfaces utilisateur des programmes de contrôle.</span><span class="sxs-lookup"><span data-stu-id="5318a-126">Instead, users control these settings through the user interfaces of control programs.</span></span> <span data-ttu-id="5318a-127">Par exemple, dans Windows Vista, le programme fourni par le système, Sndvol.exe, affiche un contrôle du volume et un contrôle muet pour chaque session de rendu active ou récemment active dans le système.</span><span class="sxs-lookup"><span data-stu-id="5318a-127">For example, in Windows Vista, the system-supplied program, Sndvol.exe, displays a volume control and mute control for each active or recently active rendering session in the system.</span></span> <span data-ttu-id="5318a-128">Grâce à ces contrôles, les utilisateurs peuvent ajuster les paramètres de volume et de silence pour toutes les sessions dans le système.</span><span class="sxs-lookup"><span data-stu-id="5318a-128">Through these controls, users can adjust the volume and mute settings for all of the sessions in the system.</span></span>

<span data-ttu-id="5318a-129">Le programme sndvol affiche actuellement des contrôles de volume pour les appareils de point de terminaison de rendu audio uniquement.</span><span class="sxs-lookup"><span data-stu-id="5318a-129">The Sndvol program currently displays volume controls for audio-rendering endpoint devices only.</span></span> <span data-ttu-id="5318a-130">Elle n’affiche pas de contrôles de volume pour les périphériques de capture audio.</span><span class="sxs-lookup"><span data-stu-id="5318a-130">It does not display volume controls for audio-capture devices.</span></span>

<span data-ttu-id="5318a-131">Une session est active si elle contient un ou plusieurs flux actifs.</span><span class="sxs-lookup"><span data-stu-id="5318a-131">A session is active if it contains one or more active streams.</span></span> <span data-ttu-id="5318a-132">Un flux actif est à l' *État en cours d’exécution*.</span><span class="sxs-lookup"><span data-stu-id="5318a-132">An active stream is in the *running state*.</span></span> <span data-ttu-id="5318a-133">Un flux inactif est à l' *état arrêté*.</span><span class="sxs-lookup"><span data-stu-id="5318a-133">An inactive stream is in the *stopped state*.</span></span> <span data-ttu-id="5318a-134">Une session devient active lorsque son premier flux devient actif.</span><span class="sxs-lookup"><span data-stu-id="5318a-134">A session becomes active when its first stream becomes active.</span></span> <span data-ttu-id="5318a-135">Une session devient inactive lorsque son dernier flux actif devient inactif.</span><span class="sxs-lookup"><span data-stu-id="5318a-135">A session becomes inactive when its last active stream becomes inactive.</span></span> <span data-ttu-id="5318a-136">Une fois qu’une session a été inactive pendant un certain temps, le système fait passer l’état de la session de l’état inactif à expiré.</span><span class="sxs-lookup"><span data-stu-id="5318a-136">After a session has been inactive for a period of time, the system changes the state of the session from inactive to expired.</span></span>

<span data-ttu-id="5318a-137">Sndvol affiche les contrôles de volume et de sourdine pour toutes les sessions de rendu actives et inactives.</span><span class="sxs-lookup"><span data-stu-id="5318a-137">Sndvol displays volume and mute controls for all active and inactive rendering sessions.</span></span> <span data-ttu-id="5318a-138">Sndvol supprime les contrôles de volume et de sourdine pour une session lorsque l’état de la session passe d’inactif à expiré, ou lorsque la session se termine.</span><span class="sxs-lookup"><span data-stu-id="5318a-138">Sndvol removes the volume and mute controls for a session when the state of the session changes from inactive to expired, or when the session terminates.</span></span> <span data-ttu-id="5318a-139">(Une session se termine lorsque le dernier de ses flux est supprimé ; autrement dit, lorsqu’un client libère le nombre de références final sur le dernier objet de flux restant dans la session.) La seule exception à cette règle concerne les sons de notification système.</span><span class="sxs-lookup"><span data-stu-id="5318a-139">(A session terminates when the last of its streams is deleted; that is, when a client releases the final reference count on the last remaining stream object in the session.) The one exception to this rule is for system-notification sounds.</span></span> <span data-ttu-id="5318a-140">Sndvol affiche toujours les contrôles volume et muet pour les sons de notification système, quel que soit l’état de la session pour ces sons.</span><span class="sxs-lookup"><span data-stu-id="5318a-140">Sndvol always displays the volume and mute controls for system-notification sounds regardless of the state of the session for these sounds.</span></span>

<span data-ttu-id="5318a-141">En règle générale, un flux appartient à une session qui couvre uniquement le processus qui contient l’application qui a créé le flux.</span><span class="sxs-lookup"><span data-stu-id="5318a-141">Typically, a stream belongs to a session that spans only the process that contains the application that created the stream.</span></span> <span data-ttu-id="5318a-142">Toutefois, les applications ont la possibilité de définir des sessions interprocessus qui combinent des flux de deux processus ou plus.</span><span class="sxs-lookup"><span data-stu-id="5318a-142">However, applications have the option of defining cross-process sessions that combine streams from two or more processes.</span></span>

<span data-ttu-id="5318a-143">WASAPI prend en charge les sessions interprocessus principalement pour que :</span><span class="sxs-lookup"><span data-stu-id="5318a-143">WASAPI supports cross-process sessions primarily so that:</span></span>

-   <span data-ttu-id="5318a-144">Le programme sndvol peut présenter à l’utilisateur un contrôle de volume unique pour gérer les sons de notification système dans toutes les applications.</span><span class="sxs-lookup"><span data-stu-id="5318a-144">The Sndvol program can present the user with a single volume control to manage system-notification sounds across all applications.</span></span>
-   <span data-ttu-id="5318a-145">Un lecteur multimédia qui s’exécute dans un processus peut diffuser du contenu protégé vers un programme de déchiffrement qui s’exécute dans un autre processus.</span><span class="sxs-lookup"><span data-stu-id="5318a-145">A media player that runs in one process can stream protected content to a decryption program that runs in another process.</span></span>

<span data-ttu-id="5318a-146">Comme pour les paramètres de contrôle pour les sessions de rendu spécifiques au processus, les paramètres de contrôle pour les sessions de rendu inter-processus sont, par défaut, persistants au redémarrage du système.</span><span class="sxs-lookup"><span data-stu-id="5318a-146">Similar to the control settings for process-specific rendering sessions, the control settings for cross-process rendering sessions are, by default, persistent across system restarts.</span></span> <span data-ttu-id="5318a-147">WASAPI fournit ce comportement principalement pour l’avantage des sons de notification système, qui doivent conserver les paramètres de volume et de silence de l’utilisateur lorsque la combinaison d’applications varie dans le temps.</span><span class="sxs-lookup"><span data-stu-id="5318a-147">WASAPI provides this behavior primarily for the benefit of system-notification sounds, which must retain the user's volume and mute settings as the application mix varies over time.</span></span>

<span data-ttu-id="5318a-148">Le programme sndvol libelle le contrôle du volume pour chaque session avec un nom d’affichage et une icône.</span><span class="sxs-lookup"><span data-stu-id="5318a-148">The Sndvol program labels the volume control for each session with a display name and an icon.</span></span> <span data-ttu-id="5318a-149">Un client a la possibilité d’attribuer explicitement un nom complet et une icône à une session.</span><span class="sxs-lookup"><span data-stu-id="5318a-149">A client has the option of explicitly assigning a display name and icon to a session.</span></span> <span data-ttu-id="5318a-150">Si le client ne fournit pas ces éléments, sndvol affiche à la place un nom par défaut et une icône par défaut.</span><span class="sxs-lookup"><span data-stu-id="5318a-150">If the client does not provide these items, then Sndvol displays a default name and default icon instead.</span></span> <span data-ttu-id="5318a-151">Le nom par défaut inclut des informations telles que le titre de la fenêtre de l’application.</span><span class="sxs-lookup"><span data-stu-id="5318a-151">The default name incorporates information such as the title of the application window.</span></span> <span data-ttu-id="5318a-152">L’icône par défaut est l’icône de la fenêtre d’application.</span><span class="sxs-lookup"><span data-stu-id="5318a-152">The default icon is the icon for the application window.</span></span> <span data-ttu-id="5318a-153">Dans le cas des sessions spécifiques au processus, ces valeurs par défaut fournissent des informations significatives aux utilisateurs.</span><span class="sxs-lookup"><span data-stu-id="5318a-153">Only in the case of process-specific sessions do these defaults provide meaningful information to users.</span></span> <span data-ttu-id="5318a-154">Notez qu’une session inter-processus peut être associée à plusieurs applications.</span><span class="sxs-lookup"><span data-stu-id="5318a-154">Note that a cross-process session can be associated with more than one application.</span></span> <span data-ttu-id="5318a-155">Dans ce cas, seul le nom d’affichage et l’icône fournis par le client sont significatifs.</span><span class="sxs-lookup"><span data-stu-id="5318a-155">In this case, only a client-provided display name and icon are meaningful.</span></span>

<span data-ttu-id="5318a-156">Bien que les paramètres volume et muet pour une session de rendu soient, par défaut, persistants au redémarrage du système, le nom complet et l’icône fournis par le client ne le sont pas.</span><span class="sxs-lookup"><span data-stu-id="5318a-156">Although the volume and mute settings for a rendering session are, by default, persistent across system restarts, the client-provided display name and icon are not.</span></span> <span data-ttu-id="5318a-157">Pour vous assurer que sndvol affiche le nom et l’icône fournis par le client, le client doit assigner explicitement le nom et l’icône à la session au moment où le client affecte le premier flux à la session.</span><span class="sxs-lookup"><span data-stu-id="5318a-157">To ensure that Sndvol displays the client-provided name and icon, the client must explicitly assign the name and icon to the session at the time that the client assigns the first stream to the session.</span></span> <span data-ttu-id="5318a-158">Le système conserve le nom complet et l’icône d’une session uniquement jusqu’à ce que la session se termine.</span><span class="sxs-lookup"><span data-stu-id="5318a-158">The system retains the display name and icon for a session only until the session terminates.</span></span>

<span data-ttu-id="5318a-159">Chaque session est identifiée par un GUID de session.</span><span class="sxs-lookup"><span data-stu-id="5318a-159">Each session is identified by a session GUID.</span></span> <span data-ttu-id="5318a-160">Au moment où un client ouvre un flux, le client lui affecte ce flux de données.</span><span class="sxs-lookup"><span data-stu-id="5318a-160">At the time that a client opens a stream, the client assigns that stream to a particular session.</span></span> <span data-ttu-id="5318a-161">Le client fournit les deux éléments d’information suivants pour identifier cette session :</span><span class="sxs-lookup"><span data-stu-id="5318a-161">The client supplies the following two pieces of information to identify that session:</span></span>

-   <span data-ttu-id="5318a-162">GUID de session.</span><span class="sxs-lookup"><span data-stu-id="5318a-162">A session GUID.</span></span>
-   <span data-ttu-id="5318a-163">Si la session est une session interprocessus ou spécifique au processus, une session spécifique au processus contient uniquement des flux du processus du client.</span><span class="sxs-lookup"><span data-stu-id="5318a-163">Whether the session is a cross-process or process-specific session—a process-specific session contains only streams from the client's process.</span></span>

<span data-ttu-id="5318a-164">Ces informations sont suffisantes pour distinguer une session particulière de toutes les autres sessions dans le même ordinateur.</span><span class="sxs-lookup"><span data-stu-id="5318a-164">This information is sufficient to distinguish a particular session from all other sessions in the same computer.</span></span> <span data-ttu-id="5318a-165">Le GUID de session pour une session spécifique au processus identifie de façon unique la session uniquement dans l’étendue du processus propriétaire de la session.</span><span class="sxs-lookup"><span data-stu-id="5318a-165">The session GUID for a process-specific session uniquely identifies the session only within the scope of the process that owns the session.</span></span> <span data-ttu-id="5318a-166">En revanche, le GUID de session pour une session inter-processus est unique dans l’étendue de tous les processus qui s’exécutent sur l’ordinateur.</span><span class="sxs-lookup"><span data-stu-id="5318a-166">In contrast, the session GUID for a cross-process session is unique within the scope of all of the processes that are running on the computer.</span></span>

<span data-ttu-id="5318a-167">Dans le cas d’une session spécifique au processus, le système utilise une combinaison de GUID de session et d’ID de processus pour identifier de manière unique la session dans l’étendue de l’ordinateur.</span><span class="sxs-lookup"><span data-stu-id="5318a-167">In the case of a process-specific session, the system uses a combination of session GUID and process ID to uniquely identify the session within the scope of the computer.</span></span> <span data-ttu-id="5318a-168">Par conséquent, si les clients de deux processus distincts affectent leurs flux respectifs à deux sessions spécifiques à un processus avec des GUID de session identiques, le système traite les sessions comme étant séparées, car leurs ID de processus sont différents.</span><span class="sxs-lookup"><span data-stu-id="5318a-168">Thus, if clients in two different processes assign their respective streams to two process-specific sessions with identical session GUIDs, the system treats the sessions as separate because their process IDs are different.</span></span> <span data-ttu-id="5318a-169">En outre, si une session inter-processus utilise le même GUID de session qu’une ou plusieurs sessions spécifiques au processus, le système traite la session interprocessus comme distincte des sessions spécifiques au processus, même si elle partage le même GUID de session.</span><span class="sxs-lookup"><span data-stu-id="5318a-169">In addition, if a cross-process session uses the same session GUID as one or more process-specific sessions, the system treats the cross-process session as distinct from the process-specific sessions, even though they share the same session GUID.</span></span>

<span data-ttu-id="5318a-170">Par exemple, dans Windows Vista, les API de niveau supérieur telles que les fonctions **WaveOutXxx** Windows Multimedia et DirectSound attribuent généralement les flux audio qu’ils créent à la valeur par défaut, les sessions spécifiques au processus qui sont identifiées par le GUID de la valeur GUID de session \_ .</span><span class="sxs-lookup"><span data-stu-id="5318a-170">For example, in Windows Vista, higher-level APIs such as the Windows multimedia **waveOutXxx** functions and DirectSound typically assign the audio streams that they create to default, process-specific sessions that are identified by the session GUID value GUID\_NULL.</span></span> <span data-ttu-id="5318a-171">Pour les clients de ces API, la session par défaut pour chaque processus client est distincte des sessions par défaut pour d’autres processus client, même si les sessions ont des GUID de session identiques.</span><span class="sxs-lookup"><span data-stu-id="5318a-171">For clients of these APIs, the default session for each client process is separate from the default sessions for other client processes, even though the sessions have identical session GUIDs.</span></span> <span data-ttu-id="5318a-172">En outre, si une ou plusieurs applications attribuent des flux à la session interprocessus qui est identifiée par la valeur GUID de session GUID \_ null, le système traite cette session inter-processus comme distincte des sessions par défaut spécifiques au processus qui partagent le même GUID de session.</span><span class="sxs-lookup"><span data-stu-id="5318a-172">In addition, if one or more applications assign streams to the cross-process session that is identified by the session GUID value GUID\_NULL, then the system treats this cross-process session as separate from the default, process-specific sessions that share the same session GUID.</span></span> <span data-ttu-id="5318a-173">En conséquence, le programme sndvol affiche un contrôle de volume distinct pour la session par défaut propre au processus, et il affiche un contrôle de volume supplémentaire pour la session interprocessus qui est identifiée par la valeur GUID de session GUID \_ null, si cette session existe.</span><span class="sxs-lookup"><span data-stu-id="5318a-173">Accordingly, the Sndvol program displays a separate volume control for each client's default, process-specific session, and it displays an additional volume control for the cross-process session that is identified by the session GUID value GUID\_NULL, if that session exists.</span></span>

<span data-ttu-id="5318a-174">Chaque session est associée à un seul périphérique de point de terminaison audio.</span><span class="sxs-lookup"><span data-stu-id="5318a-174">Each session is associated with only one audio endpoint device.</span></span> <span data-ttu-id="5318a-175">Si deux sessions ont des GUID de session et des ID de processus identiques, mais sont associés à des appareils différents, le système traite les deux sessions comme étant séparées.</span><span class="sxs-lookup"><span data-stu-id="5318a-175">If two sessions have identical session GUIDs and process IDs but are associated with different devices, then the system treats the two sessions as separate.</span></span> <span data-ttu-id="5318a-176">Une session ne peut jamais contenir à la fois des flux de capture et de rendu, car un flux de capture peut être associé uniquement à un appareil de capture et un flux de rendu ne peut être associé qu’à un appareil de rendu.</span><span class="sxs-lookup"><span data-stu-id="5318a-176">A session can never contain both capture and rendering streams because a capture stream can be associated only with a capture device and a rendering stream can be associated only with a rendering device.</span></span>

<span data-ttu-id="5318a-177">Comme mentionné précédemment, les paramètres de volume et de sourdine pour une session sont persistants au redémarrage du système.</span><span class="sxs-lookup"><span data-stu-id="5318a-177">As mentioned previously, the volume and mute settings for a session are persistent across system restarts.</span></span> <span data-ttu-id="5318a-178">Plusieurs instances d’une application peuvent créer des sessions spécifiques au processus avec des GUID de session identiques.</span><span class="sxs-lookup"><span data-stu-id="5318a-178">Two or more instances of an application can create process-specific sessions with identical session GUIDs.</span></span> <span data-ttu-id="5318a-179">Par le biais du programme sndvol, l’utilisateur peut sélectionner des paramètres de volume et de sourdine différents pour chacune de ces sessions.</span><span class="sxs-lookup"><span data-stu-id="5318a-179">Through the Sndvol program, the user can select different volume and mute settings for each of these sessions.</span></span> <span data-ttu-id="5318a-180">Une fois ces sessions terminées, le système conserve les paramètres de contrôle d’une seule de ces sessions : la dernière session à se terminer.</span><span class="sxs-lookup"><span data-stu-id="5318a-180">After these sessions terminate, the system retains the control settings of only one of these sessions—the last session to terminate.</span></span> <span data-ttu-id="5318a-181">Par la suite, si une nouvelle instance de l’application crée une session spécifique au processus avec le même GUID de session qu’auparavant, cette session hérite des paramètres de volume et de silence précédemment enregistrés.</span><span class="sxs-lookup"><span data-stu-id="5318a-181">Later, if a new instance of the application creates a process-specific session with the same session GUID as before, that session inherits the previously saved volume and mute settings.</span></span>

<span data-ttu-id="5318a-182">Une application ne doit pas tenter d’ajouter un flux à ou contrôler le niveau de volume d’une session appartenant à une autre application non liée.</span><span class="sxs-lookup"><span data-stu-id="5318a-182">An application should not attempt to add a stream to or control the volume level of a session that is owned by another, unrelated application.</span></span> <span data-ttu-id="5318a-183">En outre, une application ne doit pas tenter de contrôler le niveau de volume de la session gérée par le système pour les sons de notification.</span><span class="sxs-lookup"><span data-stu-id="5318a-183">In addition, an application should not attempt to control the volume level of the system-managed session for notification sounds.</span></span> <span data-ttu-id="5318a-184">Toutefois, une application peut lire un son via la session système pour les sons de notification en appelant la fonction **PlaySound** .</span><span class="sxs-lookup"><span data-stu-id="5318a-184">However, an application can play a sound through the system session for notification sounds by calling the **PlaySound** function.</span></span> <span data-ttu-id="5318a-185">Pour plus d’informations, consultez [sons de notification pour les applications audio héritées](notification-sounds-for-legacy-audio-applications.md).</span><span class="sxs-lookup"><span data-stu-id="5318a-185">For more information, see [Notification Sounds for Legacy Audio Applications](notification-sounds-for-legacy-audio-applications.md).</span></span>

<span data-ttu-id="5318a-186">Une application peut s’inscrire pour recevoir des notifications lorsque l’état d’une session change.</span><span class="sxs-lookup"><span data-stu-id="5318a-186">An application can register to receive notifications when the state of a session changes.</span></span> <span data-ttu-id="5318a-187">Pour plus d’informations, consultez [événements de session audio](audio-session-events.md).</span><span class="sxs-lookup"><span data-stu-id="5318a-187">For more information, see [Audio Session Events](audio-session-events.md).</span></span>

<span data-ttu-id="5318a-188">Dans de rares cas, une application qui crée une session spécifique au processus peut avoir besoin de consolider le contrôle des sessions spécifiques au processus pour deux instances d’application ou plus sous un contrôle de volume unique dans sndvol.</span><span class="sxs-lookup"><span data-stu-id="5318a-188">In rare cases, an application that creates a process-specific session might need to consolidate the control of the process-specific sessions for two or more application instances under a single volume control in Sndvol.</span></span> <span data-ttu-id="5318a-189">Pour plus d’informations, consultez [GROUPING Parameters](grouping-parameters.md).</span><span class="sxs-lookup"><span data-stu-id="5318a-189">For more information, see [Grouping Parameters](grouping-parameters.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="5318a-190">Rubriques connexes</span><span class="sxs-lookup"><span data-stu-id="5318a-190">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="5318a-191">Guide de programmation</span><span class="sxs-lookup"><span data-stu-id="5318a-191">Programming Guide</span></span>](programming-guide.md)
</dt> </dl>

 

 



