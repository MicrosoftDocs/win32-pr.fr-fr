---
title: Configuration des ordinateurs pour RPC sur HTTP
description: Pour utiliser HTTP comme protocole de transport pour RPC, le proxy RPC exécuté dans Internet Information Server (IIS) doit être configuré sur le réseau du programme serveur.
ms.assetid: 5a67af51-924a-4f2b-b013-a4fd1bfaeddd
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 62d1b39276633f3b827f778deef77edd5630c599
ms.sourcegitcommit: 2d531328b6ed82d4ad971a45a5131b430c5866f7
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 09/16/2019
ms.locfileid: "106510012"
---
# <a name="configuring-computers-for-rpc-over-http"></a><span data-ttu-id="fa7c3-103">Configuration des ordinateurs pour RPC sur HTTP</span><span class="sxs-lookup"><span data-stu-id="fa7c3-103">Configuring Computers for RPC over HTTP</span></span>

<span data-ttu-id="fa7c3-104">Pour utiliser HTTP comme protocole de transport pour RPC, le proxy RPC exécuté dans Internet Information Server (IIS) doit être configuré sur le réseau du programme serveur.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-104">To use HTTP as a transport protocol for RPC, RPC Proxy running within Internet Information Server (IIS) must be configured on the server program's network.</span></span> <span data-ttu-id="fa7c3-105">Cette section décrit les options de configuration.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-105">This section describes configuration options.</span></span> <span data-ttu-id="fa7c3-106">Pour obtenir des recommandations sur les meilleures pratiques de programmation et de configuration lors de l’utilisation de RPC sur HTTP, consultez [recommandations relatives au déploiement de RPC sur http](rpc-over-http-deployment-recommendations.md).</span><span class="sxs-lookup"><span data-stu-id="fa7c3-106">For recommendations on best programming and configuration practices when using RPC over HTTP, see [RPC over HTTP Deployment Recommendations](rpc-over-http-deployment-recommendations.md).</span></span> <span data-ttu-id="fa7c3-107">La tâche principale consiste à configurer le proxy RPC pour accepter et transférer les connexions RPC sur HTTP à un programme serveur RPC sur HTTP.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-107">The primary task is configuring the RPC Proxy to accept and forward RPC over HTTP connections to an RPC over HTTP server program.</span></span>

<span data-ttu-id="fa7c3-108">IIS doit d’abord être installé sur l’ordinateur exécutant le proxy RPC.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-108">IIS must first be installed on the machine running the RPC Proxy.</span></span> <span data-ttu-id="fa7c3-109">Une fois IIS installé, le proxy RPC est installé.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-109">Once IIS is installed, the RPC Proxy is installed.</span></span> <span data-ttu-id="fa7c3-110">IIS et le proxy RPC peuvent être installés simultanément à partir d' **Ajout/suppression de composants Windows** dans le panneau de configuration.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-110">Both IIS and the RPC Proxy can be installed simultaneously from **Add/Remove Windows Components** in the Control Panel.</span></span> <span data-ttu-id="fa7c3-111">Le proxy RPC est installé à partir des **services de mise en réseau** et est appelé **proxy RPC sur http** dans le programme d’installation de Windows.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-111">RPC Proxy is installed from **Networking Services**, and is called **RPC over HTTP Proxy** in Windows setup.</span></span> <span data-ttu-id="fa7c3-112">Si IIS et proxy RPC sont installés en même temps, Windows s’assure qu’ils sont installés dans le bon ordre.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-112">If both IIS and RPC Proxy are installed at the same time, Windows ensures they are installed in the proper order.</span></span>

> [!Note]  
> <span data-ttu-id="fa7c3-113">Une fois l’installation terminée, les services IIS doivent être redémarrés s’ils étaient en cours d’exécution pendant le processus d’installation.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-113">After installation is complete, IIS must be restarted if it was running during the installation process.</span></span>

 

<span data-ttu-id="fa7c3-114">Après l’installation du proxy RPC, vous devez effectuer certaines tâches de configuration supplémentaires :</span><span class="sxs-lookup"><span data-stu-id="fa7c3-114">After installed of the RPC Proxy, some additional configuration tasks must be performed:</span></span>

1.  <span data-ttu-id="fa7c3-115">Ouvrez le composant logiciel enfichable MMC IIS et configurez le répertoire virtuel du proxy RPC pour exiger la sécurité, comme expliqué dans la rubrique [sécurité RPC sur http](rpc-over-http-security.md).</span><span class="sxs-lookup"><span data-stu-id="fa7c3-115">Open the IIS MMC snap-in and configure the RPC Proxy virtual directory to require security, as explained in [RPC over HTTP Security](rpc-over-http-security.md).</span></span> <span data-ttu-id="fa7c3-116">Cette étape est requise uniquement si vous envisagez d’utiliser RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-116">This step is required only if you plan to use RPC over HTTP v2.</span></span>
2.  <span data-ttu-id="fa7c3-117">Si IIS n’a pas de certificat installé, un certificat valide doit être obtenu et installé pour cet ordinateur afin d’utiliser SSL lors de la communication avec le proxy RPC.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-117">If IIS does not have a certificate installed, a valid certificate must be obtained and installed for that computer in order to use SSL when communicating with the RPC Proxy.</span></span> <span data-ttu-id="fa7c3-118">Cette étape s’applique uniquement à RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-118">This step is relevant only for RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-119">Dans la mesure où RPC sur HTTP v1 ne prend pas en charge SSL, cette étape peut être omise pour RPC sur HTTP v1.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-119">Since RPC over HTTP v1 does not support SSL, this step can be omitted for RPC over HTTP v1.</span></span>
3.  <span data-ttu-id="fa7c3-120">Définissez la clé **ValidPorts** comme décrit dans [sécurité RPC sur http](rpc-over-http-security.md).</span><span class="sxs-lookup"><span data-stu-id="fa7c3-120">Set the **ValidPorts** key as described in [RPC over HTTP Security](rpc-over-http-security.md).</span></span>

<span data-ttu-id="fa7c3-121">Lorsque ces tâches de configuration sont terminées, le proxy RPC sur HTTP est prêt à transférer les demandes du client RPC sur HTTP vers le serveur RPC sur HTTP.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-121">When these configuration tasks are completed, the RPC over HTTP proxy is ready to forward requests from the RPC over HTTP client to the RPC over HTTP server.</span></span>

## <a name="rpc-over-http-registry-keys"></a><span data-ttu-id="fa7c3-122">Clés de Registre RPC sur HTTP</span><span class="sxs-lookup"><span data-stu-id="fa7c3-122">RPC over HTTP Registry Keys</span></span>

<span data-ttu-id="fa7c3-123">Cette section décrit les clés de Registre supplémentaires utilisées pour configurer le client, le serveur ou le proxy RPC dans une connexion RPC sur HTTP.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-123">This section explains additional registry keys used to configure the client, server or RPC Proxy in an RPC over HTTP connection.</span></span> <span data-ttu-id="fa7c3-124">Ces clés ne sont généralement pas requises ; ils sont fournis ici à des fins d’exhaustivité.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-124">These keys are generally not required; they are provided here for completeness.</span></span>

<span data-ttu-id="fa7c3-125">**HKLM \\ Software \\ Microsoft \\ RPC \\ DefaultChannelLifetime**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-125">**HKLM\\Software\\Microsoft\\Rpc\\DefaultChannelLifetime**</span></span>

<span data-ttu-id="fa7c3-126">DWORD.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-126">DWORD.</span></span> <span data-ttu-id="fa7c3-127">Remplace la durée de vie du canal par défaut.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-127">Overrides the default channel lifetime.</span></span> <span data-ttu-id="fa7c3-128">Utilisé sur le client et le proxy RPC.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-128">Used on the client and the RPC Proxy.</span></span> <span data-ttu-id="fa7c3-129">La durée de vie du canal est exprimée en octets.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-129">The channel lifetime is expressed in bytes.</span></span> <span data-ttu-id="fa7c3-130">S’il n’est pas spécifié, 1 Go est utilisé.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-130">If not specified, 1GB is used.</span></span> <span data-ttu-id="fa7c3-131">Utilisé uniquement dans RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-131">Used only in RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-132">En cas de modification du proxy RPC, IIS doit être redémarré pour que la modification prenne effet.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-132">When changed on RPC Proxy, IIS must be restarted for the change to take effect.</span></span>

\-

<span data-ttu-id="fa7c3-133">**HKLM \\ Software \\ Microsoft \\ RPC \\ ClientReceiveWindow**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-133">**HKLM\\Software\\Microsoft\\Rpc\\ClientReceiveWindow**</span></span>

<span data-ttu-id="fa7c3-134">DWORD.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-134">DWORD.</span></span> <span data-ttu-id="fa7c3-135">Le cas échéant, spécifie la fenêtre de réception utilisée par le client pour les données reçues du proxy RPC.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-135">When present, specifies the receive window used by the client for data received from the RPC Proxy.</span></span> <span data-ttu-id="fa7c3-136">La valeur minimale valide est 8 Ko.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-136">The minimum valid value is 8K.</span></span> <span data-ttu-id="fa7c3-137">La valeur maximale est de 1 Go.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-137">The maximum is 1GB.</span></span> <span data-ttu-id="fa7c3-138">Si la valeur n’est pas présente, 64 Ko est utilisé.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-138">If the value is not present, 64K is used.</span></span> <span data-ttu-id="fa7c3-139">Utilisé uniquement dans RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-139">Used only in RPC over HTTP v2.</span></span>

\-

<span data-ttu-id="fa7c3-140">**HKLM \\ Software \\ Microsoft \\ RPC \\ InProxyReceiveWindow**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-140">**HKLM\\Software\\Microsoft\\Rpc\\InProxyReceiveWindow**</span></span>

<span data-ttu-id="fa7c3-141">DWORD.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-141">DWORD.</span></span> <span data-ttu-id="fa7c3-142">Le cas échéant, spécifie la fenêtre de réception utilisée par le proxy RPC pour les données reçues du client.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-142">When present, specifies the receive window used by the RPC Proxy for data received from the client.</span></span> <span data-ttu-id="fa7c3-143">La valeur minimale valide est 8 Ko.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-143">The minimum valid value is 8K.</span></span> <span data-ttu-id="fa7c3-144">La valeur maximale est de 1 Go.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-144">The maximum is 1GB.</span></span> <span data-ttu-id="fa7c3-145">Si la valeur n’est pas présente, 64 Ko est utilisé.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-145">If the value is not present, 64K is used.</span></span> <span data-ttu-id="fa7c3-146">Utilisé uniquement dans RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-146">Used only in RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-147">Lorsque des modifications sont apportées à cette valeur, IIS doit être redémarré pour que la modification prenne effet.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-147">When changes are made to this value, IIS must be restarted for the change to take effect.</span></span>

\-

<span data-ttu-id="fa7c3-148">**HKLM \\ Software \\ Microsoft \\ RPC \\ OutProxyReceiveWindow**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-148">**HKLM\\Software\\Microsoft\\Rpc\\OutProxyReceiveWindow**</span></span>

<span data-ttu-id="fa7c3-149">DWORD.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-149">DWORD.</span></span> <span data-ttu-id="fa7c3-150">Le cas échéant, spécifie la fenêtre de réception utilisée par le proxy RPC pour les données reçues du serveur.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-150">When present, specifies the receive window used by the RPC Proxy for data received from the server.</span></span> <span data-ttu-id="fa7c3-151">La valeur minimale valide est 8 Ko.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-151">The minimum valid value is 8K.</span></span> <span data-ttu-id="fa7c3-152">La valeur maximale est de 1 Go.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-152">The maximum is 1GB.</span></span> <span data-ttu-id="fa7c3-153">Si la valeur n’est pas présente, 64 Ko est utilisé.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-153">If the value is not present, 64K is used.</span></span> <span data-ttu-id="fa7c3-154">Utilisé uniquement dans RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-154">Used only in RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-155">Lorsque des modifications sont apportées à cette valeur, IIS doit être redémarré pour que la modification prenne effet.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-155">When changes are made to this value, IIS must be restarted for the change to take effect.</span></span>

\-

<span data-ttu-id="fa7c3-156">**HKLM \\ Software \\ Microsoft \\ RPC \\ ServerReceiveWindow**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-156">**HKLM\\Software\\Microsoft\\Rpc\\ServerReceiveWindow**</span></span>

<span data-ttu-id="fa7c3-157">DWORD.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-157">DWORD.</span></span> <span data-ttu-id="fa7c3-158">Le cas échéant, spécifie la fenêtre de réception utilisée par le serveur pour les données reçues du proxy RPC.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-158">When present, specifies the receive window used by the server for data received from the RPC Proxy.</span></span> <span data-ttu-id="fa7c3-159">La valeur minimale valide est 8 Ko.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-159">The minimum valid value is 8K.</span></span> <span data-ttu-id="fa7c3-160">La valeur maximale est de 1 Go.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-160">The maximum is 1GB.</span></span> <span data-ttu-id="fa7c3-161">Si la valeur n’est pas présente, 64 Ko est utilisé.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-161">If the value is not present, 64K is used.</span></span> <span data-ttu-id="fa7c3-162">Utilisé uniquement dans RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-162">Used only in RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-163">Lorsque des modifications sont apportées à cette valeur, IIS doit être redémarré pour que la modification prenne effet.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-163">When changes are made to this value, IIS must be restarted for the change to take effect.</span></span>

\-

<span data-ttu-id="fa7c3-164">**\\Stratégies logicielles HKLM \\ \\ Microsoft \\ Windows NT \\ RPC \\ MinimumConnectionTimeout**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-164">**HKLM\\Software\\Policies\\Microsoft\\Windows NT\\Rpc\\MinimumConnectionTimeout**</span></span>

<span data-ttu-id="fa7c3-165">DWORD.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-165">DWORD.</span></span> <span data-ttu-id="fa7c3-166">Le cas échéant, spécifie le délai de connexion minimal utilisé par le client et le proxy RPC, en secondes.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-166">When present, specifies the minimum connection timeout used by the client and RPC Proxy, in seconds.</span></span> <span data-ttu-id="fa7c3-167">Le délai d’expiration réel utilisé est le plus bas de cette valeur et le délai d’attente de la connexion inactive IIS.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-167">The actual timeout used is the lower of this value and the IIS idle connection timeout.</span></span> <span data-ttu-id="fa7c3-168">Si la valeur est zéro ou si la clé n’est pas présente, le délai d’attente de la connexion inactive IIS est utilisé.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-168">If zero, or the key is not present, the IIS idle connection timeout is used.</span></span> <span data-ttu-id="fa7c3-169">Utilisé uniquement dans RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-169">Used only in RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-170">Lorsque des modifications sont apportées à cette valeur sur le proxy RPC, IIS doit être redémarré pour que la modification prenne effet.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-170">When changes are made to this value on the RPC Proxy, IIS must be restarted for the change to take effect.</span></span>

\-

<span data-ttu-id="fa7c3-171">**HKLM \\ Software \\ Microsoft \\ RPC \\ UseProxyForIPAddrIfRDNSFails**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-171">**HKLM\\Software\\Microsoft\\Rpc\\UseProxyForIPAddrIfRDNSFails**</span></span>

<span data-ttu-id="fa7c3-172">Lorsqu’elle est présente et définie sur une valeur différente de zéro et qu’une adresse IP numérique est fournie en tant qu’adresse proxy RPC, un client RPC sur HTTP tente une résolution de nom inverse et, en cas d’échec, tente de se connecter au proxy RPC via un proxy HTTP.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-172">When present and set to non-zero, and a numeric IP address is provided as the RPC Proxy address, an RPC over HTTP client will attempt reverse name resolution and, if that fails, will attempt connection to the RPC Proxy through an HTTP proxy.</span></span> <span data-ttu-id="fa7c3-173">Peut être utilisé pour émuler le comportement de Windows NT sur les installations qui requièrent un tel comportement.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-173">Can be used to emulate Windows NT behavior on installations that require such behavior.</span></span> <span data-ttu-id="fa7c3-174">Ignoré pour RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-174">Ignored for RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-175">Utilisé uniquement en cas d’utilisation de RPC sur HTTP v1.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-175">Used only when RPC over HTTP v1 is used.</span></span> <span data-ttu-id="fa7c3-176">Pris en charge uniquement sur Windows 2000 avec Service Pack 3 (SP3) et versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-176">Supported only on Windows 2000 with Service Pack 3 (SP3) and later.</span></span>

\-

<span data-ttu-id="fa7c3-177">**HKLM \\ Software \\ Microsoft \\ RPC \\ RpcProxy \\ AllowAnonymous**</span><span class="sxs-lookup"><span data-stu-id="fa7c3-177">**HKLM\\Software\\Microsoft\\Rpc\\RpcProxy\\AllowAnonymous**</span></span>

<span data-ttu-id="fa7c3-178">DWORD.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-178">DWORD.</span></span> <span data-ttu-id="fa7c3-179">Lorsqu’il n’est pas présent ou défini à zéro, le proxy RPC vérifie si la connexion est authentifiée et si elle est sécurisée (SSL ou une autre forme de chiffrement est utilisée).</span><span class="sxs-lookup"><span data-stu-id="fa7c3-179">When not present or set to zero, the RPC Proxy checks whether the connection is authenticated, and whether it is secure (SSL or other form of encryption is used).</span></span> <span data-ttu-id="fa7c3-180">Si l’un ou l’autre est false, la connexion est rejetée.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-180">If either is false, the connection is rejected.</span></span> <span data-ttu-id="fa7c3-181">Si cette valeur contient des connexions non null, anonymes et non chiffrées sont autorisées.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-181">If this value contains non-zero, anonymous and non-encrypted connections are allowed.</span></span> <span data-ttu-id="fa7c3-182">Ce paramètre est un ajout à tous les paramètres du répertoire virtuel.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-182">This setting is an addition to any virtual directory settings.</span></span> <span data-ttu-id="fa7c3-183">Par exemple, si l’accès anonyme est désactivé au niveau du répertoire virtuel, alors que **AllowAnonymous** est différent de zéro, l’accès anonyme restera bloqué au niveau IIS.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-183">For example, if anonymous access is disabled on the virtual directory level, but **AllowAnonymous** is non-zero, anonymous access will still be blocked at the IIS level.</span></span> <span data-ttu-id="fa7c3-184">Utilisé sur le proxy RPC uniquement dans RPC sur HTTP v2.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-184">Used on the RPC Proxy only in RPC over HTTP v2.</span></span> <span data-ttu-id="fa7c3-185">Lorsque des modifications sont apportées à cette valeur sur le proxy RPC, IIS doit être redémarré pour que la modification prenne effet.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-185">When changes are made to this value on the RPC Proxy, IIS must be restarted for the change to take effect.</span></span>

> [!WARNING]
> <span data-ttu-id="fa7c3-186">Microsoft recommande vivement de définir la valeur **AllowAnonymous** sur les systèmes de production pour des raisons de sécurité.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-186">Microsoft strongly recommends against setting the **AllowAnonymous** value on production systems for security considerations.</span></span> <span data-ttu-id="fa7c3-187">La seule raison pour laquelle cette clé doit être définie est le test sur des réseaux fermés sans accès externe.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-187">The only reason this key should be set is for testing on closed networks without outside access.</span></span> <span data-ttu-id="fa7c3-188">Tout système connecté à Internet et exécutant le proxy RPC avec la clé **AllowAnonymous** définie sur une valeur différente de zéro peut être vulnérable aux attaques.</span><span class="sxs-lookup"><span data-stu-id="fa7c3-188">Any system connected to the Internet and running the RPC Proxy with the **AllowAnonymous** key set to a nonzero value may be vulnerable to attacks.</span></span>

 

 

 




