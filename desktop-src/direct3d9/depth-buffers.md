---
description: Une mémoire tampon de profondeur, souvent appelée « z-buffer » ou « w-buffer », est une propriété de l’appareil qui stocke les informations de profondeur à utiliser par Direct3D.
ms.assetid: vs|directx_sdk|~\depth_buffers.htm
title: Mémoires tampons de profondeur (Direct3D 9)
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: fc1ab41ba98ca5e3b08980ac90a572a19fc8a72a
ms.sourcegitcommit: a47bd86f517de76374e4fff33cfeb613eb259a7e
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 01/06/2021
ms.locfileid: "104481552"
---
# <a name="depth-buffers-direct3d-9"></a><span data-ttu-id="fa553-103">Mémoires tampons de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-103">Depth Buffers (Direct3D 9)</span></span>

<span data-ttu-id="fa553-104">Une mémoire tampon de profondeur, souvent appelée « z-buffer » ou « w-buffer », est une propriété de l’appareil qui stocke les informations de profondeur à utiliser par Direct3D.</span><span class="sxs-lookup"><span data-stu-id="fa553-104">A depth buffer, often called a z-buffer or a w-buffer, is a property of the device that stores depth information to be used by Direct3D.</span></span> <span data-ttu-id="fa553-105">Lorsque Direct3D effectue le rendu d’une scène sur une surface cible, il peut utiliser la mémoire d’une surface de mémoire tampon de profondeur associée en tant qu’espace de travail pour déterminer la façon dont les pixels de polygones sont occultait l’un l’autre.</span><span class="sxs-lookup"><span data-stu-id="fa553-105">When Direct3D renders a scene to a target surface, it can use the memory in an associated depth-buffer surface as a workspace to determine how the pixels of rasterized polygons occlude one another.</span></span> <span data-ttu-id="fa553-106">Direct3D utilise une surface Direct3D hors écran comme cible dans laquelle les valeurs de couleur finales sont écrites.</span><span class="sxs-lookup"><span data-stu-id="fa553-106">Direct3D uses an off-screen Direct3D surface as the target to which final color values are written.</span></span> <span data-ttu-id="fa553-107">La surface de mémoire tampon de profondeur associée à la surface de la cible de rendu est utilisée pour stocker les informations de profondeur qui indiquent à Direct3D la profondeur de chaque pixel visible dans la scène.</span><span class="sxs-lookup"><span data-stu-id="fa553-107">The depth-buffer surface that is associated with the render-target surface is used to store depth information that tells Direct3D how deep each visible pixel is in the scene.</span></span>

<span data-ttu-id="fa553-108">Quand une scène est pixellisée avec la mise en mémoire tampon de profondeur activée, chaque point sur la surface de rendu est testé.</span><span class="sxs-lookup"><span data-stu-id="fa553-108">When a scene is rasterized with depth buffering enabled, each point on the rendering surface is tested.</span></span> <span data-ttu-id="fa553-109">Les valeurs de la mémoire tampon de profondeur peuvent être la coordonnée z d’un point ou sa coordonnée w homogène-à partir de l’emplacement (x, y, z, w) du point dans l’espace de projection.</span><span class="sxs-lookup"><span data-stu-id="fa553-109">The values in the depth buffer can be a point's z-coordinate or its homogeneous w-coordinate - from the point's (x,y,z,w) location in projection space.</span></span> <span data-ttu-id="fa553-110">Une mémoire tampon de profondeur qui utilise des valeurs z est souvent appelée une mémoire tampon z, et une autre qui utilise des valeurs w est appelée une mémoire tampon w.</span><span class="sxs-lookup"><span data-stu-id="fa553-110">A depth buffer that uses z values is often called a z-buffer, and one that uses w values is called a w-buffer.</span></span> <span data-ttu-id="fa553-111">Chaque type de mémoire tampon de profondeur présente des avantages et des inconvénients, qui sont décrits plus loin.</span><span class="sxs-lookup"><span data-stu-id="fa553-111">Each type of depth buffer has advantages and disadvantages, which are discussed later.</span></span>

<span data-ttu-id="fa553-112">Au début du test, la valeur de profondeur dans le tampon de profondeur est définie sur la plus grande valeur possible pour la scène.</span><span class="sxs-lookup"><span data-stu-id="fa553-112">At the beginning of the test, the depth value in the depth buffer is set to the largest possible value for the scene.</span></span> <span data-ttu-id="fa553-113">La valeur de couleur sur la surface de rendu est définie sur la valeur de couleur d’arrière-plan ou sur la valeur de couleur de la texture d’arrière-plan à ce stade.</span><span class="sxs-lookup"><span data-stu-id="fa553-113">The color value on the rendering surface is set to either the background color value or the color value of the background texture at that point.</span></span> <span data-ttu-id="fa553-114">Chaque polygone de la scène est testé pour voir s’il croise les coordonnées actuelles (x, y) sur la surface de rendu.</span><span class="sxs-lookup"><span data-stu-id="fa553-114">Each polygon in the scene is tested to see if it intersects with the current coordinate (x,y) on the rendering surface.</span></span> <span data-ttu-id="fa553-115">Si c’est le cas, la valeur de profondeur, qui est la coordonnée z dans une mémoire tampon z, et la coordonnée w dans une mémoire tampon w-au point actuel sont testées pour voir s’il est plus petit que la valeur de profondeur stockée dans le tampon de profondeur.</span><span class="sxs-lookup"><span data-stu-id="fa553-115">If it does, the depth value - which will be the z coordinate in a z-buffer, and the w coordinate in a w-buffer - at the current point is tested to see if it is smaller than the depth value stored in the depth buffer.</span></span> <span data-ttu-id="fa553-116">Si la profondeur de la valeur de polygone est plus petite, elle est stockée dans le tampon de profondeur et la valeur de couleur du polygone est écrite sur le point actuel sur la surface de rendu.</span><span class="sxs-lookup"><span data-stu-id="fa553-116">If the depth of the polygon value is smaller, it is stored in the depth buffer and the color value from the polygon is written to the current point on the rendering surface.</span></span> <span data-ttu-id="fa553-117">Si la valeur de profondeur du polygone à ce point est supérieure, le polygone suivant de la liste est testé.</span><span class="sxs-lookup"><span data-stu-id="fa553-117">If the depth value of the polygon at that point is larger, the next polygon in the list is tested.</span></span> <span data-ttu-id="fa553-118">Ce processus est illustré dans le diagramme suivant.</span><span class="sxs-lookup"><span data-stu-id="fa553-118">This process is shown in the following diagram.</span></span>

![diagramme des valeurs de profondeur de test](images/zbuffer.png)

> [!Note]  
> <span data-ttu-id="fa553-120">Bien que la plupart des applications n’utilisent pas cette fonctionnalité, vous pouvez modifier la comparaison utilisée par Direct3D pour déterminer quelles valeurs sont placées dans la mémoire tampon de profondeur et par la suite, la surface de rendu cible.</span><span class="sxs-lookup"><span data-stu-id="fa553-120">Although most applications don't use this feature, you can change the comparison that Direct3D uses to determine which values are placed in the depth buffer and subsequently the render-target surface.</span></span> <span data-ttu-id="fa553-121">Pour ce faire, modifiez la valeur de l' \_ État de rendu D3DRS ZFUNC.</span><span class="sxs-lookup"><span data-stu-id="fa553-121">To do so, change the value for the D3DRS\_ZFUNC render state.</span></span> <span data-ttu-id="fa553-122">Sur un matériel, la modification de la fonction de comparaison peut désactiver le test z hiérarchique.</span><span class="sxs-lookup"><span data-stu-id="fa553-122">On some hardware, changing the compare function may disable hierarchical z testing.</span></span>

 

<span data-ttu-id="fa553-123">Presque tous les accélérateurs sur le marché prennent en charge la mise en mémoire tampon z, ce qui fait de z-buffer le type de mémoire tampon de profondeur le plus courant aujourd’hui.</span><span class="sxs-lookup"><span data-stu-id="fa553-123">Nearly all accelerators on the market support z-buffering, making z-buffers the most common type of depth buffer today.</span></span> <span data-ttu-id="fa553-124">Néanmoins omniprésent, les tampons z présentent des inconvénients.</span><span class="sxs-lookup"><span data-stu-id="fa553-124">However ubiquitous, z-buffers have their drawbacks.</span></span> <span data-ttu-id="fa553-125">En raison des mathématiques impliquées, les valeurs z générées dans une mémoire tampon z ont tendance à ne pas être distribuées uniformément dans la plage de la mémoire tampon z (en général 0,0 à 1,0, inclusivement).</span><span class="sxs-lookup"><span data-stu-id="fa553-125">Due to the mathematics involved, the generated z values in a z-buffer tend not to be distributed evenly across the z-buffer range (typically 0.0 to 1.0, inclusive).</span></span> <span data-ttu-id="fa553-126">Plus précisément, le ratio entre les plans de découpage FAR et near affecte fortement la manière dont les valeurs z sont distribuées de manière inégale.</span><span class="sxs-lookup"><span data-stu-id="fa553-126">Specifically, the ratio between the far and near clipping planes strongly affects how unevenly z values are distributed.</span></span> <span data-ttu-id="fa553-127">À l’aide d’une distance plane jusqu’au taux de distance à proximité du plan de 100, 90% de la plage de mémoire tampon de profondeur est consacrée aux 10 premiers% de la plage de profondeur de la scène.</span><span class="sxs-lookup"><span data-stu-id="fa553-127">Using a far-plane distance to near-plane distance ratio of 100, 90 percent of the depth buffer range is spent on the first 10 percent of the scene depth range.</span></span> <span data-ttu-id="fa553-128">Les applications typiques pour le divertissement ou les simulations visuelles avec des scènes extérieures nécessitent souvent des ratios plan/proche du plan de n’importe où entre 1 000 et 10 000.</span><span class="sxs-lookup"><span data-stu-id="fa553-128">Typical applications for entertainment or visual simulations with exterior scenes often require far-plane/near-plane ratios of anywhere between 1,000 to 10,000.</span></span> <span data-ttu-id="fa553-129">À un ratio de 1 000, 98% de la plage est passé sur les 2 premiers% de la plage de profondeur, et la distribution se complique avec des ratios plus élevés.</span><span class="sxs-lookup"><span data-stu-id="fa553-129">At a ratio of 1,000, 98 percent of the range is spent on the first 2 percent of the depth range, and the distribution becomes worse with higher ratios.</span></span> <span data-ttu-id="fa553-130">Cela peut entraîner des artefacts de surface masqués dans les objets distants, en particulier lors de l’utilisation de mémoires tampons de profondeur 16 bits, la profondeur de bit la plus souvent prise en charge.</span><span class="sxs-lookup"><span data-stu-id="fa553-130">This can cause hidden surface artifacts in distant objects, especially when using 16-bit depth buffers, the most commonly supported bit-depth.</span></span>

<span data-ttu-id="fa553-131">Une mémoire tampon de profondeur basée sur w, en revanche, est souvent plus uniformément répartie entre les plans de clip near et Far qu’une mémoire tampon z.</span><span class="sxs-lookup"><span data-stu-id="fa553-131">A w-based depth buffer, on the other hand, is often more evenly distributed between the near and far clip planes than a z-buffer.</span></span> <span data-ttu-id="fa553-132">L’avantage clé est que le rapport entre les distances des plans de découpage FAR et near n’est plus un problème.</span><span class="sxs-lookup"><span data-stu-id="fa553-132">The key benefit is that the ratio of distances for the far and near clipping planes is no longer an issue.</span></span> <span data-ttu-id="fa553-133">Cela permet aux applications de prendre en charge des plages maximales de grande taille, tout en continuant d’obtenir une mise en mémoire tampon de profondeur relativement précise jusqu’au point oculaire.</span><span class="sxs-lookup"><span data-stu-id="fa553-133">This allows applications to support large maximum ranges, while still getting relatively accurate depth buffering close to the eye point.</span></span> <span data-ttu-id="fa553-134">Une mémoire tampon de profondeur basée sur w n’est pas parfaite et peut parfois présenter des artefacts de surface masqués pour des objets proches.</span><span class="sxs-lookup"><span data-stu-id="fa553-134">A w-based depth buffer isn't perfect, and can sometimes exhibit hidden surface artifacts for near objects.</span></span> <span data-ttu-id="fa553-135">L’une des inconvénients de l’approche w-buffer est liée à la prise en charge matérielle : la mise en mémoire tampon w n’est pas prise en charge aussi largement que la mise en mémoire tampon z dans le matériel.</span><span class="sxs-lookup"><span data-stu-id="fa553-135">Another drawback to the w-buffered approach is related to hardware support: w-buffering isn't supported as widely in hardware as z-buffering.</span></span>

<span data-ttu-id="fa553-136">L’utilisation d’une mémoire tampon z requiert une surcharge lors du rendu.</span><span class="sxs-lookup"><span data-stu-id="fa553-136">Using a z-buffer requires overhead during rendering.</span></span> <span data-ttu-id="fa553-137">Diverses techniques peuvent être utilisées pour optimiser le rendu lors de l’utilisation de la mise en mémoire tampon z.</span><span class="sxs-lookup"><span data-stu-id="fa553-137">Various techniques can be used to optimize rendering when using z-buffers.</span></span> <span data-ttu-id="fa553-138">Pour plus d’informations, consultez [Z-buffer performance](performance-optimizations.md).</span><span class="sxs-lookup"><span data-stu-id="fa553-138">For details, see [Z-Buffer Performance](performance-optimizations.md).</span></span>

> [!Note]  
> <span data-ttu-id="fa553-139">L’interprétation réelle d’une valeur de profondeur est spécifique au convertisseur.</span><span class="sxs-lookup"><span data-stu-id="fa553-139">The actual interpretation of a depth value is specific to the renderer.</span></span>

 

<span data-ttu-id="fa553-140">Cette section présente des informations sur l’utilisation des tampons de profondeur pour la ligne masquée et la suppression de la surface masquée.</span><span class="sxs-lookup"><span data-stu-id="fa553-140">This section presents information about using depth buffers for hidden line and hidden surface removal.</span></span>

-   [<span data-ttu-id="fa553-141">Interrogation de la prise en charge des tampons de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-141">Querying for Depth Buffer Support (Direct3D 9)</span></span>](querying-for-depth-buffer-support.md)
-   [<span data-ttu-id="fa553-142">Création d’un tampon de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-142">Creating a Depth Buffer (Direct3D 9)</span></span>](creating-a-depth-buffer.md)
-   [<span data-ttu-id="fa553-143">Activation de la mise en mémoire tampon de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-143">Enabling Depth Buffering (Direct3D 9)</span></span>](enabling-depth-buffering.md)
-   [<span data-ttu-id="fa553-144">Récupération d’une mémoire tampon de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-144">Retrieving a Depth Buffer (Direct3D 9)</span></span>](retrieving-a-depth-buffer.md)
-   [<span data-ttu-id="fa553-145">Effacement des tampons de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-145">Clearing Depth Buffers (Direct3D 9)</span></span>](clearing-depth-buffers.md)
-   [<span data-ttu-id="fa553-146">Modification de l’accès en écriture au tampon de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-146">Changing Depth Buffer Write Access (Direct3D 9)</span></span>](changing-depth-buffer-write-access.md)
-   [<span data-ttu-id="fa553-147">Modification des fonctions de comparaison du tampon de profondeur (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="fa553-147">Changing Depth Buffer Comparison Functions (Direct3D 9)</span></span>](changing-depth-buffer-comparison-functions.md)

## <a name="related-topics"></a><span data-ttu-id="fa553-148">Rubriques connexes</span><span class="sxs-lookup"><span data-stu-id="fa553-148">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="fa553-149">Rendu Direct3D</span><span class="sxs-lookup"><span data-stu-id="fa553-149">Direct3D Rendering</span></span>](direct3d-rendering.md)
</dt> </dl>

 

 



